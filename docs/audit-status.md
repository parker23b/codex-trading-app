# Audit status

This document is an audit log for InvestMate findings, evidence gaps, and follow-up actions. It is not a requirements spec; requirements live in `docs/spec/`.

## Current readiness blockers

InvestMate is **not ready for live or demo trading** while these blockers remain open. Startup/reload runtime-loop duplication now has backend lease evidence, but remaining P1 behaviours still need route/backend/frontend test evidence proportionate to their risk before any live/demo trading.

### P0 blockers

| Severity | Finding ID | Why it blocks readiness |
| --- | --- | --- |
| None | None | No open P0 blocker is currently tracked after the `AUDIT-RUNTIME-001` startup-leadership fix. |

### P1 blockers preventing safe supervised operation

| Severity | Finding ID | Why it blocks readiness |
| --- | --- | --- |
| P1 | AUDIT-LIFE-005 | Simulated fills/closes now have broker-result source fields, simulated entry persistence evidence, backend simulated close provenance on closed trades/positions plus trade route serialization, and rendered trade-table simulated-close evidence, but broader frontend/browser simulated provenance remains incomplete. |
| P1 | AUDIT-002 | `99-spec-coverage-matrix.md` does not yet cover or explicitly group all P0/P1 boundary and surface requirements, so future audits could miss safety-critical controls. |
| P1 | AUDIT-005 | `risk_truth_confidence` labels remain provisional rather than authoritative, which weakens backend/frontend parity audits for operator risk truth. |
| P1 | AUDIT-API-006 | Several frontend-consumed operator-critical routes still lack modelled or documented response schemas. |
| P1 | AUDIT-API-008 | Control-plane, strategy, allocation-alert, review/advisory, watchlist/strategy-watchlist, allocation-cycle, TradeIntent decision lifecycle, selected execution lifecycle, selected close broker-action, selected deployment/coverage/market-data/polling health-transition service events, and selected reconciliation/recovery paths now have session-bound audit evidence, but other mutation/broker-action paths still need durable audit-preservation proof. |
| P1 | AUDIT-UI-002 | Frontend execution and risk-confidence mappings now have narrow source/helper and rendered pending/unknown/simulated regression slices, but broader backend/frontend enum parity remains incomplete. |
| P1 | AUDIT-UI-004 | Allocation alert acknowledge/resolve now has source and rendered component evidence for backend error-detail display, refresh-failure display, and success-after-refresh source behavior, and AIMEE advisory-question controls now disclose review persistence before submission; broader mutation-control coverage remains incomplete. |
| P1 | AUDIT-UI-006 | Rendered/frontend regressions now cover pending execution, risk confidence, alert mutation-error and refresh-failure states, AIMEE passive/advisory source boundaries, narrow AIMEE control-plane/open-risk/Operate/strategies/events telemetry-unavailable states, rendered AIMEE advisory persistence copy, control-plane family missing open-risk truth, live-view all-source outage truth, dashboard stale-feed stream truth, coverage watchlist polling-fallback truth, markets catalogue-unavailable count truth, markets watchlist/streaming provenance that does not imply trading approval, risk budget exposure-unavailable truth, rendered trade-table simulated close provenance, strategy open-position stop provenance, and visible test-only destructive reset labeling, but no broad browser/e2e suite covers all operator-critical loading/error/degraded/unknown/passive/mutation states. |
| P1 | AUDIT-TEST-001 | Route/API tests do not prove passive no-write guarantees, active-read side effects, mutation errors, audit preservation, or test-only route gating. |
| P1 | AUDIT-TEST-002 | Selected control-plane, strategy, allocation-alert, review/advisory, watchlist/strategy-watchlist, allocation-cycle, TradeIntent decision lifecycle, execution lifecycle, close broker-action, deployment/coverage/market-data/polling health-transition service, reconciliation, and recovery tests now bypass the global domain-event no-op through session-bound audit persistence, but remaining broker-action and other background audit paths still need proof. |
| P1 | AUDIT-SEC-002 | Error logging and domain-event journaling can persist full tracebacks, raw broker responses, account/order payloads, and raw exception messages without a redaction boundary. |
| P1 | AUDIT-SEC-003 | Secrets are not currently tracked, but ignored local env files contain real broker/API credentials and historical commits contain SQLite trading databases. |
| P1 | AUDIT-DEP-001 | The installed Python environment has known CVEs, Python dependencies are not locked, and frontend dependency hygiene has outdated and unused packages. |
| P1 | AUDIT-DB-001 | Database evolution relies on `create_all()` plus SQLite-only patch helpers, with current schema drift and no migration framework. |
| P1 | AUDIT-OBS-001 | Operator observability still depends partly on best-effort domain events and in-process health counters, though selected route, allocation/decision lifecycle, execution lifecycle, close broker-action, deployment/coverage/market-data/polling health-transition, and recovery/reconciliation events now have durable session-bound evidence. |

### Missing evidence blocking confidence

- Route/API: no HTTP `TestClient` suite proves passive no-write behaviour, active-read side effects, route response contracts, or mutation error/audit preservation. `/testing/*` production gating now has router-registration/direct-route regression evidence, but no broad HTTP route harness exists.
- Frontend: narrow frontend regressions now prove risk-page backend-unavailable metadata is carried into unavailable risk display state, nav fallback/provenance slices avoid nominal/broker-confirmed/demo-environment overstatement, `loadWithMeta()` no longer accepts caller-provided fallback data, production modules have a mock/demo/fake/fixture import guard, control, telemetry, markets, and dashboard summary fallback source values fail closed or unavailable, live-view derived confidence cannot be `HIGH` with unavailable sources, allocation-alert mutation source code catches/displays mutation failures before success copy and distinguishes post-mutation refresh failures from clean success, and a rendered component harness proves pending execution notification, unknown/simulated risk truth, allocation-alert mutation-error and refresh-failure display, narrow AIMEE control-plane/open-risk/Operate/strategies/events telemetry-unavailable display, control-plane family missing open-risk truth, live-view all-source outage truth, dashboard stale-feed stream truth, coverage watchlist polling-fallback truth, markets catalogue-unavailable count truth, markets watchlist/streaming provenance that remains separate from trading approval, risk budget exposure-unavailable truth, rendered trade-table simulated local close provenance, strategy open-position stop provenance, and visible test-only destructive reset labeling. No broad browser/e2e suite proves full loading/error/empty/stale/degraded/fallback, passive-vs-mutation, AIMEE passive/advisory separation, freshness, or complete enum parity.
- Audit trail: selected control-plane, strategy, allocation-alert, review/advisory, watchlist/strategy-watchlist, allocation-cycle, TradeIntent decision lifecycle, execution lifecycle, close broker-action, deployment/reconcile, unmanaged-open-risk, Tier 2 market-data/coverage, polling fallback/stale/recovered health transitions, reconciliation, and recovery paths now have session-bound `DomainEvent` persistence regressions, and selected operator route families return `503` when required audit writes fail. Many tests still globally mock durable `DomainEventService.record_event`, so audit persistence is not yet proven for broad broker-action and remaining mutation/background paths.
- Security: backend auth-policy tests now prove mutation/test-only endpoints and write-on-read refresh routes require an operator identity in production-like operation, but no HTTP `TestClient` suite exists and no redaction tests prove broker/account/secrets are excluded from logs, domain events, and API errors.
- Database/supply chain: no migration drift tests, Python lockfile/constraints evidence, or dependency-vulnerability CI gate proves a reproducible production dependency/database state.
- Product/docs: README now documents safe local usage, setup prerequisites, expected degraded startup states, route-reference handoff, and current limitations; automated docs checks are still not part of CI.
- Matrix completeness: 68 boundary/surface P0/P1 spec IDs are still missing from explicit matrix rows or grouped notes.

## Remediation categories

| Category | Finding IDs |
| --- | --- |
| P0 unsafe lifecycle/risk/broker behaviour | AUDIT-RUNTIME-001 |
| P0 missing proof for safety-critical behaviour | AUDIT-TEST-001, AUDIT-TEST-002, AUDIT-LIFE-006 |
| P1 read/write boundary issue | AUDIT-API-001, AUDIT-API-002, AUDIT-API-003 |
| P1 frontend truth/provenance issue | AUDIT-RISK-003, AUDIT-UI-002, AUDIT-UI-004, AUDIT-UI-006, AUDIT-005; fixed narrow slices: AUDIT-UI-001, AUDIT-UI-003, AUDIT-UI-007 |
| P1 broker boundary/fake fidelity issue | AUDIT-LIFE-005, AUDIT-BROKER-005; fixed: AUDIT-BROKER-001 |
| P1 route/API contract issue | AUDIT-API-006, AUDIT-API-008 |
| P1 security/secrets/logging issue | AUDIT-SEC-002, AUDIT-SEC-003 |
| P1 dependency/database/observability issue | AUDIT-DEP-001, AUDIT-DB-001, AUDIT-OBS-001 |
| P1 test evidence gap | AUDIT-TEST-001, AUDIT-TEST-002, AUDIT-TEST-003, AUDIT-002 |
| P2 documentation/maintainability issue | AUDIT-001, AUDIT-004, AUDIT-006, AUDIT-LIFE-006, AUDIT-UI-005, AUDIT-BROKER-005 |

## Prioritised remediation backlog

| Order | Finding IDs | Severity | Category | Affected spec IDs | Recommended task | Expected files/areas | Required tests | Blocking reason | Suggested Codex prompt type |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | AUDIT-LIFE-001 | P0 | Verified 2026-05-08 | PROD-004, PROD-005, BROKER-002, BROKER-012, BROKER-013, STATE-002, STATE-006, STATE-008, FLOW-ENTRY-001 | Fixed: broker-neutral pending/timeout/rate-limit/unknown/ambiguous result statuses exist; acknowledged-only, entry `TimeoutError`, rate-limit/unknown DTOs, IG post-ack confirmation lookup failures, duplicate submitted-entry retry, broker-reference reconciliation correlation, `/executions` serialization, and frontend execution surfaces now preserve explicit lifecycle/audit state instead of filled/failed/duplicate-submitted/unlinked-adoption truth. | `backend/app/core/broker.py`; `backend/app/core/ig_broker.py`; `backend/app/services/strategy_service.py`; `backend/app/services/reconciliation_service.py`; `backend/app/services/trade_service.py`; `backend/tests/test_strategy_service.py`; `backend/tests/test_reconciliation_service.py`; `backend/tests/test_execution_routes.py`; `backend/tests/test_ig_broker_sizing.py`; `frontend/lib/types.ts`; `frontend/components/dashboard/notification-center.tsx`; `frontend/components/dashboard/dashboard-live.tsx`; `frontend/components/strategy/strategy-live.tsx` | `test_audit_life_001_acknowledged_only_entry_does_not_create_fill_truth`; `test_audit_life_001_entry_timeout_preserves_ambiguous_manual_review_state`; `test_audit_life_001_non_final_entry_results_preserve_manual_review_state`; `test_audit_life_001_blocks_submitted_entry_duplicate_retry_until_review`; `test_audit_life_001_reconciliation_links_ambiguous_entry_by_broker_reference`; `test_audit_life_001_execution_response_preserves_ambiguity_and_correlation`; `test_audit_life_001_ig_place_order_confirmation_rate_limit_returns_manual_review_dto`; `test_audit_life_001_ig_place_order_confirmation_timeout_returns_manual_review_dto`; `npm run typecheck` | Verified for this finding. Broker-fake/idempotency evidence is now verified under `AUDIT-BROKER-002`; frontend behavioural harness remains under `AUDIT-UI-006`. | Completed |
| 2 | AUDIT-LIFE-002 | P0 | Verified 2026-05-07 | PROD-004, PROD-011, BROKER-004, BROKER-011, BROKER-012, STATE-005, STATE-007, STATE-008, FLOW-EXIT-001 | Fixed: close authority is preserved for open-risk intents after close exception, partial close, rejected close, or ambiguous close results. | `backend/app/services/strategy_service.py`; `backend/app/services/trade_service.py`; `backend/tests/test_strategy_service.py` | `test_close_failure_keeps_position_open_and_flags_manual_review`; `test_partial_close_result_moves_execution_to_manual_review`; `test_audit_life_002_incomplete_close_preserves_close_admissible_intent` | Verified by targeted strategy-service regressions and adjacent lifecycle tests. Route/frontend visibility remains covered by broader API/UI gaps. | Completed |
| 3 | AUDIT-LIFE-003 | P0 | Verified 2026-05-07 | PROD-009, PROD-011, BROKER-008, STATE-005, STATE-010, FLOW-RUNTIME-001, FLOW-RECOVERY-001 | Fixed: startup recovery now attaches broker-confirmed open risk to explicit `RECOVERED_POSITION_ATTACHED` intent/position evidence before honoring persisted `STOPPED` runtime mode. | `backend/app/services/runtime_recovery_service.py`; `backend/tests/test_runtime_recovery_service.py` | `test_audit_life_003_stopped_runtime_recovers_remote_open_position`; `test_audit_life_003_stopped_runtime_attaches_intent_to_confirmed_local_position` | Verified by targeted runtime recovery regressions and adjacent recovery/reconciliation/control-plane suites. Route/frontend open-risk visibility remains covered by broader API/UI gaps. | Completed |
| 4 | AUDIT-BROKER-003 | P0 | Verified 2026-05-07 | BROKER-007, BROKER-015, RISK-002, RISK-015, STATE-008, FLOW-ENTRY-001 | Fixed: broker-neutral market status now fails closed unless the status is in an explicit open allowlist. | `backend/app/services/market_status_service.py`; `backend/tests/test_market_status_service.py`; `backend/tests/test_strategy_service.py` | `test_audit_broker_003_market_status_fails_closed_for_missing_or_unknown_values`; `test_market_status_blocks_known_closed_or_unavailable_states`; `test_market_status_blocks_missing_broker_update_time_even_with_price`; `test_market_status_blocks_stale_broker_update_time`; `test_market_status_blocks_broker_dealing_restrictions`; `test_audit_broker_003_unknown_market_status_blocks_entry_even_when_tradable` | Verified by targeted and adjacent backend tests. Route/frontend evidence for displaying blocked reasons remains covered by broader API/UI gaps. | Completed |
| 5 | AUDIT-SEC-001 | P0 | Fixed 2026-05-08 | API-010, TEST-016, UI-004, UI-006 | Fixed: production-like operator and broker-adjacent mutation/refresh routes now have a backend bearer-token boundary, and credentialed CORS no longer accepts arbitrary localhost/127.0.0.1 ports via regex. | `backend/app/api/auth.py`; `backend/app/main.py`; `backend/app/core/config.py`; `backend/tests/test_operator_auth_boundary.py`; `frontend/lib/api.ts` | `test_audit_sec_001_all_state_changing_routes_require_operator_auth_policy`; `test_audit_sec_001_write_on_read_refresh_routes_require_operator_auth_policy`; missing/invalid/unconfigured/valid token tests; CORS regex regression; `npm run typecheck` | Fixed for this finding. HTTP `TestClient` coverage is still blocked by missing `httpx` and remains under `AUDIT-TEST-001`; frontend behavioural mutation-error tests remain under `AUDIT-UI-006`. | Completed |
| 6 | AUDIT-RUNTIME-001 | P0 | Verified 2026-05-08 | ARCH-013, BROKER-012, BROKER-013, STATE-008, FLOW-RUNTIME-001, TEST-006 | Fixed: FastAPI lifespan now acquires a database-backed runtime leadership lease before startup recovery, market-data polling, streaming, and runtime health heartbeat loops. A non-leader worker skips those autonomous loops, the leader heartbeats/releases the lease, and a stale lease can be taken over. | `backend/app/main.py`; `backend/app/models/runtime_leadership.py`; `backend/app/services/runtime_leadership_service.py`; `backend/app/db/init_db.py`; `backend/app/core/config.py`; `backend/tests/test_runtime_leadership_service.py` | `test_audit_runtime_001_live_runtime_leader_lease_blocks_duplicate_owner`; `test_audit_runtime_001_lifespan_skips_autonomous_loops_without_leadership`; `test_audit_runtime_001_lifespan_releases_leader_lease_on_shutdown`; `test_audit_runtime_001_expired_runtime_leader_lease_can_be_taken_over`; full backend suite `backend/.venv/bin/pytest backend/tests -q` (`192 passed`) | Verified for startup/reload/worker loop duplication. Broker-fake/idempotency evidence is now verified under `AUDIT-BROKER-002`; execution-time revalidation coverage remains under `AUDIT-RISK-002`. | Completed |
| 7 | AUDIT-RISK-002 | P1 | Verified 2026-05-10 | RISK-009, RISK-015, BROKER-009, BROKER-015, TEST-003, TEST-013 | Fixed: execution-time account, sizing, broker metadata, market-status, and submitted/fill material drift evidence now fail closed or surface visibly at the relevant boundary. | `backend/app/services/strategy_service.py`; `backend/app/api/routes/executions.py`; `backend/tests/test_strategy_service.py`; `backend/tests/test_execution_routes.py`; `frontend/lib/types.ts`; `frontend/lib/execution-notifications.ts`; `frontend/components/dashboard/notification-center.tsx`; `frontend/tests/execution-notifications.test.mjs` | `test_audit_risk_002_execution_revalidates_account_before_submission`; `test_audit_risk_002_execution_blocks_material_account_equity_drift`; `test_audit_risk_002_execution_blocks_available_funds_below_approved_risk`; `test_audit_risk_002_execution_revalidates_sizing_quote_before_submission`; `test_audit_risk_002_execution_blocks_material_broker_sizing_quote_drift`; `test_audit_risk_002_execution_blocks_material_broker_unit_risk_drift`; `test_audit_risk_002_execution_blocks_material_approximate_size_drift`; `test_audit_risk_002_execution_blocks_material_approximate_unit_risk_drift`; `test_audit_risk_002_execution_revalidates_market_status_without_cache`; `test_audit_risk_002_execution_metadata_failure_before_submission_is_audited`; `test_audit_risk_002_execution_response_surfaces_material_risk_drift`; `AUDIT-RISK-002 execution notification displays material submitted/fill risk drift`; `AUDIT-RISK-002 execution notification displays critical submitted/fill risk drift`; `AUDIT-RISK-002 execution notification omits drift text without drift flags`; `npm run test:frontend`; `npm run typecheck`. Broker-fake contract evidence is verified under `AUDIT-BROKER-002`. | Verified for this finding. Broader frontend harness/degraded-state coverage remains under `AUDIT-UI-006`, route/API audit depth under `AUDIT-TEST-001`, and risk read-surface fallback truth under `AUDIT-RISK-003`. | Completed |
| 8 | AUDIT-RISK-001 | P1 | Fixed 2026-05-08 | RISK-012, RISK-013, RISK-017, FLOW-RISK-001, STATE-006, TEST-001, TEST-013 | Fixed: partial-fill exposure read models now split filled provisional live risk from remaining reserved/leaves risk. | `backend/app/services/strategy_service.py`; `backend/app/services/allocation_read_service.py`; `backend/tests/test_allocation_read_service.py` | `test_audit_risk_001_partial_fill_preserves_residual_reserved_and_provisional_live_risk`; targeted allocation-read suite (`10 passed`); targeted strategy-service suite (`39 passed`) | Fixed for backend execution/read-model accounting. Frontend behavioural fixture evidence for risk surfaces remains under `AUDIT-UI-006`/`RISK-017`. | Completed |
| 9 | AUDIT-API-002, AUDIT-API-003 | P1 | Verified 2026-05-11 | API-001, API-002, API-008, ARCH-005, ARCH-010, AIMEE-011 | Fixed: coverage/watchlist/feed-state GET routes now use passive watchlist plan snapshots, and review GET routes default to non-persisting previews with explicit `persist=true` for active-read review persistence. | `backend/app/services/watchlist_service.py`; `backend/app/services/coverage_service.py`; `backend/app/api/routes/markets.py`; `backend/app/api/routes/ai_reviewer.py`; `backend/app/api/auth.py`; `backend/tests/test_passive_read_routes.py`; `backend/tests/test_operator_auth_boundary.py` | `test_audit_api_002_strategy_watchlist_get_does_not_sync_watchlist_state`; `test_audit_api_002_feed_state_get_does_not_sync_watchlist_state`; `test_audit_api_002_coverage_summary_get_does_not_sync_watchlist_state`; `test_audit_api_003_review_gets_are_passive_without_explicit_persist`; `test_audit_api_003_review_get_persist_true_is_explicit_active_read`; updated auth-policy assertions; full backend suite `backend/.venv/bin/pytest backend/tests -q` (`246 passed`) | Verified for the route-function read/write boundary slice. Broad HTTP `TestClient` route harness, frontend passive-call tests beyond existing static guards, and response schema gaps remain under `AUDIT-TEST-001`, `AUDIT-UI-006`, and `AUDIT-API-006`. | Completed |
| 10 | AUDIT-LIFE-005, AUDIT-API-008, AUDIT-TEST-002 | P1 | P1 broker boundary/fake fidelity issue | BROKER-014, API-010, PROD-012 | Complete remaining simulated provenance surfaces and durable mutation audit behaviour. | Broker DTO consumers, domain event persistence, route mutation services, frontend execution/risk surfaces. | Remaining browser/e2e simulated-provenance tests; route/HTTP contract tests; remaining audit-event failure tests. | Operator and audit truth can still be weakened outside the simulated-entry/close, rendered trade-table provenance, and selected control-plane/strategy/allocation-alert/review/watchlist/execution/recovery/reconciliation audit slices. `AUDIT-BROKER-001` is fixed for non-adapter IG exception leakage; manual/AUTO ownership is fixed under `AUDIT-LIFE-004`; first-class sizing currency is fixed under `AUDIT-BROKER-004`; selected operator mutation, execution lifecycle, and recovery/reconciliation paths now have session-bound durable audit tests under `AUDIT-API-008`/`AUDIT-TEST-002`. | Continue cross-cutting remediation |
| 11 | AUDIT-UI-002, AUDIT-UI-004, AUDIT-UI-006 | P1 | P1 frontend truth/provenance issue | UI-001, UI-002, UI-004, UI-005, UI-006, UI-007, UI-008, UI-011, RISK-006, TEST-004, TEST-011, TEST-012, TEST-016 | Add frontend test harness and fix critical operator surfaces so unknown/degraded/provisional/mutation-failure states cannot render as healthy truth. | Frontend API client, risk allocation controls, markets, dashboard/live rendered states, AIMEE drawer. | Component/API-client tests for load failure, stale data, unknown enums, risk confidence, mutation error detail, passive calls, and rendered critical-state fixtures. | Supervised operation depends on truthful operator visibility. `AUDIT-RISK-003`, `AUDIT-UI-001`, `AUDIT-UI-003`, and `AUDIT-UI-007` are fixed for their source/static slices, but broader rendered frontend fixtures remain under `AUDIT-UI-006`. | Frontend harness + UX fixes |
| 12 | AUDIT-API-006, AUDIT-002, AUDIT-005, AUDIT-SEC-002, AUDIT-SEC-003, AUDIT-DEP-001, AUDIT-DB-001, AUDIT-OBS-001 | P1 | P1 route/API contract issue; P1 security/secrets/logging issue; P1 dependency/database/observability issue | API-003, API-004, API-010, ARCH-009, ARCH-011, ARCH-012, AIMEE-009, RISK-003, RISK-004, RISK-006, RISK-011, BROKER-010, TEST-016 | Model/document frontend-consumed response schemas, centralize `risk_truth_confidence`, add redaction and durable observability contracts, introduce real DB migrations, and gate dependency audits in CI. | Pydantic response models/docs, frontend types, logging/domain-event service, IG broker errors, pyproject/lockfiles, Alembic migrations, health/telemetry surfaces. | Contract tests, backend/frontend risk-label parity tests, redaction tests, audit-persistence failure tests, migration drift tests, dependency audit CI evidence. | Contract, security, dependency, and DB drift can hide backend truth, leak sensitive data, or inflate readiness confidence. | Contract/schema + platform hardening |
| 13 | AUDIT-001, AUDIT-004, AUDIT-006, AUDIT-LIFE-006, AUDIT-UI-005, AUDIT-BROKER-005, AUDIT-TEST-003 | P2 | P2 documentation/maintainability issue | Multiple | Clean up spec path wording, unresolved evidence index, transition-table maintainability, mutation-control UX notes, broker construction boundary, and matrix automation. | Docs/specs, transition ownership design, dependency injection notes, CI/matrix checks. | Matrix maintenance check; terminal transition tests where promoted to safety work. | These do not outrank open P0/P1 safety defects but will prevent drift. | Cleanup + review automation |

## Next implementation phases

| Phase | Goal | Included backlog orders | Exit criteria |
| --- | --- | --- | --- |
| Phase 1 | Fix/verify P0 lifecycle, broker, risk, auth, and runtime-loop issues. | 1-6 | No P0 implementation blocker remains open; each fix has a regression test and audit evidence. |
| Phase 2 | Add missing P0/P1 backend route, lifecycle, broker, risk, read-only, audit, and runtime tests. | 7-10 | Route no-write, active-read side-effect, broker fake contract, lifecycle ambiguity, risk accounting, runtime duplication, and domain-event persistence tests exist. |
| Phase 3 | Add frontend test harness and operator truth/provenance tests. | 11 | Critical UI surfaces prove unknown/degraded/stale/provisional/simulated/mutation-error states. |
| Phase 4 | Improve response schemas/OpenAPI/frontend type contracts and platform hardening. | 12 | Operator-critical dict responses are modelled or documented, frontend type contracts are tested, secrets/logs are redacted, migrations exist, and dependency audit evidence is repeatable. |
| Phase 5 | Documentation/readme cleanup and automated coverage-matrix checks. | 13 | Matrix links P0/P1 spec IDs to evidence or explicit gaps; stale wording and path ambiguity are resolved. |

## Human decisions needed

| Decision | Related findings | Needed before |
| --- | --- | --- |
| Should ambiguous close/reconciliation flows create `PENDING_CONFIRMATION`, `RECONCILIATION_NEEDED`, `NEEDS_MANUAL_REVIEW`, or a smaller set of equivalent states? | AUDIT-LIFE-002 | Phase 1 implementation |
| Should explicit mutation-like GET query paths such as `/allocation/alerts?refresh=true` and `/reviews/*?persist=true` eventually move to POST? | AUDIT-003, AUDIT-API-003 | Not blocking the current direct-route fix; track as future API ergonomics if operator archival/refresh workflows grow. |
| What is the authoritative `risk_truth_confidence` enum and simulated/live provenance vocabulary? | AUDIT-005, AUDIT-LIFE-005, AUDIT-RISK-003, AUDIT-UI-002 | Phase 3/4 contract work |
| In production-like demo mode, should `/testing/*` be removed from routing entirely or gated by auth/config? | AUDIT-API-004 | Resolved 2026-05-08: removed from router registration unless `TESTING_ROUTES_ENABLED=true`; frontend control/API helper require `NEXT_PUBLIC_TESTING_CONTROLS_ENABLED=true`. |
| What operator authentication model is acceptable for local/demo operation: static bearer token, local-only mTLS, reverse-proxy auth, or a first-party login/session model? | AUDIT-SEC-001 | Resolved 2026-05-08 for the current local/demo boundary: backend static bearer token via `OPERATOR_API_TOKEN`, enforced automatically in production-like `APP_ENV` values and whenever a token is configured; local development without a token remains auth-optional. Stronger session/proxy auth can be tracked as future platform hardening. |
| Should autonomous runtime loops run in a dedicated single worker, or should every process acquire DB/advisory leader leases before recovery, polling, streaming, and broker-submission orchestration? | AUDIT-RUNTIME-001 | Resolved 2026-05-08 for startup/background loops: every process must acquire a DB-backed runtime leadership lease before recovery, polling, streaming, and health heartbeat loops. Dedicated worker and broker-action idempotency hardening can still be considered under later platform work. |

## Audit summary

| Area | Status | Blocking issues | Notes |
| --- | --- | --- | --- |
| Spec consistency | In progress | AUDIT-002, AUDIT-005 | Source IDs are unique; no missing source references or malformed markdown tables found. |
| Audit log structure | Updated | None | This file now records summary, statuses, severities, open findings, evidence, next actions, and run history. |
| Coverage matrix | Open | AUDIT-002 | Core invariant rows exist, but boundary/surface rows need explicit matrix coverage or stated grouping. |
| Backend API routes | In progress | AUDIT-API-006, AUDIT-API-008 | Implementation route audit completed for registered FastAPI routes and direct service call paths; known passive read/write slices now have direct route-function evidence, and selected control-plane/strategy/allocation-alert/review/watchlist operator mutations plus selected strategy/deployment/coverage service events now have durable audit evidence. Schema gaps, remaining mutation audit behavior, and route-wide HTTP tests are still pending. |
| Backend lifecycle | Audited | AUDIT-LIFE-002, AUDIT-LIFE-003, AUDIT-LIFE-006 | Entry intent authority is mostly enforced and tested; acknowledged-only entry, entry timeout ambiguity, duplicate submitted-entry retry, broker-reference reconciliation correlation, route serialization, and frontend execution contract/display code now preserve explicit lifecycle state, failed/incomplete close attempts now preserve close-admissible open-risk ownership, stopped-runtime recovery now records broker-confirmed open risk before pausing, and direct runtime-mode changes now preserve manual/AUTO ownership. Remaining transition-table enforcement needs remediation. |
| Broker boundary | Audited | AUDIT-LIFE-005, AUDIT-BROKER-001, AUDIT-BROKER-005, AUDIT-002 | Entry/close broker mutations are routed through strategy/recovery services, broker-neutral market-status fail-closed handling is verified, broker-neutral ambiguous result statuses now exist, the broker fake contract matrix is verified, sizing account currency is now first-class broker-neutral DTO data, and non-adapter code now catches broker-neutral `BrokerError` instead of `IGBrokerError`. Simulated entry provenance has backend persistence evidence; simulated close provenance now has backend/read-model serialization plus narrow rendered trade-table evidence. Broader route/HTTP contract and browser/e2e provenance remain weak. |
| Security and secrets | Audited | AUDIT-SEC-002, AUDIT-SEC-003, AUDIT-API-004 | No current tracked `.env` secret was found. General mutation/refresh auth and credentialed CORS regex exposure are fixed under `AUDIT-SEC-001`, but logs/domain events can still expose sensitive internals, historical SQLite DB commits remain in git history, and HTTP route-level auth coverage is still part of broader route-test work. |
| Dependency and supply chain | Audited | AUDIT-DEP-001 | `npm audit` is clean, but Python installed packages include known CVEs, Python dependencies are not locked, frontend packages are outdated, and unused/extraneous dependency evidence needs cleanup. |
| Database and migrations | Audited | AUDIT-DB-001 | Schema is managed with `create_all()` plus SQLite-only patch helpers rather than a migration framework; current DB schema drift was observed for runtime mode/control mode nullability. |
| Runtime and concurrency | Audited | AUDIT-LIFE-002 | Startup/reload/multi-worker background-loop duplication is now prevented by a DB-backed runtime leadership lease. Execution-time broker/account/sizing/market-status drift revalidation is verified under `AUDIT-RISK-002`; process-local runtime guards and JSON action-key lookup still need separate lifecycle evidence. |
| Observability and logging | Audited | AUDIT-OBS-001, AUDIT-SEC-002, AUDIT-API-008, AUDIT-TEST-002 | Health and domain-event surfaces exist; selected operator mutation/review routes now fail rather than return clean success when required audit persistence fails; selected close broker-action, strategy runtime, duplicate retry, deployment, unmanaged-open-risk, Tier 2 market-data/coverage, polling health-transition, reconciliation, and recovery service events now persist through the active session. Broader durable audit persistence, redaction, true multi-process degraded state, and route/frontend alerting evidence remain weak. |
| Product/docs accuracy | Fixed | None | README is now a compact entry point with current no-live/demo-dealing readiness, quick start, app surfaces including `/live` and AIMEE, docs map, API route-reference handoff, and known-risk summary. Detailed safe-usage, setup, architecture, and trade-lifecycle material now lives in focused docs. `docs/spec/99-spec-coverage-matrix.md` links were also made portable. |
| Frontend operator UI | Audited | AUDIT-002, AUDIT-005, AUDIT-RISK-003, AUDIT-UI-001, AUDIT-UI-002, AUDIT-UI-003, AUDIT-UI-004, AUDIT-UI-005, AUDIT-UI-006, AUDIT-UI-007 | Risk/allocation surfaces expose many confidence labels and AIMEE passive refresh is technically isolated; `AUDIT-RISK-003` is fixed for risk-page backend-unavailable fallback truth; `AUDIT-UI-001` is fixed for nav refresh/fallback truth; `AUDIT-UI-003` is fixed for telemetry-derived broker provenance labels; `AUDIT-UI-007` is fixed for the production mock/fallback boundary; and `AUDIT-UI-006` now has rendered/source slices for pending execution notifications, unknown/simulated risk truth pills, allocation-alert mutation error detail, AIMEE passive/advisory separation, AIMEE unavailable/telemetry truth, rendered AIMEE advisory persistence copy, control-plane family missing open-risk truth, live-view all-source outage truth, dashboard stale-feed stream truth, coverage watchlist polling-fallback truth, markets catalogue-unavailable truth, markets watchlist/streaming provenance, risk budget exposure-unavailable truth, rendered trade-table simulated close provenance, strategy open-position stop provenance, and test-only destructive reset labeling. Complete enum parity, broader mutation-control coverage, active-read polling, browser/e2e non-rendering checks, and freshness display beyond these narrow slices remain gaps. |
| Testing evidence | Audited | AUDIT-002, AUDIT-RISK-003, AUDIT-UI-006, AUDIT-TEST-001, AUDIT-TEST-002, AUDIT-TEST-003 | Backend tests cover many allocation/lifecycle paths, the broker-fake contract matrix now covers strict read/sizing/position outcomes, `AUDIT-BROKER-001` has a broker-neutral exception leakage scan, `AUDIT-LIFE-005` has simulated order/close DTO, simulated-entry persistence, and simulated-close persistence/read-model regressions, `AUDIT-RISK-002` adds focused frontend notification behavioral tests, `AUDIT-RISK-003` adds a narrow risk-page unavailable fallback regression, `AUDIT-UI-007` adds source/static fallback guards, `backend/tests/test_mutation_audit_events.py` adds selected durable route-mutation, review/advisory, and watchlist audit evidence, `backend/tests/test_lifecycle_audit_events.py` proves selected execution lifecycle events persist despite the global event mock and records audit-write failure on the execution, `backend/tests/test_strategy_service.py`, `backend/tests/test_control_plane_service.py`, `backend/tests/test_coverage_allocator_service.py`, and `backend/tests/test_market_data_service.py` now prove selected close broker-action, strategy runtime, duplicate retry, deployment, unmanaged-open-risk, Tier 2 market-data/coverage, and polling health-transition events persist despite the global event mock, `backend/tests/test_reconciliation_service.py` and `backend/tests/test_runtime_recovery_service.py` now prove selected recovery/reconciliation domain events persist despite the global event mock, `frontend/tests/aimee-passive-boundary.test.mjs` adds AIMEE passive/advisory source-boundary evidence, and `frontend/tests/rendered-operator-states.test.mjs` adds rendered SSR component evidence for pending execution, allocation-alert mutation-error, AIMEE advisory persistence copy, unknown/simulated risk truth, AIMEE unavailable/telemetry truth, control-plane family missing open-risk, live-view all-source outage, dashboard stale-feed stream, coverage watchlist polling-fallback, markets catalogue-unavailable, markets watchlist/streaming provenance, risk budget exposure-unavailable, rendered trade-table simulated close provenance, strategy open-position stop provenance, and test-only destructive reset states. Broader route, browser/e2e, remaining service/background domain-event persistence, and construction-only/process evidence remain incomplete. |

## Audit findings

| ID | Severity | Status | Area | Affected spec IDs | Finding | Evidence | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AUDIT-001 | P2 | Open | Spec location / audit scope | All spec files | The requested scope and previous audit log wording referenced `specs/`, but this repository's spec set is under `docs/spec/`. | No top-level `specs/` directory exists. `docs/audit-status.md` previously said requirements live in `specs/`; repo instructions and discovered files point to `docs/spec/`. | Keep future audit prompts, scripts, and audit-log wording aligned on `docs/spec/`, or create an explicit alias if a top-level `specs/` path is desired. |
| AUDIT-002 | P1 | Open | Coverage matrix | ARCH-BND-001 through ARCH-BND-006; BROKER-BND-001 through BROKER-BND-006; UI-OPERATE-001 through UI-STRATEGY-001; UI-BND-001 through UI-BND-007; FLOW-BND-001 through FLOW-BND-008; STATE-BND-001 through STATE-BND-009; RISK-BND-001 through RISK-BND-009; AIMEE-BND-001 through AIMEE-BND-008; TEST-BND-001 through TEST-BND-007 | P0/P1 boundary and surface requirements are not individually present in `99-spec-coverage-matrix.md` and are not explicitly grouped by ID in matrix notes. | Test-evidence extraction found 192 unique source P0/P1 IDs across `docs/spec/00` through `10`, excluding the matrix itself. 68 source IDs are absent from matrix rows or explicit matrix text; all missing IDs are boundary/surface rows. The matrix maintenance rule allows grouping, but grouped coverage must be stated explicitly rather than implied. | Add matrix rows for these IDs, or update parent-row notes to list the exact grouped boundary/surface IDs and the evidence/gap that covers each group. |
| AUDIT-003 | P1 | Verified | Route classification | API-001, API-002, API-008, ARCH-010, RISK-018, FLOW-RISK-001, UI-003, UI-BND-002, TEST-012 | Fixed: default allocation-alert reads are passive, explicit `refresh=true` remains the active-read/refresh path, and passive frontend load/polling no longer requests refresh writes. | `AllocationAlertService.list_alerts()` now defaults to `refresh=False`; `GET /allocation/alerts` declares `refresh: bool = Query(default=False)`; `GET /allocation/alerts/unresolved-critical` reads persisted unresolved critical alerts without refreshing. `requires_operator_auth()` now treats only `/allocation/alerts?refresh=true` as an active-read GET for this route family. Passive dashboard/live/risk loads and polling call `getAllocationAlerts({ limit })` without `refresh: true`; `refresh=true` remains only after the explicit alert acknowledge/resolve mutation follow-up. Regression evidence: `test_audit_003_allocation_alert_service_default_read_does_not_refresh`; `test_audit_003_allocation_alert_route_default_is_passive_read`; `test_audit_003_allocation_alert_route_refresh_true_is_active_read`; `test_audit_003_unresolved_critical_route_reads_persisted_alerts_without_refresh`; `AUDIT-003 passive frontend alert reads do not request refresh=true`; targeted backend and frontend tests passed on 2026-05-10. | No further action for this finding. Related direct route-function read/write slices are now verified under `AUDIT-API-001`, `AUDIT-API-002`, and `AUDIT-API-003`; broad HTTP route evidence remains open under `AUDIT-TEST-001`; alert mutation error display remains under `AUDIT-UI-004`. |
| AUDIT-004 | P2 | Open | Central unresolved-items tracking | TERM-RISK-TRUTH-CONFIDENCE, TERM-PROVENANCE, TERM-READ-SERVICE, API-007, API-011, FLOW-* rows, TEST-009, multiple matrix rows | `Needs audit`, `Needs confirmation`, `Needs test`, and `Unknown` items are widespread in the specs but were not centrally summarized before this run. | Read-only scan found 103 occurrences across `docs/spec/01-domain-glossary.md`, `03-broker-contract.md`, `04-backend-api-contract.md`, `05-frontend-operator-ui-contract.md`, `06-end-to-end-flows.md`, `07-state-machines.md`, `08-risk-allocation-invariants.md`, `10-testing-contract.md`, and `99-spec-coverage-matrix.md`. | Create a follow-up evidence-gap index or extend the coverage matrix with filters for unresolved token, affected ID, owner, and next verification action. |
| AUDIT-005 | P1 | Open | Risk confidence terminology | TERM-RISK-TRUTH-CONFIDENCE, RISK-003, RISK-004, RISK-006, RISK-011, UI-RISK-001 | Risk confidence labels are intentionally provisional, but the spec set needs an authoritative label contract before backend/frontend audits can prove parity. | `01-domain-glossary.md` marks risk truth confidence as `Needs confirmation`; `08-risk-allocation-invariants.md` lists expected labels but says the exact enum needs confirmation; `99-spec-coverage-matrix.md` identifies the central enum/contract as missing. | Define the authoritative `risk_truth_confidence` label set in one spec section or schema contract, then require backend enum/read-model tests and frontend parity/unknown-label tests. |
| AUDIT-006 | P2 | Fixed | Verification wording | PROD-003, coverage matrix wording | One stale confidence phrase remained in a matrix note and has been normalized during remediation-backlog consolidation. | `docs/spec/99-spec-coverage-matrix.md` no longer uses "Current confidence is backend-heavy" for `PROD-003`; the row now records scoped backend, route/API, frontend, and overall confidence with a concrete next action. | No implementation action. Keep future wording scoped to verification level/confidence. |
| AUDIT-DOC-001 | P1 | Fixed | Product docs / operator safe usage | PROD-003, PROD-008, PROD-012, TEST-014, TEST-016, UI-004, UI-006 | README/operator-facing docs did not clearly state that the product must not be used for live or demo dealing while open P0 readiness blockers remain, and no safe-usage guidance was present. | `README.md` now has a top-level Status section that states InvestMate is not ready for live or demo broker dealing. `docs/readiness.md` and `docs/operator-guide.md` carry safe local usage guidance and known-risk detail, including auth/CORS, `/testing/reset-history`, duplicate runtime loops, broker ambiguity, migrations, dependency, redaction, and recovery limitations. The previous real-dealing progression is gone. Confidence: High. | No further docs action for this finding. Future automated docs checks can be tracked separately if desired. |
| AUDIT-DOC-002 | P1 | Fixed | README lifecycle accuracy | PROD-004, PROD-005, PROD-009, PROD-011, STATE-002, STATE-005, STATE-008, FLOW-ENTRY-001, FLOW-RUNTIME-001, FLOW-RECOVERY-001 | README lifecycle sections were internally stale/contradictory and overstated invariants that current code/audit evidence disproves. | `README.md` now summarizes target lifecycle boundaries without stale step detail. `docs/trade-lifecycle.md` documents the intent-first flow: raw entry candidates create `TradeIntent` proposals first, rejected entries do not create executions, approved intents create `SUBMISSION_PENDING` executions, stopped-runtime broker-risk recovery has explicit backend regression evidence, and legacy statuses are compatibility-only. Confidence: High. | No further docs action for this finding. |
| AUDIT-DOC-003 | P2 | Fixed | Clean-clone setup docs | PROD-012, TEST-010, TEST-016 | README setup commands worked from a clean clone with network access, but setup docs did not state prerequisites, expected credential-dependent degraded states, or repeatable verification/test commands. | `README.md` now puts quick-start commands near the top, and `docs/operator-guide.md` lists Python/Node/network/IG prerequisites, expected default degraded states with blank IG credentials, credential-required endpoint behavior for `/broker/positions` and `/markets/overview?category=forex`, backend and frontend verification commands, and current test limitations. Confidence: High. | No further docs action for this finding. |
| AUDIT-DOC-004 | P2 | Fixed | Documentation portability | PROD-012, TEST-010 | README and spec links used absolute local filesystem paths, making documentation non-portable in a fresh clone, GitHub view, or another developer's checkout. | README links were converted to repo-relative paths. `docs/spec/99-spec-coverage-matrix.md` spec evidence links were converted from local absolute paths to relative spec-doc links. A follow-up scan found no previous machine-specific absolute repo links in `README.md` or `docs`. Confidence: High. | No further docs action for this finding. |
| AUDIT-DOC-005 | P2 | Fixed | API docs handoff / reference truth | API-003, API-004, API-010, TEST-010 | The generated backend API route reference matched the current app route inventory, but README's API section was a partial key-endpoint list that did not clearly hand readers to the complete classified route reference. | `README.md` now links to `docs/backend-api-routes.md` as the complete implementation route reference and explicitly states the README list is only a short operator-oriented subset. It also documents route-classification caveats for active reads, broker reads, `/testing/reset-history`, and dict response contracts. Confidence: High. | No further docs action for this finding. |
| AUDIT-SEC-001 | P0 | Fixed | API security / auth / CORS | API-010, UI-004, UI-006, TEST-016 | Backend operator and broker-adjacent mutation routes did not have an effective authentication or authorization boundary, and CORS accepted credentialed requests from any localhost/127.0.0.1 port. | Fixed 2026-05-08: `backend/app/api/auth.py` defines a production-like operator bearer-token boundary for all `POST`/`PUT`/`PATCH`/`DELETE` routes plus mutation-like active-read refresh routes (`/reviews/*` generated-review GETs and explicit `/allocation/alerts?refresh=true`). `backend/app/main.py` enforces the policy in middleware before route handlers and removes the previous credentialed localhost/127.0.0.1 any-port `allow_origin_regex`; CORS now uses only configured exact origins. `backend/app/core/config.py` adds `OPERATOR_API_TOKEN`. `frontend/lib/api.ts` sends `Authorization: Bearer <NEXT_PUBLIC_OPERATOR_API_TOKEN>` when configured. Regression evidence: `backend/tests/test_operator_auth_boundary.py` proves all state-changing routes are covered by policy, write-on-read refresh routes require auth, missing/invalid/unconfigured production-like tokens fail, a valid token passes, and CORS has no localhost regex. Targeted check `backend/.venv/bin/pytest backend/tests/test_operator_auth_boundary.py backend/tests/test_testing_routes.py backend/tests/test_config.py -q` passed (`12 passed`), and `npm run typecheck` passed. Confidence: Medium-High; no HTTP `TestClient` suite exists because `httpx` is not installed, and public frontend env tokens are operator transport credentials rather than secrets. | No further action for this narrow auth/CORS defect. Keep HTTP route harness work under `AUDIT-TEST-001`, frontend mutation-error behaviour under `AUDIT-UI-004`/`AUDIT-UI-006`, and stronger auth/session architecture as future platform hardening if needed. |
| AUDIT-SEC-002 | P1 | Open | Logging / redaction / error disclosure | BROKER-010, API-010, PROD-012, TEST-016 | Error logging and domain-event journaling can persist full tracebacks, raw broker responses, account/order payloads, and raw exception messages without a redaction boundary. | `DomainEventService.record_error()` stores `exception_message` and full `traceback`. `DomainEventErrorHandler` mirrors all backend error logs and log context into domain events. `IGBroker.place_order()` logs order payload, account id, account balance/available/currency, market payload, and raw order response. IG request failures log response bodies and raise `IGBrokerError` containing raw response text; broker routes return `detail=str(exc)`. No redaction tests were found. | Add a centralized redaction/sanitization layer for logs, API error details, and domain events; keep raw broker responses and stack traces only in a restricted debug sink; add tests proving access tokens, passwords, API keys, account identifiers, and raw adapter internals are masked or omitted. |
| AUDIT-SEC-003 | P1 | Open | Secrets handling / repository history | BROKER-010, PROD-012 | No current tracked `.env` secret was found, but ignored local env files contain real broker/API credentials and historical commits contain SQLite trading databases. | `git ls-files` showed only `.env.example` files tracked, and `.gitignore` ignores `.env`, `backend/.env`, `.env.local`, and `*.db`. Sanitized local-env inspection found set values for backend API/IG credentials and a frontend public API token; values are intentionally not recorded here. `git log --all --name-status -- backend/trading_platform.db trading_platform.db` shows `backend/trading_platform.db` added on 2026-03-20, repeatedly modified, and deleted on 2026-04-10. | Rotate any local/demo broker/API credentials if the repository has been shared, purge historical DB blobs before publishing, keep public frontend env values non-secret, and add secret/history scanning to CI or pre-commit. |
| AUDIT-DEP-001 | P1 | Open | Dependency and supply chain | TEST-010, TEST-015, PROD-012 | The installed Python environment has known vulnerabilities and no reproducible Python lock/constraints file; frontend dependency hygiene also has outdated and unused package evidence. | `backend/.venv/bin/pip-audit --format json` found 11 vulnerabilities in installed packages: `aiohttp 3.13.3` fixed in `3.13.4` or later and `pytest 9.0.2` fixed in `9.0.3`. `aiohttp` is not declared as an app dependency and no app import was found, suggesting dev/transitive exposure, but it is installed. `requirements-dev.txt` is unpinned, and `backend/pyproject.toml` has no lockfile/constraints evidence. `npm audit --audit-level=moderate` was clean, but `npm outdated` showed patch/major drift for Next/React/Tailwind tooling, `npm ls` reported extraneous `@emnapi/*` packages, and `npm run audit:unused` reported unused `tailwindcss`. | Separate production and dev dependency environments, add a Python lock/constraints workflow, update vulnerable packages, clean unused/extraneous frontend packages, and gate CI/pre-commit on `pip-audit`, `npm audit`, lockfile consistency, and unused-dependency checks. |
| AUDIT-DB-001 | P1 | Open | Database schema / migrations | ARCH-009, STATE-001, STATE-003, RISK-011, TEST-010 | Database evolution relies on `SQLModel.metadata.create_all()` plus SQLite-only patch helpers rather than a migration framework, and current SQLite schema drift was observed. | No `alembic`/`migrations` directory was found. `initialize_database()` calls `SQLModel.metadata.create_all(engine)` then many `_ensure_sqlite_column()` and `_ensure_sqlite_partial_unique_index()` helpers; `_ensure_sqlite_column()` returns immediately for non-SQLite dialects. Current `backend/trading_platform.db` has nullable `strategyruntimestate.control_mode` and `runtime_mode` columns despite non-optional model fields with defaults. The active `tradeintent` partial unique index exists in SQLite, but migration coverage for non-SQLite and enum/nullability changes is absent. | Introduce Alembic or equivalent migrations, backfill and enforce runtime nullability, move SQLite patch helpers into versioned migrations or dev-only compatibility, and add migration drift tests for SQLite and the intended production database. |
| AUDIT-RUNTIME-001 | P0 | Verified | Runtime concurrency / duplicate autonomous loops | ARCH-013, BROKER-012, BROKER-013, STATE-008, FLOW-RUNTIME-001, TEST-006 | Startup/reload/worker duplication could run multiple recovery, market-data, streaming, and strategy-processing loops without cross-process coordination, increasing duplicate order submission and duplicate lifecycle mutation risk. | Fixed 2026-05-08: `backend/app/main.py` now acquires a DB-backed `RuntimeLease` through `RuntimeLeadershipService` before running startup recovery, `MarketDataService.run()`, optional `IGStreamingService.run()`, and runtime health heartbeat loops. A process that cannot acquire the live lease yields without constructing those autonomous loops; the leader heartbeats/releases the lease, and expired leases can be taken over. Regression evidence: `test_audit_runtime_001_live_runtime_leader_lease_blocks_duplicate_owner`, `test_audit_runtime_001_lifespan_skips_autonomous_loops_without_leadership`, `test_audit_runtime_001_lifespan_releases_leader_lease_on_shutdown`, and `test_audit_runtime_001_expired_runtime_leader_lease_can_be_taken_over` in `backend/tests/test_runtime_leadership_service.py`. Verification: targeted runtime/config suite passed (`45 passed`) and full backend suite passed (`192 passed`). | No further action for startup/reload/background-loop duplication. Keep execution-time revalidation tracked under `AUDIT-RISK-002`; database migration hardening for the new lease table remains under `AUDIT-DB-001`. |
| AUDIT-OBS-001 | P1 | Open - selected audit slices fixed through 2026-05-19 | Observability / durable audit / health truth | PROD-012, API-010, TEST-016, UI-001 | Operator observability exists, but durable audit visibility and true degraded state are not proven everywhere. Selected control-plane, strategy, allocation-alert, review/advisory, shortlist, strategy-watchlist, allocation-cycle, TradeIntent decision lifecycle, execution lifecycle, reconciliation, and recovery paths now persist session-bound domain events. Selected route mutations return `503` instead of clean success if required route audit writes fail. Allocation-cycle, TradeIntent, and execution lifecycle audit-write failures are now marked durably on the affected lifecycle row. | `DomainEventService.record_event()` remains best-effort for broad service/background events, and `backend/tests/conftest.py` still monkeypatches that legacy global method to return `None` for most tests. Evidence now includes `record_event_in_session()`, `persist_required_domain_event()`, route regressions in `backend/tests/test_mutation_audit_events.py` for operator-control, governance, control-plane reconcile, strategy start/stop, allocation-alert acknowledge/resolve, review persistence, advisory questions, shortlist/strategy-watchlist mutations, and audit-write failure; `backend/tests/test_decision_audit_events.py` proves allocation-cycle and TradeIntent decision-lifecycle events survive the global no-op and mark audit-write failure; `backend/tests/test_lifecycle_audit_events.py` proves execution creation and transition events survive the global no-op and record audit-write failure on the execution; service regressions in `backend/tests/test_reconciliation_service.py` and `backend/tests/test_runtime_recovery_service.py` prove selected reconciliation/recovery domain events survive the global no-op. `HealthService` still keeps heartbeat, stream, broker, order-failure, and reconciliation counters in an in-memory singleton; multi-process health aggregation and metrics/export surfaces were not found. | Keep this finding open. Next: extend required durable audit behavior to remaining broker-action branches, market-data/background events, remaining reconciliation/recovery branches, and remaining mutation paths; remove or narrow the global event no-op in audit-critical tests; add metrics/health surfaces for broker, stream, runtime, queue, and audit-write degraded states across processes. |
| AUDIT-RISK-001 | P1 | Fixed | Risk accounting lifecycle / partial fills | RISK-012, RISK-013, RISK-017, FLOW-RISK-001, STATE-006, TEST-001, TEST-013 | Partial-fill exposure read models did not preserve a visible reserved/live split for the unfilled remainder. Once a partial fill created a `Position`, the `PARTIALLY_FILLED` intent was excluded from reserved exposure because `position_id` was set, while the live position carried provisional fill truth. | Fixed 2026-05-08: partial-fill positions now persist filled-size risk percent from fill-derived risk tracking, and `AllocationReadService.get_exposure_summary()` includes `PARTIALLY_FILLED` intents with residual size as reserved/leaves risk using the `partial_fill_residual` basis. Regression evidence: `test_audit_risk_001_partial_fill_preserves_residual_reserved_and_provisional_live_risk` failed against the old read model (`reserved_risk_percent == 0.0`) and now proves residual reserved risk plus provisional live risk are both visible. Verification: targeted allocation-read suite passed (`10 passed`) and targeted strategy-service suite passed (`39 passed`). | No further backend/read-model action for this finding. Frontend behavioural fixture evidence for risk surfaces remains tracked under `AUDIT-UI-006`/`RISK-017`. |
| AUDIT-RISK-002 | P1 | Verified | Execution-time drift / broker revalidation | RISK-009, RISK-015, BROKER-009, BROKER-015, FLOW-ENTRY-001, FLOW-RISK-001, TEST-003, TEST-013 | Execution-time entry revalidation and submitted/fill material drift visibility are fixed for this finding. Before broker submission, the service now rechecks broker account equity/available funds, depleted available funds versus approved risk, material account-equity risk drift, full broker sizing quote availability/precision, exact and approximate sizing-quote size drift versus allocation evidence, exact executable risk drift from current `risk_per_unit * normalized_size`, stable-size approximate executable risk drift from current `risk_per_unit * normalized_size`, broker size-normalization metadata availability, and a forced-refresh broker market status in addition to operational policy, active intent conflict, and size normalization drift. `/executions` exposes `risk_reconciliation`, `material_execution_drift`, and `critical_execution_drift` directly, and frontend execution notifications behavior-test material, critical, and no-drift labels. | `StrategyService._assert_account_and_sizing_allow_execution()` now calls `get_account_summary()` and `quote_risk_sized_order()` before `place_order`, failing the execution and intent with `execution_revalidation_failed` evidence when account data is unavailable/invalid, available funds fall below approved risk, current equity makes approved risk materially larger, sizing is unsupported, exact or approximate sizing-quote size drift requires reallocation, exact executable risk materially exceeds approved risk, stable-size approximate executable risk materially exceeds approved risk, or approximate sizing would reach a live submission. For approximate quotes, execution-time size/risk drift baselines use the allocated executable size/risk rather than the pre-budget quote values. `TradeService.create_execution()` initializes new `SUBMISSION_PENDING` attempts with `last_transition_at=signal_time` so execution-boundary market-status checks are deterministic and tied to the signal/attempt time. `StrategyService._execute_entry_signal()` catches final `normalize_order_size()` metadata failures, fails the execution/intent with revalidation evidence, and does not call `place_order`. `StrategyService._assert_market_status_allows_execution()` calls `MarketStatusService.get_status(..., force_refresh=True)` at the execution boundary and fails the entry execution/intent if fresh broker status blocks entry. `ExecutionResponse` serializes material/critical drift flags from `details.risk_reconciliation.flags`; `frontend/lib/types.ts`, `frontend/lib/execution-notifications.ts`, and dashboard notifications consume those fields. Regression evidence: `test_audit_risk_002_execution_revalidates_account_before_submission`; `test_audit_risk_002_execution_blocks_material_account_equity_drift`; `test_audit_risk_002_execution_blocks_available_funds_below_approved_risk`; `test_audit_risk_002_execution_revalidates_sizing_quote_before_submission`; `test_audit_risk_002_execution_blocks_material_broker_sizing_quote_drift`; `test_audit_risk_002_execution_blocks_material_broker_unit_risk_drift`; `test_audit_risk_002_execution_blocks_material_approximate_size_drift`; `test_audit_risk_002_execution_blocks_material_approximate_unit_risk_drift`; `test_audit_risk_002_execution_revalidates_market_status_without_cache`; `test_audit_risk_002_execution_metadata_failure_before_submission_is_audited`; `test_audit_risk_002_execution_response_surfaces_material_risk_drift`; `frontend/tests/execution-notifications.test.mjs`; `npm run test:frontend`; `npm run typecheck`. Verification: targeted route/strategy regressions, full backend suite, frontend behavioural tests, and frontend typecheck passed on 2026-05-10. | No further action for this finding. Broader frontend harness/degraded-state coverage remains under `AUDIT-UI-006`, route/API audit depth under `AUDIT-TEST-001`, durable audit persistence under `AUDIT-TEST-002`, and risk read-surface fallback truth under `AUDIT-RISK-003`. |
| AUDIT-RISK-003 | P1 | Fixed | Frontend risk visibility / fallback truth | UI-001, UI-002, UI-008, UI-RISK-001, RISK-006, RISK-017, TEST-004, TEST-011 | The dedicated risk page converted backend risk-read failures into empty/zero initial data without passing the captured errors into the rendered risk surface, so unavailable allocation/exposure/drift data could look like "No recent cycle", "No critical alerts", zero active risk, or positive risk truth rather than unknown/degraded. | Fixed 2026-05-12: `frontend/app/risk/page.tsx` now passes `loadWithMeta()` errors for exposure, alerts, drift, cycles, intents, and selected cycle into `RiskAllocationLive`. `frontend/components/risk/risk-allocation-live.tsx` tracks initial and refresh read failures as risk load quality, renders a negative `Risk data unavailable` banner, and changes affected status strip values/table empty states to `Unavailable` instead of healthy zero/no-risk copy. `frontend/lib/risk-allocation.ts` adds `buildRiskLoadQuality()` so fallback/error handling has a tested frontend contract. Regression evidence: `frontend/tests/risk-allocation-data-quality.test.mjs` failed against the previous behavior because no risk-load quality helper existed and the risk page did not pass error metadata, then passed after the fix. Commands on 2026-05-12: `npm run test:frontend -- --test-name-pattern=AUDIT-RISK-003` failed because the npm script appends the option after the test glob and Node treated it as a file; `node --test --test-name-pattern=AUDIT-RISK-003 tests/*.test.mjs` failed before implementation and passed after (`2 passed`, `4 skipped`); `npm run test:frontend` (`6 passed`); `npm run typecheck` (passed); `npm run lint` (passed); `git diff --check` (passed). | Fixed for the risk-page backend-unavailable/zero-risk substitution slice. Remaining evidence gaps: no rendered component/browser test, no stale/unknown-confidence/partial-provisional fixture tests, and other production fallback/default paths remain open under `AUDIT-UI-002`, `AUDIT-UI-007`, and `AUDIT-UI-006`. |
| AUDIT-API-001 | P1 | Fixed | Passive read side effects | API-001, API-008, ARCH-005, ARCH-010 | Fixed: control-plane and strategy GET routes no longer seed operator-control, governance, or deployment defaults while classified as passive reads. | `OperatorControlService.get_summary()` and `get_effective_autonomous_control_enabled()` now read existing `OperatorControlState` rows without creating `id=1`; `update_autonomous_control()` keeps the explicit mutation path that creates the row when needed. `ControlPlaneService._build_family_rows()` and `StrategyService.list_strategies()` now consume `list_existing_strategies()` / `list_existing_deployments()` read helpers, while `StrategyGovernanceService.list_strategies()`, `StrategyDeploymentManagerService.list_deployments()`, and `reconcile()` retain explicit default seeding for mutation/system workflows. Regression evidence: `test_audit_api_001_operator_state_get_does_not_seed_default_row`; `test_audit_api_001_control_plane_strategy_detail_does_not_seed_governance`; `test_audit_api_001_strategy_list_get_does_not_seed_governance`; and `test_governance_service_seeds_governance_defaults_explicitly`. Targeted and adjacent suites passed on 2026-05-11. | No further action for this default-seeding slice. Coverage/watchlist/feed-state and review GET write-on-read slices are now verified under `AUDIT-API-002`/`AUDIT-API-003`; broad HTTP route harness work remains under `AUDIT-TEST-001`. |
| AUDIT-API-002 | P1 | Verified | Passive read side effects | API-001, API-008, ARCH-005, ARCH-010 | Fixed: coverage/watchlist/feed-state GET routes no longer sync watchlist state or update persisted streaming metadata while returning read models. | `WatchlistService.list_strategy_watchlist()`, `strategy_watchlist_response()`, `feed_state_response()`, `get_streaming_plan()`, and `get_tier2_refresh_plan()` now accept `sync=False` for passive snapshots. `GET /strategy-watchlist` and `GET /market-data/feed-state` pass `sync=False`; `CoverageService.get_summary()` uses passive plan snapshots. Active/background paths retain the default sync behavior for streaming and Tier 2 refresh workflows. Regression evidence: `test_audit_api_002_strategy_watchlist_get_does_not_sync_watchlist_state`; `test_audit_api_002_feed_state_get_does_not_sync_watchlist_state`; `test_audit_api_002_coverage_summary_get_does_not_sync_watchlist_state`. Verification: targeted route/watchlist/coverage suites and full backend suite passed on 2026-05-11. | No further action for this route-function passive-read slice. Broader HTTP route harness and frontend coverage-state behavioral tests remain under `AUDIT-TEST-001` and `AUDIT-UI-006`. |
| AUDIT-API-003 | P1 | Verified | Review write-on-read | API-001, API-002, API-008, AIMEE-011 | Fixed: review GET endpoints no longer persist `GeneratedReviewRecord` rows by default; review persistence is now explicit through `persist=true` and remains auth-classified as mutation-like active-read behavior. | `GET /reviews/operator-summary`, `/reviews/daily`, `/reviews/strategies/{strategy_name}`, `/reviews/runtime-health`, and `/reviews/trades/{trade_id}/postmortem` accept `persist: bool = false` and pass it to `AIReviewerService`. `requires_operator_auth()` now requires operator auth for those review GETs only when `persist=true`. Regression evidence: `test_audit_api_003_review_gets_are_passive_without_explicit_persist`; `test_audit_api_003_review_get_persist_true_is_explicit_active_read`; updated `test_audit_sec_001_write_on_read_refresh_routes_require_operator_auth_policy`. Verification: targeted route/auth suites and full backend suite passed on 2026-05-11. | No further action for this route-function review persistence slice. Frontend AIMEE/passive-call behavior and HTTP route harness evidence remain under `AUDIT-UI-006` and `AUDIT-TEST-001`; response schemas remain under `AUDIT-API-006`. |
| AUDIT-API-004 | P0 | Verified | Test-only route gating | API-009, TEST-014, UI-009, UI-BND-006 | `/testing/reset-history` was always registered and frontend-callable even though it deletes trading/audit history. | Fixed 2026-05-08: `backend/app/api/router.py` now builds routers through `build_api_router()` and includes `testing.router` only when `TESTING_ROUTES_ENABLED=true` (`backend/app/core/config.py`). `frontend/app/events/page.tsx` renders `ResetHistoryButton` only when `NEXT_PUBLIC_TESTING_CONTROLS_ENABLED=true`, and `frontend/lib/api.ts:resetTestHistory()` fails closed when the same public gate is absent. Regression evidence: `test_audit_api_004_reset_history_route_unavailable_without_explicit_test_config` proves the route is absent and data is preserved when disabled; `test_audit_api_004_reset_history_route_available_with_explicit_test_config` proves the explicit test/dev path still clears history. Targeted check `backend/.venv/bin/pytest backend/tests/test_testing_routes.py backend/tests/test_history_reset_service.py -q` passed (`3 passed`), and `npm run typecheck` passed. Confidence: Medium-High; no frontend behavioral harness or HTTP `TestClient` suite exists. | No further action for this narrow test-only route gating defect. General backend auth/CORS is now fixed under `AUDIT-SEC-001`; keep frontend behavioral harness work under `AUDIT-UI-006`. |
| AUDIT-UI-001 | P1 | Fixed 2026-05-12 | Frontend fallback/default truth | UI-001, UI-002, UI-008, UI-OPERATE-001, UI-BND-004, TEST-004 | Fixed for the global-nav fallback slice: nav status refreshes now settle broker, stream, and control-plane sources independently; stream/control-plane load failures reset those cards to explicit unknown/unavailable state instead of retaining silent stale truth; initial control-plane/stream fallback no longer renders `Nominal`; health/stream/broker cards expose error/provenance detail in `title` text. | Files changed: `frontend/components/app-nav.tsx`; `frontend/tests/operator-status-provenance.test.mjs`. Regression `AUDIT-UI-001 nav refresh failures render health as unknown instead of nominal` failed against the old silent-catch/`Nominal` behavior and now passes. Verification: `node --test --test-name-pattern='AUDIT-UI-00[13]' tests/operator-status-provenance.test.mjs` (`2 passed`), `npm run test:frontend` (`8 passed`), `npm run typecheck` (passed), `npm run lint` (passed), and `git diff --check` (passed). | No further action for the narrow nav fallback defect. Remaining frontend fallback/default evidence gaps for markets/dashboard/live and rendered browser/component coverage stay open under `AUDIT-UI-006` and `AUDIT-UI-007`. |
| AUDIT-UI-002 | P1 | Open - partial source/helper and rendered slices fixed 2026-05-14 | Frontend enum/status mapping | UI-005, UI-007, UI-BND-005, RISK-006, RISK-011, TEST-011 | Frontend execution and risk-confidence mappings were behind backend/spec states. Missing or unknown states could render as positive execution activity or estimated risk instead of pending/unknown/degraded truth. | Existing 2026-05-14 source/helper slice changed `frontend/lib/live-system-view.ts`, `frontend/lib/risk-allocation.ts`, `frontend/lib/types.ts`, and `frontend/tests/ui-enum-truth.test.mjs`: pending/submitted/acknowledged/close-requested states render as pending broker/fill/close truth, and `SIMULATED_LOCAL_FILL`/`UNKNOWN` risk confidence render degraded/not broker-confirmed. This PR adds rendered evidence in `frontend/components/dashboard/notification-center.tsx`, `frontend/components/risk/risk-allocation-live.tsx`, and `frontend/tests/rendered-operator-states.test.mjs`: `AUDIT-UI-006 rendered pending execution notification is degraded and not raw healthy truth` failed before the render units/pending notification path existed and now proves a `SUBMISSION_PENDING` entry notification renders as warning, says "Entry pending broker submission", includes manual-review fallback copy, and does not show raw `SUBMISSION_PENDING` or "Position opened"; `AUDIT-UI-002 rendered allocation risk truth marks simulated and unknown as non-exact` proves rendered risk truth pills label `SIMULATED_LOCAL_FILL` as simulated/local and `UNKNOWN` as negative unknown/unavailable, not exact or broker-confirmed. Commands run: targeted rendered/source tests passed (`5 passed`); `npm run test:frontend` passed (`24 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. | Keep this finding open with narrower remaining scope: backend/frontend enum parity is still incomplete beyond this pending/unknown/simulated slice, and no browser/e2e evidence proves every critical surface. The authoritative `risk_truth_confidence` contract remains under `AUDIT-005`. |
| AUDIT-UI-003 | P1 | Fixed 2026-05-12 | Broker provenance / frontend-derived truth | UI-001, UI-002, UI-007, UI-011, UI-BND-001, UI-BND-003, BROKER-015 | Fixed for the telemetry-derived frontend broker-status slice: the client helper no longer labels telemetry-derived state as `IG Connected`/`IG Disconnected`, and the live trust rail no longer claims `Broker-confirmed` source for broker state produced by `getOperationalTelemetry()`. | Files changed: `frontend/lib/api.ts`; `frontend/lib/live-system-view.ts`; `frontend/tests/operator-status-provenance.test.mjs`. Regression `AUDIT-UI-003 live broker status is labelled as telemetry-derived unless broker-confirmed` failed against the old `Broker-confirmed` source and now passes. Verification: `node --test --test-name-pattern='AUDIT-UI-00[13]' tests/operator-status-provenance.test.mjs` (`2 passed`), `npm run test:frontend` (`8 passed`), `npm run typecheck` (passed), `npm run lint` (passed), and `git diff --check` (passed). | No further action for this narrow provenance-label bug. Freshness/timestamp display and rendered telemetry-vs-broker-confirmed component coverage remain open under `UI-011`, `BROKER-015`, and `AUDIT-UI-006`. |
| AUDIT-UI-004 | P1 | Open - allocation-alert/AIMEE source/rendered slices fixed through 2026-05-19 | Mutation controls / error handling | UI-004, UI-006, RISK-005, RISK-BND-007, TEST-016 | Allocation alert acknowledge/resolve controls previously did not catch or display mutation failures. A failed acknowledge/resolve or failed post-mutation active-read refresh could leave the operator without backend error detail or clear post-action truth. AIMEE advisory-question controls also lacked visible copy that submitting a question creates an advisory review record. | The 2026-05-12 source slice changed `frontend/components/risk/risk-allocation-live.tsx` and `frontend/tests/allocation-alert-mutations.test.mjs`: `RiskAllocationLive.mutateAlert()` maintains per-alert mutation state, disables only the affected alert action while pending, catches failures as visible `Mutation failed: ...` status text, and shows success only after refreshed backend truth returns. The 2026-05-14 rendered slice extracted `RiskAlertCard` and added `AUDIT-UI-004 rendered allocation alert mutation failure preserves backend detail`, proving rendered backend detail text remains visible without success copy. The earlier 2026-05-19 allocation-alert slice adds `refreshError` state so a successful mutation followed by failed active-read refresh renders `Mutation submitted, but backend alert truth could not be refreshed.` plus `Refresh failed: ...`, not clean success. This slice changes `frontend/components/aimee/aimee-conversation.tsx` so AIMEE question controls render `Advisory questions`, `Creates advisory review record`, `Ask & record`, and advisory aria-label copy before calling `POST /reviews/questions`. Regression evidence: `AUDIT-UI-004 allocation alert refresh failures do not render clean success`, `AUDIT-UI-004 rendered allocation alert refresh failure is not clean success`, and `AUDIT-UI-004 rendered AIMEE advisory controls disclose review persistence before submission` failed before implementation and now pass. Commands run for the AIMEE slice: targeted `node --test --test-name-pattern='AIMEE|AUDIT-UI-004 rendered AIMEE' tests/*.test.mjs` passed (`9 passed, 34 skipped`) after the copy fix; broader checks are recorded in the remediation log. | Keep this finding open with narrower remaining scope: other mutation controls still need pending/error/success/disabled-reason coverage, and browser/e2e evidence is still missing. Backend allocation-alert durable audit-preservation is now covered under `AUDIT-API-008`/`AUDIT-TEST-002`, but broader mutation audit coverage and review/advisory route-persistence proof remain open. |
| AUDIT-UI-005 | P2 | Open | Mutation controls / operator UX | UI-004, UI-CONTROL-001, UI-STRATEGY-001, TEST-004, TEST-016 | Several runtime/governance/watchlist mutation controls have labels and pending states, but incomplete disabled reasons or confirmations for side effects. This weakens operator clarity even where backend gates may still enforce safety. | Strategy start is always rendered for the selected strategy and disabled only by global pending state (`frontend/components/strategy/strategy-live.tsx:312-321`), with no frontend disabled reason for unauthorized strategy, broker/feed unavailable, or entry-ineligible state. Runtime stop is labelled simply "Stop" (`frontend/components/strategy/strategy-live.tsx:413-420`) with no confirmation or explanation of open-risk/exits-only implications. Control-plane "Arm"/"Pause" and family "Allow Auto Deploy"/"Disallow" controls have pending states but no confirmation (`frontend/components/control-plane/control-plane-live.tsx:220-235`, `frontend/components/control-plane/control-plane-live.tsx:352-358`). The 2026-05-19 Markets provenance slice now removes the misleading watchlist `eligible`/streaming approval copy and adds rendered regression evidence, but remaining watchlist add controls still need broader pending/error/success/disabled-reason coverage. | Add visible disabled reasons and confirmation where runtime/governance changes can affect autonomous behavior or open-risk handling. Keep watchlist mutation controls scoped to watchlist/evaluation side effects, not trading approval. Add component tests for pending/error/success and disabled-reason states. |
| AUDIT-UI-006 | P1 | Open - broad rendered/source slices fixed through 2026-05-19 | Frontend test evidence | TEST-004, TEST-011, TEST-012, TEST-014, UI-001 through UI-012, UI-BND-001 through UI-BND-007, RISK-006, RISK-017, AIMEE-008, AIMEE-011, AIMEE-012 | No browser/e2e frontend behavioral suite exists for the operator console. Existing frontend scripts now include `test:frontend`, and focused Node/rendered SSR/static tests cover many helper/static/source and rendered degraded-state guards. This work materially narrows the rendered/source evidence gap but does not prove full route/browser behavior. | `frontend/tests/rendered-operator-states.test.mjs` compiles selected TSX components with a temporary TypeScript config and renders them with `react-dom/server`. It now verifies rendered pending execution notification truth, rendered allocation-alert mutation failure and refresh-failure detail, rendered AIMEE advisory persistence copy, rendered simulated/unknown risk-confidence pills, risk budget exposure-unavailable truth, markets catalogue-unavailable count truth, markets watchlist/streaming provenance that stays separate from trading or entry approval, AIMEE control-plane/open-risk/Operate/strategies/events telemetry-unavailable truth, control-plane family missing-open-risk truth, live-view all-source outage truth, dashboard stale-feed stream truth, coverage watchlist polling-fallback truth, strategy open-position stop provenance, and visible test-only destructive reset labeling. `frontend/tests/aimee-passive-boundary.test.mjs` verifies AIMEE passive loader/API source behavior, no passive `persist=true` review GET use, and explicit `POST /reviews/questions` with backend-detail preservation. Latest markets regressions failed before implementation because watchlisted-but-not-streaming rows rendered `eligible` and streaming rows lacked visible data-coverage-only / not-entry-approval provenance; targeted `node --test --test-name-pattern='rendered markets .*provenance' tests/rendered-operator-states.test.mjs` failed before the fix (`0 passed, 2 failed, 18 skipped`) and passed after (`2 passed, 18 skipped`). Broader verification passed: `npm run test:frontend` (`45 passed`), `npm run typecheck`, and `npm run lint`; `git diff --check` passed after doc updates. | Keep this finding Open with narrower remaining scope. This slice is Fixed for rendered Markets watchlist/streaming provenance only, not Verified for full Markets or market-data freshness behavior. Remaining gaps: browser/e2e fixtures, stateful route/browser behavior, complete enum parity, broader mutation controls, full AIMEE drawer-open/advisory success/error flows, and full loading/error/empty/stale/degraded/freshness states. |
| AUDIT-UI-007 | P1 | Fixed 2026-05-14 | Mock/fallback data boundary | PROD-008, PROD-013, UI-001, UI-002, UI-008, UI-BND-004, TEST-004, TEST-012 | Production frontend code permitted backend-shaped fallback/default data to substitute for unavailable backend responses. No mocked, demo, fake, or fallback backend data should appear in production app truth; unavailable backend data must render as unavailable/degraded/error instead. | Fixed for the production source/static boundary. `loadWithMeta()` now returns `data: null` on failure and no longer accepts caller-provided fallback data. Server pages call `loadWithMeta()` with only a loader, then pass explicitly named `UNAVAILABLE_*` shells only alongside captured error metadata. Production `EMPTY_*` fallback symbols were removed/renamed, and `frontend/tests/fallback-data-guard.test.mjs` now statically proves production app/component/lib modules do not import mock/demo/fake/fixture backend data, do not call `loadWithMeta()` with fallback data, and do not retain `EMPTY_*` fallback symbols. Existing same-file regressions also prove fail-closed control and telemetry unavailable values, nav account environment `Unknown`, markets unavailable source truth, dashboard missing open-risk state as `UNAVAILABLE`, and live-view derived confidence not `HIGH` with unavailable sources. Commands run on 2026-05-14: `node --test --test-name-pattern='AUDIT-UI-007 loadWithMeta does not accept caller-provided backend-shaped fallback data|AUDIT-UI-007 production modules do not import mock demo fake or fixture backend data|AUDIT-UI-007 backend-unavailable control fallbacks|AUDIT-UI-007 backend-unavailable telemetry fallback|AUDIT-UI-007 backend-unavailable market overview' tests/*.test.mjs` passed (`5 passed`, rest skipped); `npm run test:frontend` passed (`21 passed`); `npm run typecheck` passed; `npm run lint` passed after removing unused imports; `git diff --check` passed. Files changed for final closure: `frontend/lib/api.ts`, `frontend/app/page.tsx`, `frontend/app/live/page.tsx`, `frontend/app/coverage/page.tsx`, `frontend/app/control-plane/page.tsx`, `frontend/app/risk/page.tsx`, `frontend/app/markets/page.tsx`, `frontend/app/strategies/page.tsx`, `frontend/components/app-nav.tsx`, `frontend/components/aimee/aimee-shell.tsx`, `frontend/components/dashboard/autonomy-overview.tsx`, `frontend/components/dashboard/control-plane-strip.tsx`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. Confidence: High for the production mock/fallback boundary source/static fix. | No remaining action for `AUDIT-UI-007`. Broad rendered component/browser proof for degraded/loading/unknown states remains tracked under `AUDIT-UI-006`; enum parity, mutation controls, and freshness remain under their existing findings. |
| AUDIT-TEST-001 | P1 | Open | Route/API test evidence | API-001, API-002, API-003, API-005, API-008, API-010, ARCH-005, ARCH-010, TEST-002, TEST-010, TEST-016 | Route/API coverage does not prove the API contract for P0/P1 behavior. Passive reads lack no-write tests, active read/refresh routes lack side-effect tests, mutation routes lack systematic error/audit-preservation tests, and broker-action authority is only service-tested. | `backend/tests` contains no `TestClient` usage; `fastapi.testclient` currently cannot import because `httpx` is not installed in the backend dev environment. Route-like coverage includes direct function calls in health/market/AIMEE tests, `backend/tests/test_testing_routes.py` proving `/testing/reset-history` router registration is disabled by default and enabled only by explicit config, and `backend/tests/test_operator_auth_boundary.py` proving auth-policy coverage and token/CORS behavior for mutation/refresh routes. These tests still do not exercise full HTTP routing, dependency injection, response schemas, or route-wide side effects. | Add route tests for every route in `04-backend-api-contract.md`: passive GET no-write/forbidden-call checks, active-read documented side effects, mutation side effects/errors/audit state, response-contract fields consumed by frontend, and broker-action lifecycle authority. Add/install the needed HTTP client test dependency before claiming HTTP route coverage. |
| AUDIT-TEST-002 | P1 | Open - selected route/service audit slices fixed through 2026-05-19 | Domain event / audit evidence tests | PROD-002A, PROD-012, BROKER-002, BROKER-011, API-010, STATE-001, STATE-005, STATE-010, FLOW-ENTRY-001, FLOW-EXIT-001, FLOW-RECOVERY-001, TEST-001, TEST-013, TEST-016 | The core pytest fixture still mocks broad durable domain-event recording away by default, but selected operator route mutations, selected execution lifecycle events, and selected recovery/reconciliation service paths now have dedicated session-bound durable event tests that bypass that no-op. | `backend/tests/conftest.py` autouse fixture `patch_external_boundaries` still sets `domain_event_service.record_event` to `lambda **_: None`, so many service/lifecycle tests cannot catch missing event fields, failed event persistence, or absent domain-event emission. Regression evidence in `backend/tests/test_mutation_audit_events.py` proves selected control-plane, strategy, allocation-alert, review persistence, advisory, shortlist, and strategy-watchlist route mutations persist `DomainEvent` rows in the active test session and selected required route families fail clean success when required audit persistence returns `None`. New lifecycle evidence in `backend/tests/test_lifecycle_audit_events.py`: `test_audit_test_002_execution_creation_persists_submission_pending_domain_event`, `test_audit_test_002_execution_transition_persists_domain_event_despite_global_noop`, `test_audit_test_002_execution_transition_records_related_domain_ids`, and `test_audit_obs_001_execution_transition_marks_audit_persistence_failure` failed before session-bound execution event persistence and now pass. Existing recovery/reconciliation evidence remains: `test_audit_test_002_reconciliation_adoption_persists_domain_event`, `test_audit_test_002_reconciliation_forced_close_persists_domain_events`, `test_audit_test_002_runtime_recovery_resumed_runtime_persists_domain_event`, and `test_audit_test_002_runtime_recovery_broker_mismatch_persists_domain_event`. Commands for this slice: targeted audit suite passed (`21 passed`); adjacent route/lifecycle/watchlist/recovery suite passed (`113 passed`); full backend suite passed (`277 passed`); `ruff check` on touched Python files passed; `git diff --check` passed. | Keep this finding open. Remaining evidence gaps deliberately out of scope: remaining broker-action branches, market-data/background, remaining recovery/reconciliation branches, and other background domain-event persistence are still mocked or unproven; tests still need durable correlation and related intent/execution/position/runtime/allocation IDs for those flows. |
| AUDIT-TEST-003 | P2 | Open | Weak/construction-only and process evidence | PROD-010, TEST-001, TEST-005, TEST-007, TEST-008, TEST-009, TEST-BND-001, TEST-BND-006, TEST-BND-008 | The suite includes useful behavioral tests, but several evidence claims are still process/file-name based rather than behaviour-name based, and construction/configuration tests are not clearly excluded from P0/P1 confidence. The coverage matrix also has rows at `NOT_TESTED`, `CONSTRUCTION_ONLY`, `Needs audit`, or file-only evidence without a concrete next test name. | Examples of construction/configuration-style tests include registry/config/default tests such as `test_registry_lists_volatility_adjusted_pullback_continuation_strategy`, `test_registry_lists_activity_surveillance_scanner`, and `test_settings_parse_streaming_budgets_and_seed_instruments`; these are fine as low-risk tests but must not count for P0/P1 safety invariants. `99-spec-coverage-matrix.md` still has rows such as `TEST-005`, `TEST-007`, `TEST-008`, and `TEST-009` without direct behavioral evidence, and many non-test rows list files rather than specific test names. | Make the matrix name concrete tests for every P0/P1 row, mark construction/config tests as supporting-only, and add a maintenance check that rejects P0/P1 rows without a verification level, named test or explicit `Needs test`, and next action. |
| AUDIT-API-005 | P1 | Verified | Route classification / broker read | API-001, API-003, ARCH-010, ARCH-012, BROKER-015 | Fixed: `/dashboard` no longer performs a live broker account read while presenting itself as a passive persisted/runtime projection. | `DashboardService.get_dashboard()` now derives `accountValue` from persisted trades and positions only, returns `brokerInfo=None`, and no longer imports or calls `get_broker().get_account_summary()`. Regression `test_audit_api_005_dashboard_passive_read_does_not_call_broker_account` failed against the old swallowed broker-read behavior and now passes. Adjacent dashboard/AIMEE tests passed (`backend/.venv/bin/pytest backend/tests/test_dashboard_service.py backend/tests/test_aimee_read_service.py -q`, `6 passed`). | No further action for this broker-read misclassification. Keep raw response schema work under `AUDIT-API-006`, broad route no-write evidence under `AUDIT-TEST-001`, and broker freshness/provenance breadth under `BROKER-015`/`UI-011`. |
| AUDIT-API-006 | P1 | Open | Response contract gaps | API-003, API-004, ARCH-009, ARCH-012, AIMEE-009 | Several frontend-consumed operator-critical routes return raw dict/list shapes rather than Pydantic models or documented dict schemas. | Raw dict/list routes consumed from `frontend/lib/api.ts` include `/dashboard`, `/control-plane/strategies/{strategy_name}`, `/control-plane/governance/{strategy_name}`, `/allocation/*`, `/markets/*`, `/watchlist/shortlist`, `/strategy-watchlist`, `/market-data/feed-state*`, `/live/instruments/{instrument_id}/chart`, `/strategy/start`, `/strategy/stop`, and `/aimee/snapshot`. | Add Pydantic response models or documented dict schemas for frontend-consumed operator-critical routes, then add contract tests against `frontend/lib/types.ts`. |
| AUDIT-API-007 | P1 | Verified | AIMEE passive read boundary | AIMEE-001, AIMEE-006, AIMEE-008, AIMEE-011, API-001, ARCH-005 | Fixed: `/aimee/snapshot` no longer indirectly performs a broker account read through the reviewer dashboard path. | `AimeeReadService.get_snapshot()` still calls `AIReviewerService.get_operator_summary(persist=False)`, but that reviewer path now receives a persisted-only dashboard summary because `DashboardService.get_dashboard()` no longer calls broker account reads. Regression `test_audit_api_007_aimee_passive_snapshot_does_not_call_broker_account` failed against the old swallowed broker-read behavior and now passes. Adjacent dashboard/AIMEE tests passed (`backend/.venv/bin/pytest backend/tests/test_dashboard_service.py backend/tests/test_aimee_read_service.py -q`, `6 passed`). | No further action for this passive broker-read bug. Keep frontend passive AIMEE call-path tests under `AIMEE-008`/`AUDIT-UI-006` and broad route no-write evidence under `AUDIT-TEST-001`; review GET default persistence is fixed under `AUDIT-API-003`. |
| AUDIT-API-008 | P1 | Open - control-plane/strategy/allocation-alert/review/watchlist route slices fixed through 2026-05-19 | Mutation audit evidence | API-010, ARCH-011, PROD-012 | Selected control-plane, strategy, allocation-alert, review/advisory, shortlist, and strategy-watchlist operator mutation routes no longer return clean success without required durable audit evidence. Selected reconciliation/recovery service events and selected execution lifecycle events also bypass the global event mock. Other mutation and broker-action paths still need equivalent audit-preservation proof. | `backend/app/api/audit.py` adds `persist_required_domain_event()` and `backend/app/services/domain_event_service.py` adds `record_event_in_session()` so route mutations can persist `DomainEvent` rows in the active request session. `PUT /control-plane/operator-state`, `POST /control-plane/reconcile`, `PUT /control-plane/governance/{strategy_name}`, strategy start/stop routes, allocation-alert acknowledge/resolve routes, `GET /reviews/*?persist=true`, `POST /reviews/questions`, `POST/DELETE /watchlist/shortlist/{instrument_id}`, `POST /strategy-watchlist/bulk`, and `DELETE /strategy-watchlist/{instrument_id}` now have required route audit events. New watchlist evidence includes actor/source, affected instrument, previous/new state, entry IDs, requested/added/skipped instruments for bulk changes, and `503` audit-write failure behavior for representative shortlist and strategy-watchlist mutations. Regression evidence: existing control-plane/strategy/allocation-alert/review tests plus `test_audit_api_008_shortlist_mutations_persist_domain_events`, `test_audit_api_008_strategy_watchlist_mutations_persist_domain_events`, `test_audit_api_008_watchlist_mutation_returns_error_if_audit_persistence_fails`, and `test_audit_api_008_strategy_watchlist_returns_error_if_audit_persistence_fails`. Reconciliation/recovery and execution lifecycle service evidence is recorded under `AUDIT-TEST-002`. | Keep this finding open with narrower scope. Remaining gaps deliberately out of scope: HTTP route harness behavior, transactional rollback semantics after audit failure, frontend handling of non-alert audit-write failure details, market-data/background events, remaining broker-action branches, and remaining mutation paths are not yet verified. |
| AUDIT-LIFE-001 | P0 | Verified | Execution lifecycle / broker ambiguity | PROD-004, PROD-005, BROKER-002, BROKER-012, BROKER-013, STATE-002, STATE-006, STATE-008, FLOW-ENTRY-001 | Broker acknowledgement, timeout, unclear confirmation, unsafe duplicate retry, or later reconciliation could be represented as exact local entry success, exact local entry failure, another broker submission, or an unlinked adoption instead of explicit manual-review/reconciliation-linked state. | Fixed 2026-05-07/2026-05-08: `BrokerOrderStatus` now includes `PENDING`, `TIMED_OUT`, `RATE_LIMITED`, `UNKNOWN`, and `AMBIGUOUS` (`backend/app/core/broker.py`). `StrategyService` treats acknowledged/pending/timeout/rate-limit/unknown/ambiguous entry results as `ExecutionStatus.NEEDS_MANUAL_REVIEW`, leaves the `TradeIntent` in acknowledged active state, preserves the execution `client_request_id` in broker-result evidence even when the broker DTO omits it, records entry `TimeoutError` as `BROKER_CONFIRMATION_TIMEOUT`, suppresses duplicate retries for submitted/acknowledged/partial/manual-review attempts, and keeps the original `client_request_id` without returning a second broker-submit instruction. IG post-ack confirmation lookup failures return broker-neutral `TIMED_OUT`, `RATE_LIMITED`, or `AMBIGUOUS` DTOs. Reconciliation links broker-confirmed open positions back to an existing ambiguous active intent/execution by broker reference, keeps the original `execution_client_request_id`, creates a local `Position`, transitions intent/execution to open-position truth with reconciliation details, and records reconciliation evidence instead of creating an unlinked adoption. `/executions` serialization preserves `client_request_id`, `broker_reference`, manual-review flags, nested broker-result details, and reconciliation markers. Frontend execution typing now includes `SUBMISSION_PENDING`, correlation IDs, and risk truth fields; dashboard notifications, dashboard activity, and strategy execution drawers surface request/broker/reason/reconciliation details. Regression evidence: `test_audit_life_001_acknowledged_only_entry_does_not_create_fill_truth`, `test_audit_life_001_entry_timeout_preserves_ambiguous_manual_review_state`, `test_audit_life_001_non_final_entry_results_preserve_manual_review_state`, `test_audit_life_001_blocks_submitted_entry_duplicate_retry_until_review`, `test_audit_life_001_reconciliation_links_ambiguous_entry_by_broker_reference`, `test_audit_life_001_execution_response_preserves_ambiguity_and_correlation`, `test_audit_life_001_ig_place_order_confirmation_rate_limit_returns_manual_review_dto`, `test_audit_life_001_ig_place_order_confirmation_timeout_returns_manual_review_dto`, and `npm run typecheck`. Broker fake/idempotency coverage is verified under `AUDIT-BROKER-002`; absence of a frontend behavioural harness remains open under `AUDIT-UI-006`. | No further action for `AUDIT-LIFE-001`. |
| AUDIT-LIFE-002 | P0 | Verified | Exit/close lifecycle / open-risk ownership | PROD-004, PROD-011, BROKER-004, BROKER-011, BROKER-012, STATE-005, STATE-007, STATE-008, FLOW-EXIT-001 | Close failure or partial/incomplete close previously preserved the `Position` but moved the linked `TradeIntent` to terminal `FAILED`, removing it from the normal close-admissible intent query and stranding still-open risk outside the strategy exit path. | Fixed 2026-05-07: `_execute_exit_signal()` now leaves failed or incomplete close attempts in `TradeIntentState.CLOSE_REQUESTED` instead of terminal `FAILED`, and `find_close_admissible_trade_intent()` admits `CLOSE_REQUESTED`, `SUBMITTED`, and `ACKNOWLEDGED` close-owner states when matched by position/broker reference. Regression evidence covers close exception, partial close, rejected close, and ambiguous close confirmation while asserting the position remains open, the execution requires manual review, and the linked intent remains close-admissible. Evidence: `backend/.venv/bin/pytest backend/tests/test_strategy_service.py::test_close_failure_keeps_position_open_and_flags_manual_review backend/tests/test_strategy_service.py::test_partial_close_result_moves_execution_to_manual_review backend/tests/test_strategy_service.py::test_audit_life_002_incomplete_close_preserves_close_admissible_intent -q` (`4 passed`); adjacent suites `backend/tests/test_strategy_service.py`, `backend/tests/test_trade_decision_service.py`, and `backend/tests/test_intent_lifecycle_integration.py` passed (`42 passed`). | No further backend implementation action for this stranded-intent defect. Keep route/frontend operator visibility and ambiguity UI evidence tracked under `AUDIT-TEST-001` and UI findings. |
| AUDIT-LIFE-003 | P0 | Verified | Runtime recovery / stopped open risk | PROD-009, PROD-011, BROKER-008, STATE-005, STATE-010, FLOW-RUNTIME-001, FLOW-RECOVERY-001 | Startup recovery previously did not create adoption/recovery evidence for broker-confirmed open risk when the persisted runtime had `runtime_mode="STOPPED"`. It marked the runtime stopped and continued before attaching the remote position. | Fixed 2026-05-07: `RuntimeRecoveryService.recover()` now creates or preserves explicit `Position` plus `RECOVERED_POSITION_ATTACHED` `TradeIntent` evidence before applying the stopped-runtime pause. The runtime remains `STOPPED` and no engine is started, but `current_position_broker_reference` and close-admissible lifecycle ownership are preserved for broker-confirmed open risk. Regression evidence covers stopped runtime with remote broker position and no local position, plus stopped runtime with a matching local position that lacked intent evidence. Evidence: `backend/.venv/bin/pytest backend/tests/test_runtime_recovery_service.py -q` (`5 passed`); adjacent suites `backend/tests/test_runtime_recovery_service.py`, `backend/tests/test_reconciliation_service.py`, and `backend/tests/test_control_plane_service.py` passed (`23 passed`). | No further backend implementation action for this stopped-runtime recovery defect. Keep route/frontend operator visibility and durable domain-event persistence gaps tracked under `AUDIT-TEST-001`, `AUDIT-TEST-002`, and UI findings. |
| AUDIT-LIFE-004 | P1 | Fixed | Runtime ownership / direct state mutation | STATE-003, FLOW-RUNTIME-001, ARCH-013 | Fixed: `set_runtime_mode()` no longer changes persisted runtime ownership to `AUTO` when the existing runtime is `MANUAL`. | `StrategyService.set_runtime_mode()` now syncs runtime mode without passing a `control_mode` override, so `RuntimeStateService.sync_engine_state()` preserves the existing control mode. Runtime-mode domain-event payloads include the preserved control mode for audit context. Regression evidence: `test_audit_life_004_set_runtime_mode_preserves_manual_runtime_ownership` failed against the old behavior (`AUTO`) and now passes with `MANUAL` preserved. Verification: targeted regression passed; adjacent suites passed on 2026-05-11: `backend/tests/test_strategy_service.py` (`44 passed`), `backend/tests/test_control_plane_service.py` (`13 passed`), and `backend/tests/test_runtime_recovery_service.py` (`5 passed`). | No further action for the direct `set_runtime_mode()` ownership bug. Keep broader route/UI runtime-state visibility and transition-table enforcement under `AUDIT-TEST-001`, `AUDIT-UI-006`, and `AUDIT-LIFE-006`. |
| AUDIT-LIFE-005 | P1 | Open - backend simulated-entry provenance slice fixed 2026-05-18 | Simulated vs broker-confirmed truth | BROKER-014, PROD-008, STATE-008, FLOW-ENTRY-001, FLOW-EXIT-001 | Simulated/local fills and closes must be durably distinguishable from live broker-confirmed truth in broker-neutral results and downstream lifecycle records. | `BrokerOrderResult` now carries `execution_source` with `BROKER_CONFIRMED`, `SIMULATED_LOCAL_FILL`, and `SIMULATED_LOCAL_CLOSE` values. `IGBroker` simulated order/close paths set the simulated source, and `StrategyService` persists broker-result source into `Execution.details["broker_result"]` and `TradeIntent.details["broker_result"]`. Simulated entry positions persist `broker_sync_status="SIMULATED_LOCAL_FILL"` instead of `CONFIRMED`, and `TradeService.record_broker_position()` no longer overwrites that simulated status with broker-confirmed timestamps. Regression evidence: `test_audit_life_005_ig_simulated_order_and_close_are_not_broker_confirmed` and `test_audit_life_005_simulated_entry_persists_non_broker_provenance` failed before the contract existed and now pass. Files changed: `backend/app/core/broker.py`; `backend/app/core/ig_broker.py`; `backend/app/services/strategy_service.py`; `backend/app/services/trade_service.py`; `backend/tests/test_ig_broker_sizing.py`; `backend/tests/test_strategy_service.py`. Commands: targeted `backend/.venv/bin/pytest backend/tests/test_broker_service.py backend/tests/test_ig_broker_sizing.py::test_audit_life_005_ig_simulated_order_and_close_are_not_broker_confirmed backend/tests/test_strategy_service.py::test_audit_life_005_simulated_entry_persists_non_broker_provenance -q` failed before implementation with missing `BrokerExecutionSource`, then passed (`4 passed`); `backend/.venv/bin/pytest backend/tests/test_broker_service.py backend/tests/test_ig_broker_sizing.py backend/tests/test_strategy_service.py -q` passed (`53 passed`); adjacent broker/market/route suites passed (`34 passed`); full backend suite `backend/.venv/bin/pytest backend/tests -q` passed (`259 passed`); `git diff --check` passed. | Keep this finding open with narrower remaining scope. Remaining evidence gaps: durable simulated close provenance on persisted closed positions/trades, `/executions` or other read-model contract fields beyond nested details, frontend/operator simulated provenance rendering, and browser/e2e coverage are not yet verified. |
| AUDIT-LIFE-006 | P2 | Open | Lifecycle transition maintainability / terminal protection evidence | STATE-001, STATE-002, STATE-007, PROD-010 | `TradeIntent` and `Execution` transitions are centralized in owner services but still accept arbitrary string states and have no transition table/terminal-state guard, so invalid owner-service calls are not mechanically blocked. | `TradeService.transition_trade_intent()` accepts `TradeIntentState` &#124; `str` and assigns directly to `intent.state` (`backend/app/services/trade_service.py:361` and `backend/app/services/trade_service.py:392`). `TradeService.transition_execution()` accepts `ExecutionStatus` &#124; `str` and assigns directly to `execution.status` (`backend/app/services/trade_service.py:500` and `backend/app/services/trade_service.py:525`). Current tests cover important public paths, but no transition-table tests prove terminal states such as `FAILED`, `CANCELLED`, `CLOSED`, or `CLOSE_CONFIRMED` cannot reactivate through owner-service misuse. | Add transition tables or validators for `TradeIntent` and `Execution`, then add valid/invalid transition tests including terminal-state reactivation and legacy-status write prevention. |
| AUDIT-BROKER-001 | P1 | Fixed 2026-05-18 | Broker-neutral adapter boundary | BROKER-001, BROKER-010, BROKER-BND-003, ARCH-004 | Non-adapter/non-streaming code depended on the concrete IG adapter error type and sometimes exposed IG-specific wording, weakening future broker substitution. | `app.core.broker.BrokerError` is now the application-level broker-neutral exception base, and `IGBrokerError` subclasses it inside the IG adapter. Non-adapter/non-streaming route and service code now catches `BrokerError`, not `IGBrokerError`: `backend/app/api/routes/broker.py`, `backend/app/api/routes/markets.py`, `backend/app/services/broker_service.py`, `backend/app/services/market_data_service.py`, and `backend/app/services/market_overview_service.py`. Market overview operator wording now says broker, not IG, outside the adapter. Regression evidence: `test_audit_broker_001_non_adapter_code_uses_broker_neutral_errors` scans `backend/app` and allows `IGBrokerError` only in `core/ig_broker.py` and `services/ig_streaming_service.py`; `test_reconcile_positions_falls_back_to_local_positions_when_broker_unavailable` now raises neutral `BrokerError`. Files changed: `backend/app/core/broker.py`; `backend/app/core/ig_broker.py`; `backend/app/api/routes/broker.py`; `backend/app/api/routes/markets.py`; `backend/app/services/broker_service.py`; `backend/app/services/market_data_service.py`; `backend/app/services/market_overview_service.py`; `backend/tests/test_broker_service.py`. Commands: targeted broker-neutral tests passed (`4 passed` as part of the targeted slice); route/service adjacent suites passed (`34 passed`); `rg -n "IGBrokerError|Unable to load market overview from IG|IG market status" backend/app` shows only the IG adapter and IG streaming service for `IGBrokerError`, with no stale route wording; `git diff --check` passed. | No remaining action for this finding. Granular broker-neutral subclasses and redaction of raw adapter messages remain future hardening under `AUDIT-SEC-002`/platform error-contract work, not this IG exception leakage finding. |
| AUDIT-BROKER-002 | P1 | Verified | Broker fake fidelity / test evidence | BROKER-002, BROKER-007, BROKER-012, BROKER-013, BROKER-014, TEST-003, TEST-006, TEST-015 | Fixed: broker fakes can explicitly queue unsafe read, sizing, position, order, and close outcomes, and the fake contract matrix now proves those capabilities instead of relying on healthy defaults. | `FakeBroker.account_summary_outcomes`, `market_details_outcomes`, `risk_sizing_quote_outcomes`, `normalize_order_size_outcomes`, and `position_outcomes` can queue exceptions or broker DTOs for explicit degraded/live-like read, sizing, normalization, and reconciliation-input paths. Strict flags make missing account, market, sizing, normalization, and position setup fail tests. `FakeBroker.quote_risk_sized_order()` covers exact point-value, exact contract-risk, approximate price-delta, incomplete unsupported profiles, and IG-shaped `metadata["ig_sizing"]` exact point-value metadata. `place_order()` and `close_position()` require queued outcomes, never auto-succeed on unqueued retries, and fill missing `BrokerOrderResult.client_request_id` / `requested_size` from the outgoing request so ambiguity and retry tests cannot accidentally drop correlation evidence. Regression evidence: `test_audit_broker_002_fake_account_summary_can_require_explicit_read_truth`; `test_audit_broker_002_fake_market_details_can_require_explicit_read_truth`; `test_audit_broker_002_fake_sizing_quote_can_require_explicit_outcomes`; `test_audit_broker_002_fake_sizing_understands_ig_point_value_metadata`; `test_audit_broker_002_fake_derived_sizing_modes_are_contract_pinned`; `test_audit_broker_002_fake_derived_sizing_rejects_incomplete_profiles`; `test_audit_broker_002_fake_normalization_can_require_explicit_outcomes`; `test_audit_broker_002_fake_positions_can_require_explicit_reconciliation_truth`; `test_audit_broker_002_reconciliation_uses_strict_fake_position_outcome`; `test_audit_broker_002_fake_order_non_final_results_keep_request_correlation`; `test_audit_broker_002_fake_entry_incomplete_final_results_keep_correlation`; `test_audit_broker_002_fake_close_incomplete_results_keep_request_correlation`; `test_audit_broker_002_fake_mutation_retries_never_auto_succeed`. Existing service tests still cover selected strategy/allocator failures. | No further action for broker fake fidelity. Keep production simulated-vs-broker-confirmed provenance tracked under `AUDIT-LIFE-005`/`BROKER-014`, route/audit persistence under `AUDIT-TEST-001`/`AUDIT-TEST-002`, and execution-time broker drift under `AUDIT-RISK-002`. |
| AUDIT-BROKER-003 | P0 | Verified | Broker read fail-closed / market status | BROKER-007, BROKER-015, RISK-002, RISK-015, STATE-008, FLOW-ENTRY-001 | Unknown or unrecognized broker market status could previously fail open in entry/risk gates if the DTO also reported `tradable=True`. | Fixed in `MarketStatusService._is_market_open()` by changing market-open evaluation from "not blocked" to an explicit open allowlist (`TRADEABLE`, `TRADEABLE_ONLINE`) and treating missing, blank, unknown, or restricted statuses as closed. Regression tests now cover `None`, blank, unknown, dealing-restricted, closed, suspended, offline, stale update time, missing update time, broker dealing restrictions, and a strategy entry path with price present plus `tradable=True` that must not submit an order. Evidence: `backend/.venv/bin/pytest backend/tests/test_market_status_service.py backend/tests/test_strategy_service.py::test_audit_broker_003_unknown_market_status_blocks_entry_even_when_tradable -q` (`11 passed`); adjacent suites `backend/tests/test_strategy_service.py`, `backend/tests/test_trade_decision_service.py`, `backend/tests/test_capital_allocator_service.py`, and `backend/tests/test_portfolio_risk_service.py` also passed with the new tests (`65 passed`). | No further action for this fail-open defect. Keep broader route/frontend blocked-reason visibility tracked under `AUDIT-TEST-001` and UI findings. |
| AUDIT-BROKER-004 | P1 | Fixed | Broker sizing details dependency | BROKER-001, BROKER-005, RISK-008, RISK-009, RISK-016 | Fixed: app services no longer depend on `BrokerRiskSizingQuote.details["account_currency"]` for decision or risk-audit currency. | `BrokerRiskSizingQuote.account_currency` is now a first-class broker-neutral DTO field. `IGBroker.quote_risk_sized_order()` and broker fakes populate that field for exact sizing quotes, while adapter/debug `details` no longer carries top-level `account_currency`. `CapitalAllocatorService._serialize_sizing_quote()` serializes `account_currency` explicitly. `TradeDecisionService._create_trade_intent()` and `StrategyService._estimate_execution_risk_snapshot()` read the serialized first-class field instead of adapter-owned details. Regression evidence: `test_audit_broker_004_allocator_serializes_sizing_currency_as_first_class_quote_field`; `test_audit_broker_004_decision_uses_first_class_sizing_quote_currency`; `test_audit_broker_004_execution_risk_snapshot_uses_first_class_sizing_quote_currency`; IG adapter assertion that successful quote exposes `quote.account_currency` and omits `details["account_currency"]`. Verification: `backend/.venv/bin/pytest backend/tests/test_capital_allocator_service.py backend/tests/test_trade_decision_service.py backend/tests/test_strategy_service.py backend/tests/test_ig_broker_sizing.py backend/tests/test_broker_fake_contract.py -q` (`101 passed`) on 2026-05-11. | No further action for this sizing-currency boundary defect. Keep remaining broker-neutral exception, simulated/live provenance, durable audit persistence, and broker construction boundary gaps tracked under their existing findings. |
| AUDIT-BROKER-005 | P2 | Open | Broker construction boundary / dependency injection | BROKER-001, ARCH-004, ARCH-011, TEST-006 | Direct `get_broker()` calls are spread through runtime and services instead of being limited to a composition root, making broker substitution depend on monkeypatching many modules and increasing hidden coupling risk. | Direct `get_broker()` usage appears in `runtime_manager.start()`, `MarketDataService.__init__`, `ReconciliationService.__init__`, `BrokerService`, `MarketOverviewService`, `MarketStatusService`, `ChartService`, `RuntimeRecoveryService`, and `IGStreamingService`. `DashboardService` no longer directly fetches a broker after the `AUDIT-API-005`/`AUDIT-API-007` fix. Direct `IGBroker(...)` construction is limited to `broker_factory.py` and adapter tests, which is good, but `backend/tests/conftest.py` must monkeypatch `app.core.runtime.get_broker`, `app.core.broker_factory.get_broker`, `app.services.market_status_service.get_broker`, `app.services.reconciliation_service.get_broker`, and `app.services.runtime_recovery_service.get_broker` to keep tests isolated. | Keep concrete adapter construction in `broker_factory.py`, but pass `Broker` dependencies into services/runtimes through constructors or root composition where practical. Add construction-boundary tests or import/call-graph checks so new services do not instantiate or fetch concrete broker dependencies accidentally. |

## Test coverage audit snapshot

| Area | Current verification level | Evidence | Gap |
| --- | --- | --- | --- |
| Backend service tests | `SERVICE_VERIFIED` for many lifecycle, allocation, market-data, recovery, AIMEE, control-plane, and runtime-leadership paths | Full backend suite passed on 2026-05-10 (`backend/.venv/bin/pytest backend/tests -q`, `228 passed`). Named service evidence includes entry/exit lifecycle, allocation fail-closed, market-status fail-closed, account/sizing/account-drift/exact-sizing-quote-size-drift/exact-executable-risk-drift/approximate-sizing-quote-size-drift/stable-size approximate-executable-risk-drift/broker-metadata-failure/cache-bypassing market-status execution revalidation, acknowledged-only/timeout entry ambiguity, close-failure/partial/rejected/ambiguous close ownership preservation, stopped-runtime broker-risk recovery, stale data, reconciliation/adoption, runtime recovery, runtime leader lease duplicate/takeover/release behavior, lifespan duplicate-loop skipping, AIMEE side-effect-free tests, fake strict read/sizing/position outcome tests, fake exact/contract/approx sizing matrix, fake IG-shaped exact sizing, strict fake reconciliation-input service use, fake retry/idempotency safety, and fake non-final/partial outcome correlation tests. Route serialization now preserves material submitted/fill drift flags in `/executions`. | Important failure modes still missing: frontend behavioural submitted/fill material drift proof, terminal transition-table protection, and domain-event persistence. |
| Route/API tests | `NOT_TESTED` to partial `ROUTE_VERIFIED` | Direct route-function tests exist for health and AIMEE; route functions are also called directly in a few market tests. `backend/tests/test_testing_routes.py` now verifies `/testing/reset-history` router registration is gated by explicit config and the enabled route still clears history. | No `TestClient`/HTTP route test suite; no route-wide passive no-write, active-read side-effect, response-contract, or mutation error/audit proof. |
| Frontend tests | partial `FRONTEND_VERIFIED` for narrow helper/static/rendered guards | `frontend/tests/execution-notifications.test.mjs` covers execution drift notification text, `frontend/tests/allocation-alert-passive-calls.test.mjs` covers passive allocation-alert calls, `frontend/tests/allocation-alert-mutations.test.mjs` covers allocation-alert mutation source behavior, `frontend/tests/risk-allocation-data-quality.test.mjs` covers the risk-page backend-unavailable metadata path, `frontend/tests/operator-status-provenance.test.mjs` covers nav fallback/provenance and live broker provenance source strings, `frontend/tests/ui-enum-truth.test.mjs` covers narrow pending/unknown/simulated enum truth, `frontend/tests/fallback-data-guard.test.mjs` covers control fallback, markets fallback, and live-view derived-confidence source guards, and `frontend/tests/rendered-operator-states.test.mjs` covers rendered pending execution, allocation-alert mutation-error, unknown/simulated risk-confidence, risk budget exposure-unavailable truth, markets catalogue-unavailable count truth, markets watchlist/streaming provenance, narrow AIMEE control-plane/open-risk/Operate/strategies/events telemetry-unavailable components, control-plane family missing open-risk state, live-view all-source outage state, dashboard stale-feed stream state, coverage watchlist polling-fallback state, strategy open-position stop provenance, and visible test-only destructive reset labeling. `npm run test:frontend` passed on 2026-05-19 (`45 passed`). | No broad browser/e2e suite proves full loading/error/empty/stale/degraded/fallback, passive-vs-mutation, AIMEE passive/advisory separation, freshness, complete enum parity, or refresh-failure behaviour. |
| Full-stack/e2e | `NOT_TESTED` | No browser/e2e tests were found. | No proof that backend truth appears correctly in dashboard, live, risk, control-plane, coverage, markets, events, strategies, or AIMEE operator surfaces. |
| Broker/service fakes | `SERVICE_VERIFIED` | `FakeBroker` can queue account/market/sizing/normalization/position outcomes, order/close results, or exceptions and supports derived sizing/normalization. Contract tests prove strict account/market/sizing/normalization/position setup, exact point-value, exact contract-risk, approximate price-delta, incomplete unsupported profiles, IG-shaped exact point-value metadata, unqueued retry failure, strict reconciliation-service position outcomes, and queued non-final/incomplete entry/close results preserving client request id correlation and requested size. | No broker-fake-fidelity gap remains under `AUDIT-BROKER-002`; production simulated-vs-broker-confirmed truth remains tracked separately under `AUDIT-LIFE-005`/`BROKER-014`. |
| Coverage matrix accuracy | `NEEDS_AUDIT` for completeness | 192 unique source P0/P1 IDs were found outside `99-spec-coverage-matrix.md`; 68 boundary/surface IDs are absent from explicit matrix rows. | Matrix remains backend-heavy, often file-name based, and lacks route/frontend/e2e evidence columns strong enough to prevent confidence inflation. |

## Finding statuses

- Open
- In progress
- Fixed
- Verified
- Accepted risk
- Superseded

## Severity definitions

- P0: Could allow unsafe trading, unbounded risk, unauditable broker action, unmanaged open exposure, hidden state corruption, or false readiness for a safety-critical control.
- P1: Violates or obscures a documented invariant, breaks a core audit flow, weakens P0/P1 evidence, misleads the operator/auditor, hides degraded state, mutates unexpectedly, or weakens auditability.
- P2: Documentation drift, incomplete central tracking, ambiguous ownership, confusing terminology, missing evidence structure, or maintainability issue outside an immediate P0/P1 safety path.
- P3: Minor cleanup or editorial issue. The spec set currently uses P0-P2 for requirement severity; P3 is available for audit-log housekeeping if needed.

## Audit runs

| Date | Audit | Scope | Result | Follow-up |
| --- | --- | --- | --- | --- |
| 2026-05-19 | `AUDIT-API-008`/`AUDIT-TEST-002` close broker-action audit slice | Strategy exit/close broker-action lifecycle, simulated close provenance, trade/position read-model fields, and rendered operator trade-table provenance; narrow `AUDIT-LIFE-005`/`AUDIT-OBS-001`/`TEST-016`/`AUDIT-UI-006` evidence upgrade | Added first-class `close_execution_source` persistence on closed `Trade` and `Position` rows, kept simulated local close truth as `SIMULATED_LOCAL_CLOSE` instead of collapsing it to broker-confirmed `CONFIRMED`, and exposed close source through trade and position route serializers. Added required session-bound close broker-action domain events: `broker.close_confirmed` for successful closes and `broker.close_requires_manual_review` for partial/incomplete close outcomes. Events carry service actor/source, execution client-request correlation id, strategy/instrument, related intent/execution/position/trade ids where available, previous/new execution state, broker close reference, execution source, and broker-result payload. If the required close-confirmed audit event cannot be persisted, the execution row is marked with `domain_event_persistence_failed` and an `audit_event_failures` entry rather than silently claiming clean audit confidence. Frontend trade tables now type and render close-source provenance, with visible `Simulated local close` and `Local simulation; not broker-confirmed close truth.` copy. Regressions failed before implementation where practical and now pass: `test_audit_life_005_simulated_close_persists_non_broker_provenance`, `test_audit_test_002_successful_close_persists_broker_action_domain_event`, `test_audit_obs_001_close_audit_failure_marks_execution`, `test_audit_test_002_incomplete_close_persists_manual_review_domain_event`, and `AUDIT-LIFE-005 rendered trade tables label simulated close as local truth`. Verification commands: targeted backend regression slice first failed (`3 failed` after setup correction, then the incomplete-close event regression failed separately) and passed after implementation (`4 passed`); targeted rendered trade-table regression passed (`1 passed, 20 skipped`); full strategy-service suite passed (`52 passed`); adjacent lifecycle/reconciliation/recovery suite passed (`20 passed`); adjacent execution-route/mutation-audit suite passed (`19 passed`); full backend suite passed (`291 passed`); frontend suite passed (`46 passed`); `npm run typecheck`, `npm run lint`, touched-file pre-commit, and `git diff --check` passed. Files changed: `backend/app/models/trade.py`, `backend/app/db/init_db.py`, `backend/app/services/strategy_service.py`, `backend/app/services/trade_service.py`, `backend/app/api/routes/trades.py`, `backend/app/api/routes/positions.py`, `backend/tests/test_strategy_service.py`, `frontend/lib/types.ts`, `frontend/components/trades-table.tsx`, `frontend/components/dashboard/recent-trades-table.tsx`, `frontend/components/dashboard/dashboard-live.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, `AUDIT-OBS-001`, `AUDIT-LIFE-005`, and `AUDIT-UI-006` remain Open with narrower scope rather than Verified globally. This slice is Fixed for successful close audit persistence, incomplete close manual-review audit persistence, close audit-write failure marking, backend/read-model simulated close provenance, and rendered trade-table simulated close provenance. Remaining gaps deliberately out of scope: HTTP `TestClient` route harness behavior under `AUDIT-TEST-001`, browser/e2e simulated close rendering, complete route response schema parity, transactional rollback semantics for every audit failure path, direct broker-action HTTP coverage if added later, remaining non-close broker-action/background paths, and broad frontend handling of non-alert audit-write failures. |
| 2026-05-19 | `AUDIT-API-008`/`AUDIT-TEST-002` service/background audit slice | Strategy runtime/duplicate retry, deployment/open-risk, Tier 1 polling health-transition, and Tier 2 market-data/coverage service audit evidence; narrow `AUDIT-OBS-001`/`TEST-016` evidence upgrade | Added `record_required_domain_event()` and moved selected safety-relevant service/background events from mocked best-effort `record_event()` to active-session `record_event_in_session()`: strategy runtime start/stop/mode changes, duplicate entry/close retry suppression, partial-fill manual-review and execution-drift events, autonomous runtime restart/deployment state changes, unmanaged-open-risk events, Tier 2 market refresh/promotion request/allocation-cycle events, coverage promotion accepted/rejected events, and Tier 1 polling fallback/stale/recovered health transitions when the market-data loop has a session. New persisted events carry actor/source, correlation/runtime/execution IDs where available, previous/new state or previous/new open-risk state, deployment/promotion ids, and related payload context. Required service audit persistence now raises rather than returning clean success when the session-bound audit write returns `None`; the coverage allocator regression proves the promotion request remains pending and no event is present when this required event fails before promotion, and the polling-health regression proves a failed health-transition audit write does not silently claim a clean transition. Regressions failed before implementation and now pass: `test_audit_test_002_allocator_accept_persists_session_bound_domain_event`, `test_audit_test_002_allocator_reject_persists_session_bound_domain_event`, `test_audit_obs_001_allocator_audit_failure_blocks_clean_mutation`, `test_audit_test_002_tier2_refresh_persists_session_bound_domain_events`, `test_audit_test_002_polling_health_transitions_persist_with_session`, `test_audit_obs_001_polling_health_audit_failure_blocks_clean_transition`, `test_audit_test_002_deployment_reconcile_persists_session_bound_domain_events`, `test_audit_test_002_unmanaged_open_risk_persists_domain_event`, `test_audit_test_002_strategy_start_stop_persist_session_bound_domain_events`, and `test_audit_test_002_duplicate_entry_retry_suppression_persists_domain_event`. Updated existing mutation-audit tests to account for the newly durable underlying strategy-service runtime events in addition to route operator events. Verification commands: initial targeted regressions failed before implementation (`8 failed`) and the added polling-health regressions failed before implementation (`2 failed`); targeted service/background regressions passed after implementation; adjacent service/route audit suite passed (`94 passed`); full backend suite `backend/.venv/bin/pytest backend/tests -q` passed (`287 passed`). Files changed: `backend/app/services/audit_event_recorder.py`, `backend/app/services/coverage_allocator_service.py`, `backend/app/services/market_data_service.py`, `backend/app/services/strategy_deployment_manager_service.py`, `backend/app/services/strategy_service.py`, `backend/tests/test_control_plane_service.py`, `backend/tests/test_coverage_allocator_service.py`, `backend/tests/test_market_data_service.py`, `backend/tests/test_mutation_audit_events.py`, `backend/tests/test_strategy_service.py`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, and `AUDIT-OBS-001` remain Open with narrowed scope rather than Verified. This slice is Fixed for the selected strategy/deployment/coverage/Tier 1/Tier 2 market-data service events and Verified by session-bound regression evidence. Remaining gaps deliberately out of scope: HTTP `TestClient` route harness behavior (`AUDIT-TEST-001` still Open), broader simulated close browser/e2e provenance, frontend display for non-alert audit-write failures, transactional semantics for every audit failure path, broad broker-action branches, and any remaining market-data/background/reconciliation/recovery/mutation paths not named above. |
| 2026-05-19 | `AUDIT-API-008`/`AUDIT-TEST-002` allocation and TradeIntent decision audit slice | Allocation-cycle completion plus TradeIntent creation/transition durable lifecycle audit evidence; narrow `AUDIT-OBS-001`/`PROD-012` evidence upgrade | Added session-bound durable `DomainEvent` persistence for `CapitalAllocatorService.allocate()` allocation-cycle completion and `TradeService.create_trade_intent()` / `TradeService.transition_trade_intent()` decision lifecycle changes. Allocation events now carry service actor/source, cycle correlation id, previous/new state, candidate approval/rejection counts, risk totals, budget/rejection summaries, and per-candidate selected/rejected decisions. TradeIntent events now carry service actor/source, intent or execution-client correlation id, allocation cycle id, previous/new state, decision/close reasons, broker references, risk fields, confidence/provenance, and related position/trade ids. If allocation or TradeIntent audit persistence fails, the affected `AllocationCycle` or `TradeIntent` row is durably marked with `domain_event_persistence_failed` and `audit_event_failures` rather than silently claiming clean audit confidence. New regressions in `backend/tests/test_decision_audit_events.py` failed before implementation (`8 failed`) and now pass: `test_audit_test_002_allocation_cycle_persists_domain_event_despite_global_noop`, `test_audit_obs_001_allocation_cycle_marks_audit_persistence_failure`, `test_audit_test_002_trade_intent_creation_persists_domain_event`, `test_audit_test_002_trade_intent_transition_persists_domain_event`, `test_audit_test_002_trade_intent_transition_records_related_domain_ids`, `test_audit_obs_001_trade_intent_audit_failure_marks_intent`, `test_audit_test_002_decision_flow_links_allocation_and_intent_audit_events`, and `test_audit_test_002_rejected_decision_flow_persists_rejection_audit_event`. Verification commands: targeted new regression suite passed (`8 passed`); adjacent decision/lifecycle/allocation suite passed (`37 passed`); allocation read and intent lifecycle suite passed (`12 passed`); strategy and mutation-audit suites passed (`52 passed`, `17 passed`); reconciliation/recovery suite passed (`16 passed`); full backend suite `backend/.venv/bin/pytest backend/tests -q` passed (`299 passed`); targeted `ruff check` passed; `git diff --check` passed. Files changed: `backend/app/services/capital_allocator_service.py`, `backend/app/services/trade_service.py`, `backend/tests/test_decision_audit_events.py`, `backend/tests/test_lifecycle_audit_events.py`, `backend/tests/test_reconciliation_service.py`, `backend/tests/test_runtime_recovery_service.py`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, and `AUDIT-OBS-001` remain Open with narrower scope rather than Verified. This slice is Fixed for allocation-cycle and TradeIntent decision-lifecycle service audit persistence/failure behavior. Remaining gaps deliberately out of scope: HTTP route harness behavior, transactional rollback semantics after audit failure, frontend display of non-alert audit-write failures, broad market-data/background events, remaining broker-action branches, remaining reconciliation/recovery branches beyond selected existing tests, broader simulated close provenance, redaction, and multi-process observability. |
| 2026-05-19 | `AUDIT-API-008`/`AUDIT-TEST-002` execution and watchlist audit slice | Execution lifecycle domain events plus shortlist/strategy-watchlist operator mutation route audit evidence; narrow `AUDIT-OBS-001`/`TEST-016` evidence upgrade | Added session-bound durable `DomainEvent` persistence for `TradeService.create_execution()` and `TradeService.transition_execution()` so `SUBMISSION_PENDING` creation and later execution transitions survive the global `DomainEventService.record_event` no-op in `backend/tests/conftest.py`. Execution events now carry service actor/source, client request correlation id, previous/new state, execution/intent/position/trade references, broker reference, risk fields, and details; if execution event persistence fails, the execution row is durably marked with `domain_event_persistence_failed` and `audit_event_failures`. Added required route audit events for `POST/DELETE /watchlist/shortlist/{instrument_id}`, `POST /strategy-watchlist/bulk`, and `DELETE /strategy-watchlist/{instrument_id}` with previous/new state, affected instruments, related entry ids, actor/source, and representative `503` audit-write failure behavior. Regressions failed before implementation and now pass: `test_audit_test_002_execution_creation_persists_submission_pending_domain_event`, `test_audit_test_002_execution_transition_persists_domain_event_despite_global_noop`, `test_audit_test_002_execution_transition_records_related_domain_ids`, `test_audit_obs_001_execution_transition_marks_audit_persistence_failure`, `test_audit_api_008_shortlist_mutations_persist_domain_events`, `test_audit_api_008_strategy_watchlist_mutations_persist_domain_events`, `test_audit_api_008_watchlist_mutation_returns_error_if_audit_persistence_fails`, and `test_audit_api_008_strategy_watchlist_returns_error_if_audit_persistence_fails`. Verification commands: targeted audit suite passed (`21 passed`); adjacent route/lifecycle/watchlist/recovery suite passed (`113 passed`); full backend suite `backend/.venv/bin/pytest backend/tests -q` passed (`277 passed`); `backend/.venv/bin/ruff check backend/app/api/routes/markets.py backend/app/services/trade_service.py backend/tests/test_lifecycle_audit_events.py backend/tests/test_mutation_audit_events.py` passed; `git diff --check` passed. Files changed: `backend/app/services/trade_service.py`, `backend/app/api/routes/markets.py`, `backend/tests/test_lifecycle_audit_events.py`, `backend/tests/test_mutation_audit_events.py`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, and `AUDIT-OBS-001` remain Open with narrower scope rather than Verified. This slice is Fixed for selected execution lifecycle event durability and watchlist/strategy-watchlist route mutation audit persistence/failure. Remaining gaps deliberately out of scope: HTTP `TestClient` route harness behavior, transactional rollback semantics after audit failure, frontend display for non-alert audit-write failures, broad market-data/background events, simulated close provenance, and remaining broker-action/reconciliation/recovery/mutation branches. |
| 2026-05-19 | `AUDIT-UI-006` markets watchlist/streaming provenance slice | Rendered Markets catalogue and strategy-watchlist states where tradable/watchlisted/streaming data could be mistaken for trading or entry approval; narrow `UI-MARKETS-001`/`UI-001`/`UI-002`/`UI-007`/`UI-011` evidence upgrade | Added rendered SSR regressions `AUDIT-UI-006 rendered markets watchlist provenance does not imply trading approval` and `AUDIT-UI-006 rendered markets streaming provenance stays separate from entry approval` to `frontend/tests/rendered-operator-states.test.mjs`. They failed before implementation because watchlisted-but-not-streaming rows rendered `eligible`, the strategy-watchlist panel said `Eligible for backend streaming/evaluation`, and streaming rows lacked visible data-coverage-only / not-entry-approval provenance. `MarketOverviewDashboard` now renders catalogue states as `Watchlisted, not streaming` or `Streaming data`, renders watchlist rows as `Evaluation candidate only` or `Streaming data`, and shows visible `Not trading approval` / `Not entry approval` details. Verification commands: targeted `node --test --test-name-pattern='rendered markets .*provenance' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 2 failed, 18 skipped`) and passed after (`2 passed, 18 skipped`); `npm run test:frontend` passed (`45 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. Files changed: `frontend/components/markets/market-overview-dashboard.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered Markets watchlist/streaming provenance, not Verified for all Markets, route/browser behaviour, or market-data freshness. Remaining evidence gaps: browser/e2e Markets behavior, feed stale/disconnected fixtures, mutation-control refresh failures, route/API freshness preservation, complete enum parity, AIMEE full advisory flow, and backend durable audit persistence. |
| 2026-05-19 | `AUDIT-UI-004`/`AUDIT-UI-006` AIMEE passive/advisory boundary slice | AIMEE passive snapshot source proof plus rendered advisory-question side-effect copy; narrow `TEST-012`/`TEST-016` evidence upgrade | Added `frontend/tests/aimee-passive-boundary.test.mjs`, which proves the passive AIMEE loader imports/calls `getAimeeSnapshot()` only, `getAimeeSnapshot()` uses `GET /aimee/snapshot` without POST/persistence, passive AIMEE files do not pass `persist: true` or call mutation-like review GET routes, and `askOperationalQuestion()` remains an explicit `POST /reviews/questions` path with backend `detail` preservation. Updated `AimeeConversation` so question controls visibly render `Advisory questions`, `Creates advisory review record`, `Ask & record`, and advisory aria-label copy before submission. Added rendered regression `AUDIT-UI-004 rendered AIMEE advisory controls disclose review persistence before submission`; it failed before the copy change because the control rendered only `Questions`/`Ask`, then passed. Verification commands: first targeted `node --test --test-name-pattern='AIMEE|AUDIT-UI-004 rendered AIMEE' tests/*.test.mjs` failed on missing advisory copy, then passed after implementation (`9 passed, 34 skipped`); `npm run test:frontend` passed (`43 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. Files changed: `frontend/components/aimee/aimee-conversation.tsx`, `frontend/tests/aimee-passive-boundary.test.mjs`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-004` and `AUDIT-UI-006` remain Open with narrower scope. This slice is Fixed for AIMEE passive source separation and rendered advisory persistence disclosure. Remaining gaps: browser/e2e drawer-open proof, full advisory success/error flow rendering, response schema/model coverage, other mutation controls, and broad loading/error/stale/degraded frontend coverage. |
| 2026-05-19 | `AUDIT-API-008`/`AUDIT-TEST-002` review and recovery audit slice | Review/advisory active persistence plus selected reconciliation/recovery durable audit evidence; narrow `AUDIT-OBS-001` evidence upgrade | Added required session-bound domain-event persistence for `GET /reviews/operator-summary?persist=true`, `GET /reviews/daily?persist=true`, `GET /reviews/strategies/{strategy_name}?persist=true`, `GET /reviews/runtime-health?persist=true`, `GET /reviews/trades/{trade_id}/postmortem?persist=true`, and `POST /reviews/questions`. Persisted review events record actor, source, `review:{type}:{id}` correlation id, review id/type/scope, previous/new persisted state, generation mode, routed review context, and related strategy/trade ids where applicable; review/advisory routes return `503` instead of clean success if required audit persistence fails after the review record is created. Also moved selected reconciliation/recovery events from mocked `record_event()` to active-session `record_event_in_session()`: unmatched broker-position adoption, forced local close, runtime recovery resume, and broker-mismatch recovery-required. Regressions failed before implementation and now pass: `test_audit_api_008_review_persist_true_routes_persist_domain_events`, `test_audit_api_008_review_advisory_question_persists_domain_event`, `test_audit_api_008_review_persist_true_returns_error_if_audit_persistence_fails`, `test_audit_api_008_review_advisory_returns_error_if_audit_persistence_fails`, `test_audit_test_002_reconciliation_adoption_persists_domain_event`, `test_audit_test_002_reconciliation_forced_close_persists_domain_events`, `test_audit_test_002_runtime_recovery_resumed_runtime_persists_domain_event`, and `test_audit_test_002_runtime_recovery_broker_mismatch_persists_domain_event`. Verification commands: targeted mutation-audit suite passed (`13 passed`); adjacent review/passive/auth/reconciliation/recovery suites passed (`48 passed`); full backend suite passed (`269 passed`). Files changed: `backend/app/api/routes/ai_reviewer.py`, `backend/app/services/reconciliation_service.py`, `backend/app/services/runtime_recovery_service.py`, `backend/tests/test_mutation_audit_events.py`, `backend/tests/test_reconciliation_service.py`, `backend/tests/test_runtime_recovery_service.py`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, and `AUDIT-OBS-001` remain Open with narrowed scope. This slice is Fixed for review/advisory route audit persistence/failure and selected recovery/reconciliation service event persistence. Remaining evidence gaps deliberately out of scope: broker-action/lifecycle route mutations, remaining recovery/reconciliation branches, broad market-data/background events, HTTP route harness behavior, transactional rollback semantics after audit failure, frontend handling of non-alert audit-write failures, broader simulated close browser/e2e provenance, and redaction/multi-process observability. |
| 2026-05-19 | `AUDIT-API-008`/`AUDIT-UI-004` allocation-alert mutation audit and refresh-failure slice | Allocation-alert acknowledge/resolve durable route audit evidence plus frontend mutation/refresh-failure truth; narrow `AUDIT-TEST-002`/`AUDIT-UI-006` evidence upgrade | Added required session-bound domain-event persistence for `POST /allocation/alerts/{alert_id}/acknowledge` and `POST /allocation/alerts/{alert_id}/resolve`. The routes now record `operator.allocation_alert_acknowledged` / `operator.allocation_alert_resolved` with actor, previous/current state, alert key/type/severity, and related intent/cycle/execution ids, and return `503` instead of clean success if required audit persistence fails. Frontend alert mutation state now distinguishes mutation failure from post-mutation active-read refresh failure; a refresh failure renders `Mutation submitted, but backend alert truth could not be refreshed.` plus `Refresh failed: ...`, not `Mutation confirmed`. Regressions `test_audit_api_008_allocation_alert_mutations_persist_domain_events`, `test_audit_api_008_allocation_alert_returns_error_if_audit_persistence_fails`, `AUDIT-UI-004 allocation alert refresh failures do not render clean success`, and `AUDIT-UI-004 rendered allocation alert refresh failure is not clean success` failed before implementation and now pass. Verification commands: `backend/.venv/bin/pytest backend/tests/test_mutation_audit_events.py -q` (`9 passed`); `backend/.venv/bin/pytest backend/tests/test_allocation_alert_routes.py backend/tests/test_mutation_audit_events.py -q` (`13 passed`); targeted frontend `AUDIT-UI-004` tests passed (`5 passed, 15 skipped`); `npm run test:frontend` passed (`39 passed`); `npm run typecheck` passed. Files changed: `backend/app/api/routes/allocation.py`, `backend/tests/test_mutation_audit_events.py`, `frontend/components/risk/risk-allocation-live.tsx`, `frontend/tests/allocation-alert-mutations.test.mjs`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, `AUDIT-UI-004`, and `AUDIT-UI-006` remain Open with narrower scope. This slice is Fixed for allocation-alert route audit persistence/failure and rendered refresh-failure truth. Remaining gaps after the later review/recovery audit slice: broker-action/lifecycle route mutations, remaining recovery/reconciliation branches, transactional rollback semantics after audit failure, non-alert audit-write frontend details, other mutation controls, and browser/e2e coverage. |
| 2026-05-18 | `AUDIT-LIFE-005`/`AUDIT-BROKER-001` broker provenance boundary slice | Broker-neutral error boundary, simulated order/close result source, simulated entry lifecycle provenance; narrow `BROKER-001`/`BROKER-014`/`TEST-003` evidence upgrade | Added `BrokerError` and `BrokerExecutionSource` to the broker-neutral contract. `IGBrokerError` now subclasses `BrokerError`, non-adapter routes/services catch `BrokerError`, and stale IG-specific market-overview wording was made broker-neutral. Simulated IG order/close results now carry `SIMULATED_LOCAL_FILL`/`SIMULATED_LOCAL_CLOSE`; strategy entry lifecycle persists broker-result source into execution and intent details; simulated entry positions retain `broker_sync_status="SIMULATED_LOCAL_FILL"` instead of being overwritten to `CONFIRMED`. Regressions `test_audit_broker_001_non_adapter_code_uses_broker_neutral_errors`, `test_audit_life_005_ig_simulated_order_and_close_are_not_broker_confirmed`, and `test_audit_life_005_simulated_entry_persists_non_broker_provenance` failed before implementation where practical and pass after. Verification commands: targeted broker/provenance slice passed (`4 passed`), related strategy/IG/broker-service suite passed (`53 passed`), adjacent broker/market/execution route suite passed (`34 passed`), full backend suite passed (`259 passed`), `rg -n "IGBrokerError|Unable to load market overview from IG|IG market status" backend/app` shows IG exception references only inside adapter/streaming code, and `git diff --check` passed. Files changed: `backend/app/core/broker.py`, `backend/app/core/ig_broker.py`, broker route/service callers, strategy/trade services, focused backend tests, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-BROKER-001` is Fixed. `AUDIT-LIFE-005` remains Open with narrower scope: durable simulated close provenance on closed positions/trades, route/read-model contract fields beyond nested details, and frontend/operator simulated provenance rendering remain unverified. |
| 2026-05-18 | `AUDIT-UI-006` remaining rendered operator-state slice | Rendered AIMEE strategies/events telemetry-unavailable truth, strategy open-position stop provenance, and test-only destructive reset labeling; narrow `UI-004`/`UI-007`/`UI-009`/`UI-011`/`FLOW-AIMEE-001` evidence upgrade | Added four rendered SSR regressions to `frontend/tests/rendered-operator-states.test.mjs`: `AUDIT-UI-006 rendered AIMEE strategies summary does not convert missing telemetry into zero-health truth`, `AUDIT-UI-006 rendered AIMEE events summary does not convert missing telemetry into disconnected-zero truth`, `AUDIT-UI-006 rendered strategy open-position runtime keeps stop action provenance visible`, and `AUDIT-UI-006 rendered test reset control is visibly test-only and destructive`. The targeted test command failed before implementation (`0 passed, 4 failed, 12 skipped`) because AIMEE rendered monitoring/zero-count health copy for missing telemetry, the strategy stop action lacked open-risk provenance, and the reset control rendered as an ordinary clear button. `AimeeOverview` summary helpers now downgrade missing strategy/event telemetry to unavailable/degraded truth, `StrategyLive` shows broker reference and open-risk stop provenance for runtimes with open positions, and `ResetHistoryButton` renders visible test-only/destructive reset copy. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered (AIMEE strategies|AIMEE events|strategy open-position|test reset)' tests/rendered-operator-states.test.mjs` passed after implementation (`4 passed, 12 skipped`); `npm run test:frontend` passed (`37 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. Files changed: `frontend/components/aimee/utils.ts`, `frontend/components/strategy/strategy-live.tsx`, `frontend/components/testing/reset-history-button.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope rather than Verified. This slice is Fixed for the added rendered AIMEE telemetry, strategy stop provenance, and test-only reset labeling paths. Remaining evidence gaps: browser/e2e route behavior, stateful refresh-failure tests, complete enum parity, complete AIMEE passive/advisory call separation, broader mutation-control coverage, route/API freshness preservation, and backend durable audit persistence. |
| 2026-05-18 | `AUDIT-UI-006` markets rendered catalogue-unavailable status slice | Rendered Markets status strip when the catalogue/backend market sources are unavailable; narrow `UI-008`/`UI-MARKETS-001` evidence upgrade | Strengthened rendered SSR regression `AUDIT-UI-006 rendered markets dashboard does not turn unavailable sources into market counts` in `frontend/tests/rendered-operator-states.test.mjs`. It failed before implementation because the top status strip rendered `Catalogue` as `0` while the catalogue source was unavailable, even though later operator notes said `Unavailable`. `MarketOverviewDashboard` now renders the status-strip catalogue value as `Unavailable` when `initialCatalogueError`/catalogue load failure is present. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered markets dashboard' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 11 skipped`) and passed after (`1 passed, 11 skipped`). Files changed: `frontend/components/markets/market-overview-dashboard.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered Markets catalogue-unavailable count truth, not Verified for all Markets or market-data freshness behavior. Remaining evidence gaps: browser/e2e Markets behavior, catalogue/feed stale/disconnected fixtures, mutation-control refresh failures, route/API freshness preservation, test-only-control rendering, complete enum parity, AIMEE passive/advisory separation, and backend durable audit persistence. |
| 2026-05-18 | `AUDIT-UI-006` risk budget rendered exposure-unavailable slice | Rendered Risk page budget/exposure cards when allocation exposure backend truth is unavailable; narrow `RISK-017`/`UI-001`/`UI-008` evidence upgrade | Added rendered SSR regression `AUDIT-UI-006 rendered risk budget cards do not convert unavailable exposure into zero-risk truth` to `frontend/tests/rendered-operator-states.test.mjs`. It failed before implementation because the risk status strip rendered exposure as unavailable, but the Budgets and Exposure cards still showed `0.0%`, `0.0% headroom remaining`, and `0 reserved intents · 0 positions` from the unavailable fallback object. `RiskAllocationLive` now renders those derived exposure cards and live exposure hotspot cards as `Unavailable` with `Exposure read failed` copy when the exposure source is unavailable. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered risk budget cards' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 11 skipped`) and passed after (`1 passed, 11 skipped`); `npm run test:frontend` passed (`33 passed`); `npm run typecheck` passed. Files changed: `frontend/components/risk/risk-allocation-live.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered risk budget exposure-unavailable truth, not Verified for all risk or frontend degraded-state behavior. Remaining evidence gaps: browser/e2e risk behavior, refresh-failure stateful component tests, broader risk confidence/provisional/partial fixtures, passive-call breadth, test-only-control rendering, complete enum parity, AIMEE passive/advisory separation, route/API freshness preservation, and backend durable audit persistence. |
| 2026-05-18 | `AUDIT-API-008` required route audit slice | Control-plane and strategy operator mutation route audit persistence; narrow `AUDIT-TEST-002`/`AUDIT-OBS-001` evidence upgrade | Added session-bound required audit persistence for selected operator mutations. `DomainEventService.record_event_in_session()` persists `DomainEvent` rows through the active request/test session, and `persist_required_domain_event()` returns `503` instead of clean success when the required audit write fails. Routes updated: `PUT /control-plane/operator-state`, `POST /control-plane/reconcile`, `PUT /control-plane/governance/{strategy_name}`, `POST /strategy/start`, `POST /strategy/stop`, `POST /strategies/{name}/start`, and `POST /strategies/{name}/stop`. Regression suite `backend/tests/test_mutation_audit_events.py` failed before implementation (`6 failed`) and passed after the by-name route regression was added (`7 passed`). Adjacent verification `backend/.venv/bin/pytest backend/tests/test_mutation_audit_events.py backend/tests/test_operator_auth_boundary.py backend/tests/test_passive_read_routes.py backend/tests/test_control_plane_service.py -q` passed (`35 passed`), and the full backend suite passed (`256 passed`). Files changed: `backend/app/api/audit.py`, `backend/app/api/routes/control_plane.py`, `backend/app/api/routes/strategies.py`, `backend/app/services/domain_event_service.py`, `backend/tests/test_mutation_audit_events.py`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-API-008`, `AUDIT-TEST-002`, and `AUDIT-OBS-001` remain Open with narrower remaining scope. This slice is Fixed for selected control-plane/strategy operator route mutations, not Verified for all mutation/broker-action audit behavior. Remaining evidence gaps: allocation-alert mutations, review/advisory persistence, broker-action/lifecycle/reconciliation/recovery mutations, HTTP route harness behavior, transactional rollback semantics after audit failure, frontend display of new audit-write failure details, and broad service/background domain-event persistence still mocked by the default fixture. |
| 2026-05-17 | `AUDIT-UI-006` coverage rendered polling-fallback slice | Rendered Coverage strategy-watchlist row where watchlist membership says active/streamed but per-instrument feed state says polling fallback; narrow `ARCH-008`/`FLOW-MARKET-DATA-001`/`UI-011` evidence upgrade | Added rendered SSR regression `AUDIT-UI-006 rendered coverage watchlist does not tone polling fallback as healthy streaming` to `frontend/tests/rendered-operator-states.test.mjs`. It failed before implementation because `CoverageLive` rendered the watchlist row and `Polling fallback` stream pill as positive/healthy from `row.streamed` and `row.status="ACTIVE"` even while feed state said `stream_connected=false`, `streaming_now=false`, and `price_source="FALLBACK"`. `CoverageLive` now derives stream row/pill tone from per-instrument feed state first, shows fallback/disconnected/stale stream states as warning/negative instead of positive, exposes the backend `operator_action` as pill detail, and renders `No live tick` when no current live tick is available during fallback. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered coverage watchlist' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 9 skipped`) and passed after (`1 passed, 9 skipped`); `npm run test:frontend` passed (`31 passed`); `npm run typecheck` passed; `npm run lint` passed. Files changed: `frontend/components/coverage/coverage-live.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered coverage watchlist polling-fallback truth, not Verified for all coverage or market-data freshness behavior. Remaining evidence gaps: browser/e2e coverage behavior, coverage disconnected/stale fixture breadth, markets catalogue/feed semantics, dashboard polling/disconnected variants, route/API freshness preservation, refresh-failure states, passive-call breadth, test-only-control rendering, complete enum parity, AIMEE passive/advisory separation, and backend durable audit persistence. |
| 2026-05-17 | `AUDIT-UI-006` dashboard rendered stale-feed stream slice | Rendered Operate dashboard status strip with control-plane stale feed state and connected stream health; narrow `ARCH-008`/`FLOW-MARKET-DATA-001`/`UI-011` evidence upgrade | Added rendered SSR regression `AUDIT-UI-006 rendered dashboard stream strip preserves stale feed truth over connected stream` to `frontend/tests/rendered-operator-states.test.mjs`. It failed before implementation because `DashboardLive` rendered the top-level `Stream` status as `Live`/positive from `streamHealth.connected` even while `controlPlane.feed_source_state="STALE"` blocked entries and stale coverage evidence was visible elsewhere. `DashboardLive` now derives the Stream status strip from control-plane feed source first, rendering `Stale`, `Polling`, or `Disconnected` before falling back to raw stream connectivity. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered dashboard stream strip' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 8 skipped`) and passed after (`1 passed, 8 skipped`); `npm run test:frontend` passed (`30 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. Files changed: `frontend/components/dashboard/dashboard-live.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered dashboard stale-feed stream truth, not Verified for all dashboard or market-data freshness behavior. Remaining evidence gaps: browser/e2e dashboard behavior, dashboard polling-fallback/disconnected fixtures, coverage/markets freshness rendering, route/API freshness preservation, refresh-failure states, passive-call breadth, test-only-control rendering, complete enum parity, AIMEE passive/advisory separation, and backend durable audit persistence. |
| 2026-05-17 | `AUDIT-UI-006` live-view rendered all-source outage slice | Rendered Live System View with all critical backend sources unavailable; narrow `ARCH-008`/`UI-001`/`UI-011` evidence upgrade | Added rendered SSR regression `AUDIT-UI-006 rendered live view does not call all-source outage nominal or observation-only` to `frontend/tests/rendered-operator-states.test.mjs`. The test harness now stubs `next/link` so the live component can render in the Node SSR fixture. The regression failed before implementation because `LiveSystemView` rendered `Nominal live posture`, `Action Required` as `NO`/`Observation only`, and a positive no-unusual-activity empty state while all eleven source errors were present. The live hero now reports degraded source count, the action rail renders `UNKNOWN` with source-coverage degraded copy when source errors exist, and the anomaly empty state says unusual activity cannot be fully evaluated during degraded source coverage. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered live view' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 7 skipped`) and passed after (`1 passed, 7 skipped`); `npm run test:frontend` passed (`29 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. Files changed: `frontend/components/live/live-system-view.tsx`, `frontend/lib/live-system-view.ts`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered live-view all-source outage truth, not Verified for all live-view freshness/provenance behavior. Remaining evidence gaps: browser/e2e live-view behavior, stale-vs-delayed-vs-disconnected rendered fixtures, dashboard/coverage/markets freshness rendering, route/API freshness preservation, refresh-failure states, passive-call breadth, test-only-control rendering, complete enum parity, AIMEE passive/advisory separation, and backend durable audit persistence. |
| 2026-05-17 | `AUDIT-UI-006` control-plane rendered family open-risk slice | Rendered control-plane family row with aligned deployment but missing family-level open-risk state; narrow `STATE-010`/`UI-007` evidence upgrade | Added rendered SSR regression `AUDIT-UI-006 rendered control-plane family does not default missing open-risk state to no open risk` to `frontend/tests/rendered-operator-states.test.mjs`. It failed before implementation because `ControlPlaneLive` rendered the family row as positive and the risk cell as `NO_OPEN_RISK`. The control-plane family tone and detail helpers now treat missing/`UNAVAILABLE`/`UNKNOWN` family open-risk state as warning/unavailable truth, render `Open-risk state unavailable`, and keep the family in the issues set instead of the healthy-family set. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered control-plane family' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 6 skipped`) and passed after (`1 passed, 6 skipped`); `npm run test:frontend` passed (`28 passed`); `npm run typecheck` passed; `npm run lint` passed. Files changed: `frontend/components/control-plane/control-plane-live.tsx`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered control-plane family missing-open-risk truth, not Verified for all control-plane or open-risk behavior. Remaining evidence gaps: browser/e2e control-plane behavior, route/API open-risk preservation, restart/recovery/deployment visibility, loading/error/empty/stale/fallback breadth, refresh-failure states, test-only-control rendering, complete enum parity, AIMEE passive/advisory separation, and backend durable audit persistence. |
| 2026-05-17 | `AUDIT-UI-006` AIMEE rendered telemetry-unavailable slice | Rendered AIMEE Operate summary with clean reviewer facts but missing telemetry; narrow `AIMEE-010` evidence upgrade | Added rendered regression `AUDIT-UI-006 rendered AIMEE operate summary does not look healthy without telemetry source` to `frontend/tests/rendered-operator-states.test.mjs`. It failed before implementation because AIMEE rendered the top summary as `Healthy`, showed `0` warnings, and labelled missing telemetry as `Streaming disconnected` rather than source-unavailable truth. `buildSystemSummary()` now downgrades the Operate summary to degraded when telemetry is absent, includes source-missing warning count, and states that stream, broker, and freshness truth cannot be verified. `buildWhatMatters()` now labels the market-data card `Telemetry unavailable` when telemetry is absent, and `buildWarningItems()` adds a visible telemetry-source warning. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered AIMEE operate summary' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 1 failed, 5 skipped`) and passed after (`1 passed, 5 skipped`); `npm run test:frontend` passed (`27 passed`); `npm run typecheck` passed; `npm run lint` passed. Files changed: `frontend/components/aimee/utils.ts`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered AIMEE Operate telemetry-unavailable truth, not Verified for all AIMEE behavior. Remaining evidence gaps: AIMEE passive/advisory call separation, browser/e2e drawer behavior, loading/error/empty/stale/fallback breadth, additional freshness surfaces, refresh-failure states, test-only-control rendering, complete enum parity, and backend durable audit persistence. |
| 2026-05-17 | `AUDIT-UI-006` AIMEE rendered unavailable-truth slice | Rendered AIMEE control-plane source and open-risk unavailable states; narrow `AUDIT-UI-007`/`AIMEE-010` evidence upgrade | Added two rendered SSR regressions to `frontend/tests/rendered-operator-states.test.mjs`. Both failed before implementation: AIMEE rendered a missing control-plane source as `0` mismatch truth with no active warning, and rendered `open_risk_management_state="UNAVAILABLE"` as `No unmanaged or exit-only open risk is currently surfaced.` `buildSystemSummary()`, `buildWhatMatters()`, and `buildWarningItems()` now keep missing control-plane source and unavailable/unknown open-risk truth explicit; `AimeeShell` passes route context into AIMEE warnings and displays `Mismatches unknown` instead of `0 mismatches` when the control-plane snapshot is absent. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-006 rendered AIMEE' tests/rendered-operator-states.test.mjs` failed before implementation (`0 passed, 2 failed, 3 skipped`) and passed after (`2 passed, 3 skipped`); `npm run test:frontend` passed (`26 passed`); `npm run typecheck` passed; `npm run lint` passed. Files changed: `frontend/components/aimee/aimee-shell.tsx`, `frontend/components/aimee/utils.ts`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006` remains Open with narrower remaining scope. This slice is Fixed for rendered AIMEE control-plane/open-risk unavailable truth, not Verified for all AIMEE behavior. Remaining evidence gaps: AIMEE passive/advisory call separation, browser/e2e drawer behavior, loading/error/empty/stale/fallback breadth, freshness rendering, refresh-failure states, test-only-control rendering, complete enum parity, and backend durable audit persistence. |
| 2026-05-14 | `AUDIT-UI-006` rendered operator-state slice | Rendered frontend component evidence for pending execution notifications, allocation-alert mutation errors, and unknown/simulated risk truth; narrow `AUDIT-UI-002`/`AUDIT-UI-004` evidence upgrade | Added the first rendered SSR component harness in `frontend/tests/rendered-operator-states.test.mjs`. The targeted test failed before implementation (`0 passed, 3 failed`) because the needed renderable units and pending notification path were absent, then passed after implementation (`3 passed`). `NotificationCenter` now exposes renderable notification units and renders `SUBMISSION_PENDING` entries as warning pending broker-submission truth with manual-review fallback copy rather than raw status or opened-position truth. `RiskAllocationLive` now exposes `RiskAlertCard` and `RiskTruthConfidencePill`; rendered tests prove allocation-alert mutation errors preserve backend detail and `SIMULATED_LOCAL_FILL`/`UNKNOWN` risk confidence is non-exact. Verification commands: `node --test tests/allocation-alert-mutations.test.mjs tests/rendered-operator-states.test.mjs` passed (`5 passed`); `npm run test:frontend` passed (`24 passed`); `npm run typecheck` passed; `npm run lint` passed; `git diff --check` passed. Files changed: `frontend/components/dashboard/notification-center.tsx`, `frontend/components/risk/risk-allocation-live.tsx`, `frontend/tests/allocation-alert-mutations.test.mjs`, `frontend/tests/rendered-operator-states.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-006`, `AUDIT-UI-002`, and `AUDIT-UI-004` remain open with narrower scope. Remaining evidence gaps: no rendered/browser breadth for loading/error/empty/stale/fallback/passive/test-only/AIMEE/freshness states, no complete enum parity, no refresh-failure stateful component test, and no backend durable audit confidence for mutation routes. |
| 2026-05-14 | `AUDIT-UI-007` complete fallback/mock boundary closure | Frontend API client fallback boundary, production mock/demo/fake/fixture import guard, page load paths, unavailable shell naming, narrow source/static regressions | Closed `AUDIT-UI-007` for the production source/static boundary. `loadWithMeta()` no longer accepts caller-provided fallback data and returns `data: null` on failure. Frontend pages now call it with only a loader and pass explicitly named `UNAVAILABLE_*` shells alongside error metadata where component props still require shape-compatible unavailable state. Production `EMPTY_*` fallback symbols were removed/renamed. Regression `AUDIT-UI-007 loadWithMeta does not accept caller-provided backend-shaped fallback data` failed before implementation and now passes; `AUDIT-UI-007 production modules do not import mock demo fake or fixture backend data` provides the new static import guard. Verification commands: targeted `node --test --test-name-pattern='AUDIT-UI-007 loadWithMeta does not accept caller-provided backend-shaped fallback data|AUDIT-UI-007 production modules do not import mock demo fake or fixture backend data|AUDIT-UI-007 backend-unavailable control fallbacks|AUDIT-UI-007 backend-unavailable telemetry fallback|AUDIT-UI-007 backend-unavailable market overview' tests/*.test.mjs` passed (`5 passed`, rest skipped); `npm run test:frontend` passed (`21 passed`); `npm run typecheck` passed; `npm run lint` passed after unused import cleanup; `git diff --check` passed. Files changed: `frontend/lib/api.ts`, `frontend/app/page.tsx`, `frontend/app/live/page.tsx`, `frontend/app/coverage/page.tsx`, `frontend/app/control-plane/page.tsx`, `frontend/app/risk/page.tsx`, `frontend/app/markets/page.tsx`, `frontend/app/strategies/page.tsx`, `frontend/components/app-nav.tsx`, `frontend/components/aimee/aimee-shell.tsx`, `frontend/components/dashboard/autonomy-overview.tsx`, `frontend/components/dashboard/control-plane-strip.tsx`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | No remaining `AUDIT-UI-007` gap. Broad rendered component/browser proof is intentionally still tracked under `AUDIT-UI-006`, not claimed here. |
| 2026-05-14 | `AUDIT-UI-007` dashboard summary open-risk fallback slice | Frontend autonomy overview and control-plane strip missing open-risk defaults, narrow source regression | Implemented the smallest safe dashboard summary slice. `AutonomyOverview` and `ControlPlaneStrip` now default missing `open_risk_management_state` to `UNAVAILABLE` instead of `NO_OPEN_RISK`; the autonomy narrative states that open-risk state is unverified when unavailable/unknown, and the control-plane strip adds explicit unavailable copy while suppressing the no-exceptions path for unavailable open-risk truth. Regression `AUDIT-UI-007 dashboard summaries do not default missing open risk to no risk` failed before implementation (`0 passed, 1 failed, 18 skipped`) and now passes (`1 passed, 18 skipped`). Verification commands: `node --test --test-name-pattern='AUDIT-UI-007 dashboard summaries do not default missing open risk to no risk' tests/*.test.mjs` failed before the fix and passed after; `npm run test:frontend` passed (`19 passed`); `npm run typecheck` passed. Files changed: `frontend/components/dashboard/autonomy-overview.tsx`, `frontend/components/dashboard/control-plane-strip.tsx`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-007` silent fallback helper slice | Frontend API client fallback helper, narrow source regression | Removed unused `withFallback()` from `frontend/lib/api.ts`. Production code can no longer import a helper that catches request failures and returns fallback data without error metadata. Regression `AUDIT-UI-007 API client does not expose silent fallback helper` failed before implementation (`0 passed, 1 failed, 17 skipped`) and now passes (`1 passed, 17 skipped`). Verification commands: `node --test --test-name-pattern='AUDIT-UI-007 API client does not expose silent fallback helper' tests/*.test.mjs` failed before the fix and passed after; `npm run test:frontend` passed (`18 passed`); `npm run typecheck` passed. Files changed: `frontend/lib/api.ts`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-007` nav account-environment slice | Frontend global nav account-environment fallback, narrow frontend source regression | Implemented the smallest safe nav account-environment slice. The nav no longer hardcodes `Env`/`Demo` without backend account source truth; it now renders `Account Env`/`Unknown` and explains in the title that account environment is unavailable because no backend account source is loaded in the nav summary. Regression `AUDIT-UI-007 nav does not hardcode demo environment without backend source truth` failed before implementation (`0 passed, 1 failed, 16 skipped`) and now passes (`1 passed, 16 skipped`). Verification commands: `node --test --test-name-pattern='AUDIT-UI-007 nav does not hardcode demo environment' tests/*.test.mjs` failed before the fix and passed after; `npm run test:frontend` passed (`17 passed`); `npm run typecheck` passed. Files changed: `frontend/components/app-nav.tsx`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-007` telemetry unavailable fallback slice | Frontend operational telemetry backend-unavailable fallback values, live/coverage telemetry source fallback, narrow frontend source regression | Implemented the smallest safe telemetry fallback slice. `EMPTY_OPERATIONAL_TELEMETRY` now fails closed for backend-unavailable telemetry by using `entry_block_reason="backend_unavailable"`, `exit_block_reason="backend_unavailable"`, and `open_risk_management_state="UNAVAILABLE"` with an explicit unavailable reason instead of asserting `NO_OPEN_RISK`. Regression `AUDIT-UI-007 backend-unavailable telemetry fallback does not assert no open risk` failed before implementation (`0 passed, 1 failed, 15 skipped`) and now passes (`1 passed, 15 skipped`). Verification commands: attempted `npm run test:frontend -- --test-name-pattern='AUDIT-UI-007 backend-unavailable telemetry fallback'`, which failed because the npm script places the Node option after the test glob; `node --test --test-name-pattern='AUDIT-UI-007 backend-unavailable telemetry fallback' tests/*.test.mjs` failed before the fix and passed after; `npm run test:frontend` passed (`16 passed`); `npm run typecheck` passed; `git diff --check` passed. Files changed: `frontend/lib/api.ts`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-007` live-view derived-confidence slice | Frontend live-view trust rail derived confidence, backend-source error handling, narrow frontend source regression | Implemented the smallest safe derived-summary slice. `buildTrustRail()` now requires `missingSourceCount === 0` before rendering `Confidence = HIGH`; with any backend source unavailable, the live view remains at medium/low/unknown derived confidence and keeps the degraded-source meta. Regression `AUDIT-UI-007 live-view derived confidence cannot be high with unavailable sources` failed before implementation (`0 passed, 1 failed, 14 skipped`) and now passes (`1 passed, 14 skipped`). Verification: targeted regression passed, `npm run test:frontend` passed (`15 passed`), `npm run typecheck` passed, and `git diff --check` passed. Files changed: `frontend/lib/live-system-view.ts`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-007` markets unavailable fallback slice | Frontend markets overview backend-unavailable fallback values, `MarketStatus` frontend union, narrow frontend source regression | Implemented the smallest safe markets fallback slice. `EMPTY_FOREX_OVERVIEW` now renders source truth as unavailable rather than as a legitimate limited/on-demand market state: `status="UNAVAILABLE"`, `headline="Backend unavailable"`, epoch `nextTransitionAt`, `nextTransitionLabel="Unavailable"`, and detail copy saying counts are unavailable rather than zero market truth. Regression `AUDIT-UI-007 backend-unavailable market overview fallback is explicit, not limited market truth` failed before implementation (`0 passed, 1 failed, 13 skipped`) and now passes (`1 passed, 13 skipped`). Verification: targeted regression passed, `npm run test:frontend` passed (`14 passed`), `npm run typecheck` passed, and `git diff --check` passed. Files changed: `frontend/app/markets/page.tsx`, `frontend/lib/types.ts`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-007` control fallback fail-closed slice | Frontend control-plane and operating-limit backend-unavailable fallback values, dashboard/control-plane open-risk fallback display, narrow frontend source regression | Implemented the smallest safe slice for fallback truth. `EMPTY_CONTROL_PLANE_SUMMARY` and `EMPTY_SYSTEM_OPERATING_LIMITS` now fail closed for backend-unavailable control data, using false autonomy flags, blocked entry/exit reasons of `backend_unavailable`, and `open_risk_management_state="UNAVAILABLE"` instead of `NO_OPEN_RISK`. Dashboard and control-plane status strips now treat missing/`UNAVAILABLE`/`UNKNOWN` open-risk state as inactive/unavailable rather than positive. Regression `AUDIT-UI-007 backend-unavailable control fallbacks are explicit and fail closed` failed before implementation (`0 passed, 1 failed`) and now passes. Verification: targeted regression passed (`1 passed`), `npm run test:frontend` passed (`11 passed`), `npm run typecheck` passed, and `git diff --check` passed. Files changed: `frontend/lib/api.ts`, `frontend/components/dashboard/dashboard-live.tsx`, `frontend/components/control-plane/control-plane-live.tsx`, `frontend/tests/fallback-data-guard.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | Superseded by the complete fallback/mock boundary closure row above. This row records the narrower evidence available when the slice landed; broad rendered component/browser proof remains tracked under `AUDIT-UI-006`. |
| 2026-05-14 | `AUDIT-UI-002` pending/unknown enum truth slice | Frontend execution status tone/copy, risk truth confidence labels, narrow frontend source/helper regressions | Implemented the smallest safe slice for enum/status truth. Pending execution states now render as warning/pending truth rather than default positive activity, unsupported execution states render inactive, `SIMULATED_LOCAL_FILL` and `UNKNOWN` are part of the frontend risk-confidence union, simulated fills are labelled local/not broker-confirmed, and unknown/missing/unrecognized confidence is counted as degraded instead of estimated. Regressions `AUDIT-UI-002 pending execution statuses render as pending, not positive activity` and `AUDIT-UI-002 unknown and simulated risk confidence render degraded, not estimated` failed before implementation (`0 passed, 2 failed`) and now pass. Verification: targeted regression passed (`2 passed`), `npm run test:frontend` passed (`13 passed`), `npm run typecheck` passed, and `git diff --check` passed. Files changed: `frontend/lib/live-system-view.ts`, `frontend/lib/risk-allocation.ts`, `frontend/lib/types.ts`, `frontend/tests/ui-enum-truth.test.mjs`, `docs/audit-status.md`, and `docs/spec/99-spec-coverage-matrix.md`. | `AUDIT-UI-002` remains open. This is helper/source evidence only; no rendered component/browser harness proves operator-visible pending/unknown/simulated display across all critical surfaces, and the authoritative backend/frontend risk-confidence contract is still tracked under `AUDIT-005`. |
| 2026-05-12 | `AUDIT-UI-004` allocation-alert mutation error slice | Risk allocation alert acknowledge/resolve controls, per-alert pending/error/success state, narrow frontend source regression | Implemented the smallest safe slice for allocation alert mutation truth. `RiskAllocationLive` now catches acknowledge/resolve failures, displays backend `Error.message`/`HttpError.detail` through visible `Mutation failed: ...` copy, disables only the affected alert action while pending, and shows success only after refreshed backend alert truth returns. Regressions `AUDIT-UI-004 allocation alert mutation failures keep backend error detail visible` and `AUDIT-UI-004 allocation alert mutation success refreshes backend truth before success copy` failed against the old uncaught async transition path and now pass. Verification: targeted regression first failed (`0 passed, 2 failed`) before implementation, then passed (`2 passed`); `npm run test:frontend` passed (`10 passed`); `npm run typecheck` passed. | `AUDIT-UI-004` remains open pending rendered component/browser evidence that mocked mutation failures and refresh failures are visibly displayed to an operator. Keep broad frontend harness work under `AUDIT-UI-006`; keep backend durable mutation-audit confidence under `AUDIT-API-008`/`AUDIT-TEST-002`. |
| 2026-05-12 | `AUDIT-UI-001`/`AUDIT-UI-003` nav and broker-provenance slice | Global nav refresh/fallback truth, telemetry-derived broker status labels, live-view broker trust-rail provenance, narrow frontend static/source regressions | Closed `AUDIT-UI-001` and `AUDIT-UI-003` as fixed for their narrow source/static slices. The nav now tracks stream/control-plane load errors explicitly, avoids rendering initial or failed control-plane fallback as `Nominal`, and exposes unavailable/provenance details. Broker status derived from telemetry is labelled `Telemetry Connected`/`Telemetry Disconnected`, and the live trust rail source is `Telemetry-derived broker state` rather than `Broker-confirmed`. Regressions `AUDIT-UI-001 nav refresh failures render health as unknown instead of nominal` and `AUDIT-UI-003 live broker status is labelled as telemetry-derived unless broker-confirmed` failed before the fix and now pass. Verification: `node --test --test-name-pattern='AUDIT-UI-00[13]' tests/operator-status-provenance.test.mjs` (`2 passed`), `npm run test:frontend` (`8 passed`), `npm run typecheck` (passed), `npm run lint` (passed), and `git diff --check` (passed). | Keep broad rendered component/browser coverage, market/dashboard/live fallback paths, freshness/timestamp display, enum parity, and production mock/fallback static guards open under `AUDIT-UI-006`, `AUDIT-UI-007`, `UI-011`, and `AUDIT-UI-002`. |
| 2026-05-12 | `AUDIT-RISK-003` risk-page unavailable truth slice | Risk page initial load errors, risk live refresh errors, risk status-strip/empty-state fallback labels, narrow frontend risk unavailable regression | Closed `AUDIT-RISK-003` as fixed for the risk-page backend-unavailable/zero-risk substitution path. The risk page now passes `loadWithMeta()` error metadata into `RiskAllocationLive`; refresh failures update risk load quality; affected status strip values, hero pills, panel tones, and empty states render `Unavailable`/negative degraded labels instead of healthy zero/no-risk copy. Regression `AUDIT-RISK-003 risk load failures render as unavailable instead of zero-risk truth` and source-contract test `AUDIT-RISK-003 risk page passes load-error metadata to the risk console` failed before the fix and now pass. Verification: `node --test --test-name-pattern=AUDIT-RISK-003 tests/*.test.mjs` (`2 passed`, `4 skipped`), `npm run test:frontend` (`6 passed`), `npm run typecheck` (passed), `npm run lint` (passed), and `git diff --check` (passed). | Keep broader frontend fallback/default truth work open: enum/risk-confidence parity (`AUDIT-UI-002`), production fallback/mock guard (`AUDIT-UI-007`), and broad frontend behavioural harness (`AUDIT-UI-006`). Add rendered component/browser coverage before claiming full frontend confidence. |
| 2026-05-11 | `AUDIT-BROKER-004` sizing currency boundary | `BrokerRiskSizingQuote`, IG/fake sizing quotes, allocator serialization, decision risk currency persistence, execution risk snapshot fallback | Closed `AUDIT-BROKER-004` as fixed. Sizing account currency is now `BrokerRiskSizingQuote.account_currency`, serialized as a first-class broker-neutral quote field, and consumed by `TradeDecisionService`/`StrategyService` without looking inside adapter-owned `details`. Regressions failed against the old details-dependent behavior, then passed: `test_audit_broker_004_allocator_serializes_sizing_currency_as_first_class_quote_field`, `test_audit_broker_004_decision_uses_first_class_sizing_quote_currency`, and `test_audit_broker_004_execution_risk_snapshot_uses_first_class_sizing_quote_currency`. Adjacent broker sizing/allocator/decision/strategy/fake suites passed (`101 passed`). | No further action for this narrow boundary defect. Keep simulated/live provenance, broker-neutral exception handling, durable mutation audit persistence, and broker construction boundary work under their existing findings. |
| 2026-05-11 | `AUDIT-API-002`/`AUDIT-API-003` read/write boundary closure | `GET /coverage/summary`, `GET /strategy-watchlist`, `GET /market-data/feed-state`, review GET routes, active-read auth classification, adjacent watchlist/coverage/review route tests | Closed the coverage/watchlist/feed-state and review GET write-on-read findings as verified for direct route functions. Passive coverage/watchlist/feed-state calls now use `sync=False` snapshots and leave `WatchlistEntry.last_streamed_at`/`updated_at` unchanged; background streaming/Tier 2 paths retain explicit sync behavior. Review GETs default to non-persisting previews, while `persist=true` remains the explicit active-read persistence path and is still operator-auth classified. Regressions failed against the old behavior, then passed: `test_audit_api_002_strategy_watchlist_get_does_not_sync_watchlist_state`, `test_audit_api_002_feed_state_get_does_not_sync_watchlist_state`, `test_audit_api_002_coverage_summary_get_does_not_sync_watchlist_state`, `test_audit_api_003_review_gets_are_passive_without_explicit_persist`, and `test_audit_api_003_review_get_persist_true_is_explicit_active_read`. Full backend suite passed (`246 passed`). | No further action for this narrow batch. Keep HTTP `TestClient` route harness, frontend passive-call/degraded-state evidence, and response schemas under `AUDIT-TEST-001`, `AUDIT-UI-006`, and `AUDIT-API-006`. |
| 2026-05-11 | `AUDIT-API-001` control-plane/strategy default-seeding slice | `GET /control-plane/operator-state`, `GET /control-plane/strategies/{strategy_name}`, `GET /strategies`, operator-control summary reads, governance/deployment list helpers, adjacent control-plane/strategy service suites | Closed `AUDIT-API-001` as fixed for default seeding from passive reads. Passive operator-state reads now synthesize configured/effective autonomy from settings plus any existing override row without creating `OperatorControlState(id=1)`. Passive control-plane and strategy reads now use existing governance/deployment rows only and render missing governance as `UNKNOWN`; explicit seeding remains available in governance/deployment mutation/system workflows. Regressions `test_audit_api_001_operator_state_get_does_not_seed_default_row`, `test_audit_api_001_control_plane_strategy_detail_does_not_seed_governance`, and `test_audit_api_001_strategy_list_get_does_not_seed_governance` failed against the old behavior and now pass. Verification: targeted passive-read/control-plane suites and adjacent strategy/auth/telemetry suites passed. | No further action for this default-seeding slice. Coverage/watchlist/feed-state and review GET write-on-read slices are now verified under `AUDIT-API-002`/`AUDIT-API-003`; schema gaps and broader HTTP/frontend route evidence remain open. |
| 2026-05-11 | `AUDIT-API-005`/`AUDIT-API-007` dashboard/AIMEE broker-read slice | `DashboardService.get_dashboard()`, `GET /dashboard` passive behavior, `AimeeReadService.get_snapshot()` via `AIReviewerService.get_operator_summary(persist=False)`, adjacent dashboard/AIMEE backend suites | Closed `AUDIT-API-005` and `AUDIT-API-007` as verified. Dashboard KPIs now use persisted trades/positions only, return `brokerInfo=None`, and no longer call broker account summary. AIMEE passive snapshot inherits that persisted-only dashboard path and no longer performs an indirect broker account read. Regressions `test_audit_api_005_dashboard_passive_read_does_not_call_broker_account` and `test_audit_api_007_aimee_passive_snapshot_does_not_call_broker_account` failed against the old swallowed broker-read behavior and now pass. Verification: `backend/.venv/bin/pytest backend/tests/test_dashboard_service.py backend/tests/test_aimee_read_service.py -q` (`6 passed`). | No further action for this narrow broker-read bug. Later `AUDIT-API-002`/`AUDIT-API-003` work closed the known coverage/watchlist/feed-state and review GET read/write slices; schema gaps and broad route/frontend test evidence remain open. |
| 2026-05-11 | `AUDIT-LIFE-004` runtime ownership slice | Direct `StrategyService.set_runtime_mode()` ownership preservation, persisted `StrategyRuntimeState.control_mode`, adjacent strategy/control-plane/recovery runtime suites | Fixed `AUDIT-LIFE-004` for the direct runtime-mode path. `set_runtime_mode()` no longer passes `control_mode="AUTO"` into runtime-state sync, so existing `MANUAL` ownership is preserved while runtime mode changes. Regression `test_audit_life_004_set_runtime_mode_preserves_manual_runtime_ownership` failed against the old behavior and now passes. Verification: targeted regression passed; adjacent suites passed (`backend/tests/test_strategy_service.py`, `backend/tests/test_control_plane_service.py`, `backend/tests/test_runtime_recovery_service.py`). | No further action for this direct ownership bug. Broader route/UI runtime visibility and transition-table enforcement remain under `AUDIT-TEST-001`, `AUDIT-UI-006`, and `AUDIT-LIFE-006`. |
| 2026-05-10 | `AUDIT-003` allocation-alert passive-read closure | `GET /allocation/alerts`, `GET /allocation/alerts/unresolved-critical`, allocation-alert auth classification, passive dashboard/live/risk frontend calls | Closed `AUDIT-003` as verified. Allocation alert service and route defaults are now passive (`refresh=false`), unresolved-critical reads persisted alerts without refresh, and only explicit `/allocation/alerts?refresh=true` remains auth-classified as active read/refresh. Passive dashboard/live/risk page loads and polling no longer request `refresh=true`; explicit alert mutation follow-up may still refresh. Verification: targeted backend tests passed (`5 passed`) and targeted frontend static test passed. | No further action for `AUDIT-003`. Route-wide no-write proof remains under `AUDIT-TEST-001`; later `AUDIT-API-002`/`AUDIT-API-003` work closed the other known write-on-read GET slices. |
| 2026-05-10 | `AUDIT-RISK-002` closure | Execution-time broker/account/sizing/market-status revalidation plus submitted/fill material drift route and frontend visibility evidence | Closed `AUDIT-RISK-002` as verified. Backend execution now fails closed for account availability, depleted available funds, material account-equity drift, sizing quote availability/precision, exact and approximate sizing-quote size drift, exact and stable-size approximate executable-risk drift, final broker metadata failure, live approximate-sizing, and fresh broker market-status drift before `place_order`. `/executions` serializes `risk_reconciliation`, `material_execution_drift`, and `critical_execution_drift`. Frontend execution notifications use a testable helper and behavioral tests prove material, critical, and no-drift labels. Verification: `npm run test:frontend` passed (`3 passed`), frontend typecheck passed, full backend suite passed, and `git diff --check` passed. | No further action for `AUDIT-RISK-002`. Broader frontend harness/degraded-state coverage remains under `AUDIT-UI-006`, route/API audit depth under `AUDIT-TEST-001`, durable audit persistence under `AUDIT-TEST-002`, and risk read-surface fallback truth under `AUDIT-RISK-003`. |
| 2026-05-10 | `AUDIT-RISK-002` submitted/fill drift route/UI contract slice | `/executions` serialization, submitted/fill material drift route contract, frontend execution typing and notification display | Added explicit `risk_reconciliation`, `material_execution_drift`, and `critical_execution_drift` fields to `ExecutionResponse`, derived from persisted execution risk reconciliation flags. Dashboard execution notifications now type and display material/critical risk drift instead of relying on opaque nested details. Regression `test_audit_risk_002_execution_response_surfaces_material_risk_drift` failed against the old route model, then passed. Targeted route/strategy regressions passed (`4 passed`), `backend/tests/test_execution_routes.py` passed (`2 passed`), and `npm run typecheck` passed. | Closed by the 2026-05-10 `AUDIT-RISK-002` frontend behavioral test slice. |
| 2026-05-10 | `AUDIT-RISK-002` approximate sizing-quote size-drift slice | Entry execution boundary, approximate broker sizing quote executable-size drift before submission | Added a fail-closed check for material drift between the allocated executable size and the execution-time approximate broker sizing quote size. Approximate quote drift now uses allocated executable size/risk baselines rather than pre-budget quote values, so normal budget-capped demo execution is preserved while explicit non-exact metadata drift fails before `place_order`. Regression `test_audit_risk_002_execution_blocks_material_approximate_size_drift` failed against the old behavior, then passed. Targeted approximate size/unit-risk regressions passed (`2 passed`), `backend/tests/test_strategy_service.py` passed (`43 passed`), and adjacent allocation-read/execution-route tests passed (`11 passed`). | Closed by the 2026-05-10 `AUDIT-RISK-002` route and frontend behavioral slices. |
| 2026-05-10 | `AUDIT-RISK-002` stable-size approximate executable-risk drift slice | Entry execution boundary, approximate broker sizing quote unit-risk drift before submission | Added a fail-closed check for material drift between approved risk and current approximate executable risk (`normalized_size * risk_per_unit`) when allocation-time and execution-time quote sizes still match, so a non-exact unit-risk change cannot reach `place_order` just because the executable size is unchanged. Regression `test_audit_risk_002_execution_blocks_material_approximate_unit_risk_drift` failed against the old behavior, then passed. Targeted strategy-service suite passed (`42 passed`) and adjacent allocation-read suite passed (`10 passed`). | Closed by the later 2026-05-10 `AUDIT-RISK-002` approximate-size and route/frontend behavioral slices. |
| 2026-05-10 | `AUDIT-RISK-002` exact executable-risk drift slice | Entry execution boundary, exact broker sizing quote unit-risk drift before submission, deterministic initial execution transition timing | Added a fail-closed check for material drift between approved risk and current exact executable risk (`normalized_size * risk_per_unit`) even when the broker-normalized executable size has not changed. If broker metadata changes unit risk enough that the same submitted size would exceed approved risk, the execution and intent fail with `execution_revalidation_failed` / `sizing_quote_risk_drift` evidence before `place_order`. Also fixed new `SUBMISSION_PENDING` executions to use `signal_time` as their initial `last_transition_at`, preventing wall-clock weekday/session state from changing deterministic backend tests or the attempt's initial lifecycle timestamp. Regression `test_audit_risk_002_execution_blocks_material_broker_unit_risk_drift` failed against the old behavior, then passed. Targeted strategy/allocation/control-plane/lifecycle suites passed (`76 passed`) and the full backend suite passed (`228 passed`). | Closed by the later 2026-05-10 `AUDIT-RISK-002` approximate-size and route/frontend behavioral slices. |
| 2026-05-08 | `AUDIT-RISK-002` exact sizing-quote drift slice | Entry execution boundary, exact broker sizing quote revalidation, pre-submit risk drift evidence | Added a fail-closed check for material drift between allocation-time exact broker sizing quote size and execution-time exact broker sizing quote size. If exact broker metadata changes so the approved risk now maps to a materially different executable size, the execution and intent fail with `execution_revalidation_failed` / `sizing_quote_drift` evidence before `place_order`. Regression `test_audit_risk_002_execution_blocks_material_broker_sizing_quote_drift` failed against the old behavior, then passed. Full strategy-service suite passed (`40 passed`) and adjacent allocation-read/market-status/execution-route suites passed (`21 passed`). | Closed by the later 2026-05-10 `AUDIT-RISK-002` executable-risk, approximate, and route/frontend behavioral slices. |
| 2026-05-08 | `AUDIT-RISK-001` partial-fill residual accounting | Partial-fill execution risk persistence, allocation exposure read model, reserved/live/provisional risk split | Fixed partial-fill exposure accounting so filled-size risk is persisted on the provisional live `Position`, while the unfilled residual stays visible as reserved/leaves risk in `AllocationReadService.get_exposure_summary()` with `partial_fill_residual` basis. Regression `test_audit_risk_001_partial_fill_preserves_residual_reserved_and_provisional_live_risk` failed against the old read model, then passed. Targeted allocation-read suite passed (`10 passed`) and targeted strategy-service suite passed (`39 passed`). | No further backend/read-model action for `AUDIT-RISK-001`. Frontend behavioural risk-surface fixture evidence remains under `AUDIT-UI-006`/`RISK-017`; submitted/fill drift route/frontend proof is verified under `AUDIT-RISK-002`. |
| 2026-05-08 | `AUDIT-BROKER-002` closure | Full broker fake contract matrix: strict read/sizing/normalization/position outcomes, exact/contract/approx sizing, incomplete profiles, entry/close final and non-final outcomes, retry/idempotency safety, service-level strict reconciliation input use | Closed `AUDIT-BROKER-002` as verified for fake fidelity. Added final contract/service regressions for exact point-value, exact contract-risk, approximate price-delta, unsupported fake sizing profiles, partial/rejected/failed/cancelled entry outcomes, unqueued mutation retries that must not auto-succeed, and reconciliation consuming strict queued position outcomes. Targeted broker/reconciliation suites passed (`34 passed`), then the full backend suite passed (`226 passed`). | No further action for broker fake fidelity. Production simulated-vs-broker-confirmed provenance remains open under `AUDIT-LIFE-005`/`BROKER-014`; route/audit persistence remains under `AUDIT-TEST-001`/`AUDIT-TEST-002`; execution-time broker drift remains under `AUDIT-RISK-002`. |
| 2026-05-08 | `AUDIT-BROKER-002` fake IG-shaped sizing slice | Broker fake sizing derivation, IG adapter `ig_sizing` metadata, exact point-value fake parity | Updated `FakeBroker.quote_risk_sized_order()` so production-shaped `BrokerMarketDetails.metadata["ig_sizing"]` is translated into exact point-value sizing instead of silently falling back to approximate sizing. Regression `test_audit_broker_002_fake_sizing_understands_ig_point_value_metadata` failed against the old fake behavior, then passed. Adjacent IG sizing, allocator, and allocation-read suites passed (`27 passed`), and the full backend suite passed (`214 passed`). | Later 2026-05-08 closure work verified the broader fake matrix. Production simulated-vs-live provenance remains under `AUDIT-LIFE-005`. |
| 2026-05-08 | `AUDIT-BROKER-002` fake position/reconciliation input slice | Broker fake remote positions, strict position defaults, unavailable/explicit reconciliation inputs | Added `position_outcomes` and `require_explicit_positions` to `FakeBroker`. Tests can now opt out of implicit empty remote-position truth and queue `get_positions()` exceptions or explicit broker positions for reconciliation/recovery paths. Regression `test_audit_broker_002_fake_positions_can_require_explicit_reconciliation_truth` failed against the old fake behavior, then passed. Adjacent reconciliation/runtime-recovery suites passed (`11 passed`), and the full backend suite passed (`213 passed`). | Later 2026-05-08 closure work verified broader service-level strict reconciliation input use, retry/idempotency behavior, and derived sizing parity. Production provenance remains separate. |
| 2026-05-08 | `AUDIT-BROKER-002` fake sizing/normalization slice | Broker fake sizing quote reads, size normalization reads, strict sizing defaults, unavailable/unsupported/drift outcomes | Added strict fake sizing and normalization modes plus queued sizing quote and normalization outcomes to `FakeBroker`. Tests can now opt out of derived approximate sizing and queue sizing quote exceptions, unsupported sizing DTOs, normalization metadata-drift exceptions, and rejected normalization DTOs. Regressions `test_audit_broker_002_fake_sizing_quote_can_require_explicit_outcomes` and `test_audit_broker_002_fake_normalization_can_require_explicit_outcomes` failed against the old fake behavior, then passed. Adjacent strategy-service suite passed (`39 passed`). | Later 2026-05-08 closure work verified derived exact/contract/approx sizing parity, broader incomplete order outcomes, strict reconciliation input use, and retry/idempotency behavior. |
| 2026-05-08 | `AUDIT-BROKER-002` fake read-outcome slice | Broker fake account summary reads, market metadata reads, strict fake defaults, degraded/unavailable read DTOs | Added strict fake read modes and queued account/market read outcomes to `FakeBroker`. Tests can now opt out of healthy default account/market truth and queue account-read exceptions, zero-equity/depleted-account DTOs, market-read exceptions, and suspended/untradable metadata DTOs without monkeypatching broker methods. Regressions `test_audit_broker_002_fake_account_summary_can_require_explicit_read_truth` and `test_audit_broker_002_fake_market_details_can_require_explicit_read_truth` failed against the old fake behavior, then passed. Adjacent strategy-service suite passed (`39 passed`). | Later 2026-05-08 closure work verified explicit sizing/normalization queues, derived sizing parity, strict reconciliation input use, and retry/idempotency behavior. |
| 2026-05-08 | `AUDIT-BROKER-002` fake correlation slice | Broker fake queued order/close DTOs, non-final entry statuses, incomplete close statuses, client request id correlation | Added `backend/tests/test_broker_fake_contract.py` and updated `FakeBroker` so queued `BrokerOrderResult` values inherit the outgoing order/close `client_request_id` and missing `requested_size`. Regressions `test_audit_broker_002_fake_order_non_final_results_keep_request_correlation` and `test_audit_broker_002_fake_close_incomplete_results_keep_request_correlation` failed against the old fake behavior, then passed. Adjacent strategy-service suite passed (`39 passed`). | Later 2026-05-08 closure work verified retry/idempotency behavior, strict reconciliation input use, sizing/normalization coverage, and incomplete final entry outcomes. |
| 2026-05-08 | Frontend mock-data residue audit | `frontend/app/**/*`, `frontend/components/**/*`, `frontend/lib/api.ts`, `frontend/lib/types.ts`, risk/nav/markets/dashboard/live data-loading paths, backend allocation-alert refresh path | Added `AUDIT-UI-007`. No old production mock trade/position/portfolio dataset was found, but production frontend fallback/default backend-shaped objects remain and can substitute for backend failures, especially risk-page empty summaries, nav nominal status, and markets overview fallback. The app should contain no mocked/demo/fake backend data in production truth paths. | Remove or explicitly degrade all production fallback/mock data paths, add frontend behavioral tests for backend-unavailable states, and add static/import guards that keep mock/demo/fixture data out of production modules. |
| 2026-05-08 | `AUDIT-RISK-002` market-status cache slice | Entry execution boundary, market-status cache freshness, broker status drift before submission, allocation read timing fixtures | Added forced-refresh market-status revalidation at the final entry broker-submission boundary. A cached admission-time `TRADEABLE` result can no longer mask a fresh broker `CLOSED` status inside `market_status_cache_ttl_ms`; the execution and intent now fail with `execution_revalidation_failed` evidence before `place_order`. Regression `test_audit_risk_002_execution_revalidates_market_status_without_cache` passed, targeted market-status/strategy/allocation suites passed, and full backend suite passed (`195 passed`). | Closed by the later `AUDIT-RISK-002` broker metadata, route, and frontend behavioral slices. Broker-fake contract evidence is verified under `AUDIT-BROKER-002`. |
| 2026-05-08 | `AUDIT-RISK-002` broker-metadata failure slice | Entry execution boundary, final broker size normalization, metadata-read failure audit state | Added failure handling around final `normalize_order_size()` before entry submission. If broker metadata becomes unavailable after the market-status recheck but before order placement, the execution and intent now fail with `execution_revalidation_failed` / `broker_metadata_unavailable` evidence and `place_order` is not called. Regression `test_audit_risk_002_execution_metadata_failure_before_submission_is_audited` failed against the old behavior, then passed. Adjacent strategy/allocation-read/market-status suites passed (`57 passed`), and full backend suite passed (`196 passed`). | Closed by the later `AUDIT-RISK-002` broker metadata, route, and frontend behavioral slices. Broker-fake contract evidence is verified under `AUDIT-BROKER-002`. |
| 2026-05-08 | `AUDIT-RISK-002` account-drift slice | Entry execution boundary, current account funds, material equity drift before submission | Added final account drift checks before sizing quote/order submission. If available funds fall below approved risk, or current account equity makes the approved risk percent materially larger than the allocation-time risk percent, the execution and intent now fail with `execution_revalidation_failed` evidence and `place_order` is not called. Regressions `test_audit_risk_002_execution_blocks_material_account_equity_drift` and `test_audit_risk_002_execution_blocks_available_funds_below_approved_risk` failed against the old behavior, then passed. Adjacent strategy/allocation-read/market-status suites passed (`59 passed`), and full backend suite passed (`198 passed`). | Closed by the later `AUDIT-RISK-002` broker metadata, route, and frontend behavioral slices. Broker-fake contract evidence is verified under `AUDIT-BROKER-002`. |
| 2026-05-08 | `AUDIT-RUNTIME-001` closure | FastAPI lifespan startup leadership, runtime recovery loop gating, market-data/streaming loop gating, lease acquisition/expiry/takeover tests, and backend regression suite | Closed `AUDIT-RUNTIME-001` as verified for startup/reload/background-loop duplication. Added `RuntimeLease` and `RuntimeLeadershipService`, wired lifespan to acquire the DB-backed lease before recovery, market-data polling, streaming, and heartbeat loops, and added regressions for live lease duplicate blocking, lifespan skip behavior, leader lease release, and expired lease takeover. Targeted runtime/config suite passed (`45 passed`) and full backend suite passed (`192 passed`). | No further action for this narrow P0. Keep execution-time revalidation under `AUDIT-RISK-002` and migration framework hardening under `AUDIT-DB-001`; broker fake/idempotency evidence is verified under `AUDIT-BROKER-002`. |
| 2026-05-08 | `AUDIT-RISK-002` account/sizing slice | Entry execution boundary, broker account availability, broker sizing quote availability/precision, live approximate-sizing guard, allocation read evidence | Added final pre-submit account and sizing-quote revalidation in `StrategyService._execute_entry_signal()`. Account unavailable/invalid, unsupported quote, unavailable quote, and live approximate-sizing now fail closed before `place_order`, transition `Execution`/`TradeIntent` to failed revalidation, and preserve revalidation details. Targeted regressions passed and adjacent strategy/allocation/market-status suites passed (`68 passed`). | Later on 2026-05-08, the market-status cache, broker-metadata failure, and account-drift slices added more execution-time evidence. Closed by later `AUDIT-RISK-002` slices; broker-fake contract evidence is verified under `AUDIT-BROKER-002`. |
| 2026-05-08 | `AUDIT-SEC-001` closure | Backend operator auth policy, mutation/write-on-read route coverage, CORS configuration, frontend auth header propagation | Closed `AUDIT-SEC-001` as fixed. Backend regressions cover state-changing route policy coverage, write-on-read refresh auth classification, missing/invalid/unconfigured/valid production-like bearer-token behavior, and removal of the credentialed localhost regex from CORS. Frontend API client now sends the configured operator bearer token and `npm run typecheck` passed. | No further action for `AUDIT-SEC-001`; keep full HTTP route harness under `AUDIT-TEST-001` and frontend behavioural mutation-error tests under `AUDIT-UI-004`/`AUDIT-UI-006`. |
| 2026-05-08 | `AUDIT-LIFE-001` closure | Entry execution ambiguity handling, duplicate retry suppression, reconciliation correlation, `/executions` serialization, and frontend execution surfaces | Closed `AUDIT-LIFE-001` as verified. Backend regressions cover acknowledged-only, timeout, rate-limit/unknown, IG confirmation lookup failures, duplicate submitted-entry retry suppression, broker-reference reconciliation correlation, and execution response preservation. Frontend execution typing/display code was updated and `npm run typecheck` passed. | No further action for `AUDIT-LIFE-001`; broker-fake/idempotency evidence is verified under `AUDIT-BROKER-002`; frontend behavioural harness remains under `AUDIT-UI-006`. |
| 2026-05-07 | Product/docs accuracy audit | README, generated backend API route reference, docs/spec operator/API/test contracts, clean-clone backend/frontend setup simulation, FastAPI registered route inventory, tracked docs link portability, and external documentation-practice references | Added and then fixed `AUDIT-DOC-001` through `AUDIT-DOC-005`. README was compacted into a root entry point and detailed readiness, operator, architecture, and trade-lifecycle material was moved into focused docs. The generated API route-reference handoff, honest limitations, `/live` and AIMEE surface references, and portable links are now documented. `docs/spec/99-spec-coverage-matrix.md` links were also made portable. | No open product/docs accuracy follow-up remains from this run. |
| 2026-05-07 | Security, dependency, database, runtime, and observability audit | Secrets/env/git-history scan, route auth/CORS scan, broker/logging/error paths, Python/Node dependency audit and outdated checks, DB schema/migration drift checks, runtime startup/concurrency paths, health/domain-event observability paths, relevant specs and external best-practice references | Added `AUDIT-SEC-001` through `AUDIT-SEC-003`, `AUDIT-DEP-001`, `AUDIT-DB-001`, `AUDIT-RUNTIME-001`, and `AUDIT-OBS-001`; updated readiness blockers, remediation categories, backlog, phases, decisions, and audit summary. No current tracked `.env` secrets were found, but backend auth, runtime-loop duplication, redaction, dependency, migration, and durable observability gaps remained open at the time of this audit. | `AUDIT-SEC-001`, `AUDIT-RUNTIME-001`, and `AUDIT-BROKER-002` were fixed/verified on 2026-05-08. Next add redaction tests, dependency lock/audit CI, Alembic migrations, and durable domain-event/health aggregation tests. |
| 2026-05-06 | Test coverage and evidence audit | `backend/tests/**/*`, frontend test discovery, `backend/pyproject.toml`, `frontend/package.json`, `docs/spec/04-backend-api-contract.md`, `docs/spec/06-end-to-end-flows.md`, `docs/spec/07-state-machines.md`, `docs/spec/08-risk-allocation-invariants.md`, `docs/spec/10-testing-contract.md`, `docs/spec/99-spec-coverage-matrix.md`, `docs/audit-status.md` | Added `AUDIT-TEST-001` through `AUDIT-TEST-003`; upgraded `AUDIT-UI-006` to P1; updated `AUDIT-002` with current matrix extraction counts; added the test coverage audit snapshot. Backend service evidence is broad, but route/frontend/full-stack/e2e and domain-event persistence evidence are still not readiness-grade. | Build route tests first for passive no-write, active-read side effects, mutation/auth behavior, and response contracts; broker fake contract tests were added under `AUDIT-BROKER-002`; then add frontend critical-state tests before claiming full-stack coverage. |
| 2026-05-06 | Frontend operator console audit | `frontend/app/**/*`, `frontend/components/**/*`, `frontend/lib/api.ts`, `frontend/lib/types.ts`, `frontend/lib/format.ts`, `frontend/lib/live-system-view.ts`, `frontend/lib/risk-allocation.ts`, frontend route/spec references, UI/API/RISK/AIMEE/TEST specs | Added 6 frontend UI findings; updated `AUDIT-003`, `AUDIT-RISK-003`, and `AUDIT-API-004` with frontend evidence. AIMEE passive refresh currently calls `GET /aimee/snapshot` only, but frontend safety evidence is still review-only because no frontend test suite was found. | With P0 test-reset gating now verified, address nav fallback truth, enum parity, broker provenance, alert mutation error handling, active-read alert polling, and frontend degraded-state tests. |
| 2026-05-05 | Backend API route audit | `backend/app/api/router.py`, `backend/app/api/routes/*`, called backend services, `frontend/lib/api.ts`, API/AIMEE/architecture specs | 8 API findings opened plus `AUDIT-003` updated; 60 registered routes inventoried; no direct API broker-mutation route found, but multiple GET read/mutation classification violations found. | Resolve P0 test-route gating first, then reclassify/fix write-on-read GETs and schema gaps before claiming API contract readiness. |
| 2026-05-05 | Spec consistency audit | `docs/spec/`, `docs/audit-status.md` | 6 findings opened; no duplicate source IDs, missing source references, severity-label mismatches, or markdown table formatting failures found. | Resolve AUDIT-002, AUDIT-003, and AUDIT-005 before relying on the specs as a complete source of truth for backend/frontend/test audits. |
| 2026-05-05 | Backend lifecycle/state-machine audit | `backend/app/models/*`, lifecycle services, `backend/app/core/runtime.py`, relevant backend tests, lifecycle/broker/runtime/recovery specs | 6 lifecycle findings opened; relevant backend lifecycle tests passed locally (`58 passed`). Entry intent authority is mostly in place, but broker ambiguity, close-open-risk ownership, stopped-runtime recovery, manual/AUTO preservation, simulated provenance, and transition guards remain open. | Prioritize P0 lifecycle fixes before expanding route/frontend visibility tests; then add broker ambiguity, close retry, stopped-open-risk recovery, simulated provenance, and transition-table tests. |
| 2026-05-05 | InvestMate broker integration audit | Broker core/factory/IG adapter, streaming/fallback services, broker callers, fakes, broker-related backend tests, broker/risk/flow/testing specs, external IG/AWS/Azure/FIX/IBKR references | 5 broker-specific findings opened; existing lifecycle findings retained for ambiguity, close ownership, and simulated provenance. Broker DTO use is mostly neutral, but IG exception leakage, unknown-status fail-open behaviour, fake fidelity, sizing-details dependency, and direct broker construction boundaries remain open. | `AUDIT-BROKER-003`, `AUDIT-LIFE-001`, `AUDIT-LIFE-002`, and `AUDIT-BROKER-002` were later verified. Next add broker-neutral error/result/provenance DTOs before claiming broker-contract readiness. |
| 2026-05-05 | Risk and allocation behaviour audit | Capital allocator, portfolio risk, trade decision, strategy execution, allocation read/alert services, trade/allocation models, allocation routes, backend risk tests, frontend risk/dashboard surfaces, risk/flow/state/broker/UI/testing specs, external SEC/FIX/NIST references | 3 risk-specific findings opened and `AUDIT-003` updated with frontend auto-refresh evidence. Allocation admission is broadly fail-closed and intent-gated, but partial-fill exposure accounting, execution-time drift revalidation, and frontend risk fallback truth remain open. | Fix or explicitly document partial-fill reserved/live accounting first, then harden execution-time broker/account/market revalidation and add route/frontend degraded-state tests. |
