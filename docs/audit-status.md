# Audit status

This document is an audit log for InvestMate findings, evidence gaps, and follow-up actions. It is not a requirements spec; requirements live in `docs/spec/`.

## Current readiness blockers

| Severity | Finding ID | Why it blocks readiness |
| --- | --- | --- |
| P0 | AUDIT-LIFE-001 | Broker acknowledgement/confirmation ambiguity can be collapsed into exact entry success or exact entry failure instead of manual-review/reconciliation-needed state. |
| P0 | AUDIT-LIFE-002 | A failed or incomplete close can move the linked `TradeIntent` to terminal `FAILED`, after which normal exit-signal admission can no longer find the still-open risk. |
| P0 | AUDIT-LIFE-003 | Startup recovery skips adoption/recovery evidence for broker-confirmed open risk when the persisted runtime mode is `STOPPED`. |
| P0 | AUDIT-API-004 | `/testing/reset-history` is always registered and frontend-callable, but it deletes trading/audit history and has no production gating in the route/router. |
| P1 | AUDIT-LIFE-004 | `set_runtime_mode` persists `control_mode="AUTO"` regardless of existing runtime ownership, so a manual runtime can be silently reclassified. |
| P1 | AUDIT-LIFE-005 | Simulated fills/closes return the same broker-neutral `FILLED` result shape as live broker confirmations, with no durable provenance flag. |
| P1 | AUDIT-002 | `99-spec-coverage-matrix.md` does not yet cover or explicitly group all P0/P1 boundary and surface requirements, so future audits could miss safety-critical controls. |
| P1 | AUDIT-003 | Allocation alert route classification is internally inconsistent around `GET /allocation/alerts` default refresh/write-on-read behavior. |
| P1 | AUDIT-005 | `risk_truth_confidence` labels remain provisional rather than authoritative, which weakens backend/frontend parity audits for operator risk truth. |
| P1 | AUDIT-API-001 | Multiple GET routes documented as passive reads can create or update database rows through default seeding. |
| P1 | AUDIT-API-002 | Coverage/watchlist/feed-state GET routes can sync watchlist state and update persisted streaming metadata. |
| P1 | AUDIT-API-003 | Review GET endpoints persist `GeneratedReviewRecord` rows by default and need explicit active-read route tests. |
| P1 | AUDIT-API-005 | `/dashboard` performs a live broker account read despite passive-style route wording and raw response shape. |
| P1 | AUDIT-API-006 | Several frontend-consumed operator-critical routes still lack modelled or documented response schemas. |
| P1 | AUDIT-API-007 | AIMEE passive snapshot indirectly performs a broker account read through the reviewer dashboard path. |
| P1 | AUDIT-API-008 | Safety-relevant mutation routes can return success even if domain-event audit persistence fails. |

## Audit summary

| Area | Status | Blocking issues | Notes |
| --- | --- | --- | --- |
| Spec consistency | In progress | AUDIT-002, AUDIT-003, AUDIT-005 | Source IDs are unique; no missing source references or malformed markdown tables found. |
| Audit log structure | Updated | None | This file now records summary, statuses, severities, open findings, evidence, next actions, and run history. |
| Coverage matrix | Open | AUDIT-002 | Core invariant rows exist, but boundary/surface rows need explicit matrix coverage or stated grouping. |
| Backend API routes | In progress | AUDIT-003, AUDIT-API-001, AUDIT-API-002, AUDIT-API-004, AUDIT-API-005, AUDIT-API-006, AUDIT-API-007, AUDIT-API-008 | Implementation route audit completed for registered FastAPI routes and direct service call paths; route tests still pending. |
| Backend lifecycle | Audited | AUDIT-LIFE-001, AUDIT-LIFE-002, AUDIT-LIFE-003, AUDIT-LIFE-004, AUDIT-LIFE-006 | Entry intent authority is mostly enforced and tested, but ambiguity, close retry ownership, stopped-runtime recovery, manual/AUTO preservation, and transition-table enforcement need remediation. |
| Broker boundary | In progress | AUDIT-LIFE-001, AUDIT-LIFE-005, AUDIT-002 | Entry/close broker mutations are routed through strategy/recovery services, but broker-neutral result states and simulated/live provenance are too weak for the spec's ambiguity and truth-confidence expectations. |
| Frontend operator UI | Not started | AUDIT-002, AUDIT-005 | Future audit should prioritize provenance, enum parity, passive/mutation separation, and risk confidence labels. |
| Testing evidence | Not started | AUDIT-002, AUDIT-API-004 | Future audit should map P0/P1 rows to concrete test files and test names. |

## Open findings

| ID | Severity | Status | Area | Affected spec IDs | Finding | Evidence | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AUDIT-001 | P2 | Open | Spec location / audit scope | All spec files | The requested scope and previous audit log wording referenced `specs/`, but this repository's spec set is under `docs/spec/`. | No top-level `specs/` directory exists. `docs/audit-status.md` previously said requirements live in `specs/`; repo instructions and discovered files point to `docs/spec/`. | Keep future audit prompts, scripts, and audit-log wording aligned on `docs/spec/`, or create an explicit alias if a top-level `specs/` path is desired. |
| AUDIT-002 | P1 | Open | Coverage matrix | ARCH-BND-001 through ARCH-BND-006; BROKER-BND-002 through BROKER-BND-006; UI-OPERATE-001 through UI-STRATEGY-001; UI-BND-001 through UI-BND-007; FLOW-BND-001 through FLOW-BND-008; STATE-BND-001 through STATE-BND-009; RISK-BND-001 through RISK-BND-009; AIMEE-BND-001 through AIMEE-BND-008; TEST-BND-001 through TEST-BND-007 | P0/P1 boundary and surface requirements are not individually present in `99-spec-coverage-matrix.md` and are not explicitly grouped by ID in matrix notes. | Read-only extraction found 67 source P0/P1 IDs absent from matrix rows or explicit matrix text. The matrix maintenance rule allows grouping, but grouped coverage must be stated explicitly rather than implied. | Add matrix rows for these IDs, or update parent-row notes to list the exact grouped boundary/surface IDs and the evidence/gap that covers each group. |
| AUDIT-003 | P1 | Open | Route classification | API-001, API-002, API-008, ARCH-010, RISK-018, FLOW-RISK-001 | `GET /allocation/alerts` has inconsistent passive-vs-active wording across specs, and implementation confirms the default route is write-on-read. | `backend/app/api/routes/allocation.py` declares `refresh: bool = Query(default=True)` for `GET /allocation/alerts`; it calls `AllocationAlertService.list_alerts(refresh=refresh)`. `AllocationAlertService.refresh_alerts()` upserts/reopens/resolves alerts via `TradeService.upsert_allocation_alert()`, which calls `session.add()` and `session.commit()`. `GET /allocation/alerts/unresolved-critical` also calls `list_alerts(... refresh=True ...)`. | Classify default `GET /allocation/alerts` and `/allocation/alerts/unresolved-critical` as `ACTIVE_READ_REFRESH`, or make default passive with explicit refresh mutation. Add route tests proving the chosen behavior. |
| AUDIT-004 | P2 | Open | Central unresolved-items tracking | TERM-RISK-TRUTH-CONFIDENCE, TERM-PROVENANCE, TERM-READ-SERVICE, API-007, API-011, FLOW-* rows, TEST-009, multiple matrix rows | `Needs audit`, `Needs confirmation`, `Needs test`, and `Unknown` items are widespread in the specs but were not centrally summarized before this run. | Read-only scan found 103 occurrences across `docs/spec/01-domain-glossary.md`, `03-broker-contract.md`, `04-backend-api-contract.md`, `05-frontend-operator-ui-contract.md`, `06-end-to-end-flows.md`, `07-state-machines.md`, `08-risk-allocation-invariants.md`, `10-testing-contract.md`, and `99-spec-coverage-matrix.md`. | Create a follow-up evidence-gap index or extend the coverage matrix with filters for unresolved token, affected ID, owner, and next verification action. |
| AUDIT-005 | P1 | Open | Risk confidence terminology | TERM-RISK-TRUTH-CONFIDENCE, RISK-003, RISK-004, RISK-006, RISK-011, UI-RISK-001 | Risk confidence labels are intentionally provisional, but the spec set needs an authoritative label contract before backend/frontend audits can prove parity. | `01-domain-glossary.md` marks risk truth confidence as `Needs confirmation`; `08-risk-allocation-invariants.md` lists expected labels but says the exact enum needs confirmation; `99-spec-coverage-matrix.md` identifies the central enum/contract as missing. | Define the authoritative `risk_truth_confidence` label set in one spec section or schema contract, then require backend enum/read-model tests and frontend parity/unknown-label tests. |
| AUDIT-006 | P2 | Open | Verification wording | PROD-003, coverage matrix wording | One stale confidence phrase remains in a matrix note and should be normalized during the next spec cleanup. | `docs/spec/99-spec-coverage-matrix.md` line for `PROD-003` says "Current confidence is backend-heavy" while the rest of the spec set uses "Current verification confidence", "Current verification level", or scoped backend/frontend/overall confidence. | Reword the note to avoid stale "Current confidence" phrasing when specs are next edited. |
| AUDIT-API-001 | P1 | Open | Passive read side effects | API-001, API-008, ARCH-005, ARCH-010 | GET routes documented/classified as passive can seed default database rows. | `GET /control-plane/operator-state` calls `OperatorControlService.get_summary() -> get_state()`, which creates `OperatorControlState(id=1)` with `session.add()`/`commit()` when absent. `GET /control-plane/summary`, `GET /control-plane/strategies/{strategy_name}`, and `GET /strategies` call `StrategyGovernanceService.list_strategies() -> ensure_defaults()`, which creates or updates governance records with `session.add()`/`commit()`. | Reclassify these routes as `ACTIVE_READ_REFRESH` until default seeding is moved to startup/migration or a mutation workflow. Add no-write tests for passive reads and explicit seeding tests. |
| AUDIT-API-002 | P1 | Open | Passive read side effects | API-001, API-008, ARCH-005, ARCH-010 | Coverage/watchlist/feed-state GET routes mutate watchlist state while returning read models. | `GET /coverage/summary` calls `CoverageService.get_summary() -> WatchlistService.get_streaming_plan()` and `get_tier2_refresh_plan()`, which call `_sync_system_entries()`/`_sync_tier2_seed_entries()`, update `last_streamed_at`, and commit. `GET /strategy-watchlist` and `GET /market-data/feed-state` call `list_strategy_watchlist()`, which calls `_sync_system_entries()` and commits. | Reclassify these routes as `ACTIVE_READ_REFRESH` or split passive read from explicit sync/refresh. Add route tests proving database rows are unchanged for passive endpoints. |
| AUDIT-API-003 | P1 | Open | Review write-on-read | API-001, API-002, API-008, AIMEE-011 | Review GET endpoints persist `GeneratedReviewRecord` rows by default; this is documented as active-read behavior but remains a mutation-like GET used by frontend code. | `GET /reviews/operator-summary`, `/reviews/daily`, `/reviews/strategies/{strategy_name}`, `/reviews/runtime-health`, and `/reviews/trades/{trade_id}/postmortem` call `AIReviewerService` methods with default `persist=True`; `_persist_review()` calls `session.add()` and `session.commit()`. `frontend/lib/api.ts:getOperatorSummaryReview()` calls `/reviews/operator-summary`. | Keep these routes classified as `ACTIVE_READ_REFRESH`, add tests for persistence side effects, and consider moving persistence to POST or adding passive preview endpoints. |
| AUDIT-API-004 | P0 | Open | Test-only route gating | API-009, TEST-014, UI-009 | `/testing/reset-history` is always registered and can delete trading/audit history without route-level environment gating. | `backend/app/api/router.py` always includes `testing.router`. `POST /testing/reset-history` calls `HistoryResetService.clear_test_history()`, which deletes trades, intents, executions, reconciliation events, domain events, reviews, closed positions, and idle runtimes, then commits. `frontend/lib/api.ts:resetTestHistory()` and `frontend/components/testing/reset-history-button.tsx` expose a frontend caller. | Gate the route and frontend control by explicit dev/test configuration, or remove the route from production-like app registration. Add production-gating route/component tests. |
| AUDIT-API-005 | P1 | Open | Route classification / broker read | API-001, API-003, ARCH-010, ARCH-012, BROKER-015 | Dashboard is documented like a passive projection but performs a live broker account read and returns raw dict broker/account fields. | `GET /dashboard` calls `DashboardService.get_dashboard()`, which calls `_broker_info() -> get_broker().get_account_summary()`. The route docstring says it returns KPIs from persisted/runtime state only, and the spec inventory classifies it as `PASSIVE_READ`; actual behavior is broker-read capable. | Reclassify `/dashboard` as `BROKER_READ` or remove the live broker call from the dashboard read path. Add provenance/schema tests for `brokerInfo` and fallback behavior. |
| AUDIT-API-006 | P1 | Open | Response contract gaps | API-003, API-004, ARCH-009, ARCH-012, AIMEE-009 | Several frontend-consumed operator-critical routes return raw dict/list shapes rather than Pydantic models or documented dict schemas. | Raw dict/list routes consumed from `frontend/lib/api.ts` include `/dashboard`, `/control-plane/strategies/{strategy_name}`, `/control-plane/governance/{strategy_name}`, `/allocation/*`, `/markets/*`, `/watchlist/shortlist`, `/strategy-watchlist`, `/market-data/feed-state*`, `/live/instruments/{instrument_id}/chart`, `/strategy/start`, `/strategy/stop`, and `/aimee/snapshot`. | Add Pydantic response models or documented dict schemas for frontend-consumed operator-critical routes, then add contract tests against `frontend/lib/types.ts`. |
| AUDIT-API-007 | P1 | Open | AIMEE passive read boundary | AIMEE-001, AIMEE-006, AIMEE-008, AIMEE-011, API-001, ARCH-005 | `/aimee/snapshot` is side-effect conscious for review persistence, but it indirectly performs a broker account read through the reviewer dashboard path. | `AimeeReadService.get_snapshot()` calls `AIReviewerService.get_operator_summary(persist=False)`. That method builds `dashboard = DashboardService(...).get_dashboard()`, and `DashboardService._broker_info()` calls `get_broker().get_account_summary()`. The AIMEE spec says broker reads should be avoided unless explicitly documented, and current evidence says no broker reads are expected for passive snapshot. | Either remove broker-account reads from AIMEE passive snapshot, inject a passive persisted dashboard variant, or re-document and test the broker-read behavior with freshness/provenance and forbidden broker-mutation guards. |
| AUDIT-API-008 | P1 | Open | Mutation audit evidence | API-010, ARCH-011, PROD-012 | Safety-relevant mutation routes write domain events through a best-effort global event service whose failures are swallowed, so routes can return success without durable audit evidence. | Control-plane and strategy mutation routes call `domain_event_service.record_event(...)` after updates. `DomainEventService.record_event()` opens its own session, catches all exceptions, logs, and returns `None`; callers do not check the result. Affected routes include operator-state update, governance update, control-plane reconcile, and strategy start/stop. | For safety-critical mutations, persist audit evidence transactionally with the state change or fail/mark degraded if audit event persistence fails. Add route/service tests for audit-event failure behavior. |
| AUDIT-LIFE-001 | P0 | Open | Execution lifecycle / broker ambiguity | PROD-004, PROD-005, BROKER-002, BROKER-012, BROKER-013, STATE-002, STATE-006, STATE-008, FLOW-ENTRY-001 | Broker acknowledgement, timeout, or unclear confirmation can be represented as exact local entry success or exact local entry failure instead of manual-review/reconciliation-needed state. | `BrokerOrderStatus` only models `ACKNOWLEDGED`, `PARTIALLY_FILLED`, `FILLED`, `CANCELLED`, `REJECTED`, and `FAILED`; it has no timeout/pending/unknown/ambiguous status (`backend/app/core/broker.py:19`). `_transition_execution_from_broker_result()` maps every broker status other than partial/rejected/failed/cancelled to `ExecutionStatus.FILL_FULL` and `TradeIntentState.FILLED`, so an `ACKNOWLEDGED` result becomes fill truth (`backend/app/services/strategy_service.py:2557`). `IGBroker._wait_for_deal_confirmation()` raises after a confirmation timeout even though a deal reference may already exist (`backend/app/core/ig_broker.py:902`), and entry submission catches that exception as `ExecutionStatus.FAILED` with `requires_manual_review=False` and `TradeIntentState.FAILED` (`backend/app/services/strategy_service.py:1953`). Existing `test_entry_broker_failure_fails_safely_without_opening_position` asserts this timeout-like path is exact `FAILED` (`backend/tests/test_strategy_service.py:598`). | Add broker-neutral `PENDING`/`TIMEOUT`/`AMBIGUOUS` or equivalent result states and classify post-deal-reference confirmation failures as manual-review/reconciliation-needed. Entry `ACKNOWLEDGED` must not open a position without fill evidence. Add tests for acknowledged-only, confirmation-timeout-after-deal-reference, confirmation lookup failure, unknown broker response, rate-limit, and retry correlation with preserved `client_request_id`. |
| AUDIT-LIFE-002 | P0 | Open | Exit/close lifecycle / open-risk ownership | PROD-004, PROD-011, BROKER-004, BROKER-011, BROKER-012, STATE-005, STATE-007, STATE-008, FLOW-EXIT-001 | Close failure or partial/incomplete close preserves the `Position`, but moves the linked `TradeIntent` to terminal `FAILED`, which removes it from the normal close-admissible intent query and can strand still-open risk outside the strategy exit path. | `_execute_exit_signal()` changes the intent to `CLOSE_REQUESTED`, then to generic `SUBMITTED` for a close attempt (`backend/app/services/strategy_service.py:2340`). On broker close exception it marks the execution `NEEDS_MANUAL_REVIEW` but transitions the intent to `TradeIntentState.FAILED` (`backend/app/services/strategy_service.py:2389`). On non-`FILLED` close result it also transitions the intent to `FAILED` (`backend/app/services/strategy_service.py:2419`). `find_close_admissible_trade_intent()` only admits `PARTIALLY_FILLED`, `POSITION_OPENED`, `EXTERNAL_POSITION_ADOPTED`, and `RECOVERED_POSITION_ATTACHED`, not `FAILED`, `SUBMITTED`, or `CLOSE_REQUESTED` (`backend/app/services/trade_service.py:298`). Existing tests cover that the position stays open and execution needs manual review, but do not prove a subsequent exit/retry/recovery path remains close-admissible. | Keep open-risk intents close-admissible after failed/ambiguous close, or introduce explicit close-manual-review/reconciliation-needed states that preserve exit authority until broker/local risk is resolved. Add tests that after close timeout, partial close, rejected close, and ambiguous close confirmation, a subsequent close/recovery path can still act without creating duplicate exposure or losing audit correlation. |
| AUDIT-LIFE-003 | P0 | Open | Runtime recovery / stopped open risk | PROD-009, PROD-011, BROKER-008, STATE-005, STATE-010, FLOW-RUNTIME-001, FLOW-RECOVERY-001 | Startup recovery does not create adoption/recovery/manual-review evidence for broker-confirmed open risk when the persisted runtime has `runtime_mode="STOPPED"`; it marks the runtime stopped and continues before attaching the remote position. | `RuntimeRecoveryService.recover()` resolves `remote_position`, then checks `runtime.runtime_mode == "STOPPED"` before the code that creates a `Position` and `RECOVERED_POSITION_ATTACHED` intent for remote broker risk (`backend/app/services/runtime_recovery_service.py:196`). The adoption branch starts only after that stopped-runtime `continue` (`backend/app/services/runtime_recovery_service.py:215`). The existing stopped-mode test covers no remote open position, not stopped-with-broker-risk (`backend/tests/test_runtime_recovery_service.py:86`). | If a stopped runtime has broker-confirmed open risk, create or preserve explicit `RECOVERED_POSITION_ATTACHED`, reconciliation/manual-review, or unmanaged-open-risk evidence before leaving it stopped. Add a recovery test for `runtime_mode=STOPPED` plus matching remote broker position and no local position. |
| AUDIT-LIFE-004 | P1 | Open | Runtime ownership / direct state mutation | STATE-003, FLOW-RUNTIME-001, ARCH-013 | `set_runtime_mode()` changes persisted runtime ownership to `AUTO` even when the existing runtime was `MANUAL`, violating the manual-vs-auto preservation rule. | `StrategyService.set_runtime_mode()` writes the engine runtime mode and calls `RuntimeStateService.sync_engine_state(... control_mode="AUTO", runtime_mode=runtime_mode ...)` unconditionally (`backend/app/services/strategy_service.py:532`). `RuntimeStateService.sync_engine_state()` persists any non-null `control_mode` argument over the existing control mode (`backend/app/services/runtime_state_service.py:101`). No identified test covers a MANUAL runtime receiving `set_runtime_mode()` and remaining MANUAL. | Preserve the existing runtime `control_mode` unless an explicit owner-authorized control-mode transition is requested and audited. Add tests proving `set_runtime_mode`, restart, recovery, and deployment reconcile do not silently reclassify `MANUAL` to `AUTO`. |
| AUDIT-LIFE-005 | P1 | Open | Simulated vs broker-confirmed truth | BROKER-014, PROD-008, STATE-008, FLOW-ENTRY-001, FLOW-EXIT-001 | Simulated/local fills and closes are not durably distinguishable from live broker-confirmed truth in the broker-neutral result and downstream lifecycle records. | When trading is disabled, `IGBroker.place_order()` and `close_position()` return local simulated results (`backend/app/core/ig_broker.py:109`). `_simulate_place_order()` and `_simulate_close_position()` return `BrokerOrderStatus.FILLED` with ordinary broker references and no `simulated`/`provenance` field (`backend/app/core/ig_broker.py:731`). `BrokerOrderResult` has no source/provenance field (`backend/app/core/broker.py:61`). Downstream execution/position/trade states therefore receive the same fill/close status path as live confirmations unless callers infer from account type or logs. | Add broker-result provenance such as `source=SIMULATED`/`BROKER_CONFIRMED`, persist it into `Execution.details`, risk truth confidence/read models, and operator surfaces. Add backend tests proving simulated entry and close remain visibly simulated and do not claim broker-confirmed truth. |
| AUDIT-LIFE-006 | P2 | Open | Lifecycle transition maintainability / terminal protection evidence | STATE-001, STATE-002, STATE-007, PROD-010 | `TradeIntent` and `Execution` transitions are centralized in owner services but still accept arbitrary string states and have no transition table/terminal-state guard, so invalid owner-service calls are not mechanically blocked. | `TradeService.transition_trade_intent()` accepts `TradeIntentState | str` and assigns directly to `intent.state` (`backend/app/services/trade_service.py:361` and `backend/app/services/trade_service.py:392`). `TradeService.transition_execution()` accepts `ExecutionStatus | str` and assigns directly to `execution.status` (`backend/app/services/trade_service.py:500` and `backend/app/services/trade_service.py:525`). Current tests cover important public paths, but no transition-table tests prove terminal states such as `FAILED`, `CANCELLED`, `CLOSED`, or `CLOSE_CONFIRMED` cannot reactivate through owner-service misuse. | Add transition tables or validators for `TradeIntent` and `Execution`, then add valid/invalid transition tests including terminal-state reactivation and legacy-status write prevention. |

## Finding statuses

- Open
- In progress
- Fixed
- Verified
- Accepted risk
- Superseded

## Severity definitions

- P0: Could allow unsafe trading, unbounded risk, unauditable broker action, unmanaged open exposure, hidden state corruption, or false readiness for a safety-critical control.
- P1: Violates or obscures a documented invariant, breaks a core audit flow, weakens P0/P1 evidence, misleads the operator/auditor, hides degraded state, mutates unexpectedly, or weakens auditability.
- P2: Documentation drift, incomplete central tracking, ambiguous ownership, confusing terminology, missing evidence structure, or maintainability issue outside an immediate P0/P1 safety path.
- P3: Minor cleanup or editorial issue. The spec set currently uses P0-P2 for requirement severity; P3 is available for audit-log housekeeping if needed.

## Audit runs

| Date | Audit | Scope | Result | Follow-up |
| --- | --- | --- | --- | --- |
| 2026-05-05 | Backend API route audit | `backend/app/api/router.py`, `backend/app/api/routes/*`, called backend services, `frontend/lib/api.ts`, API/AIMEE/architecture specs | 8 API findings opened plus `AUDIT-003` updated; 60 registered routes inventoried; no direct API broker-mutation route found, but multiple GET read/mutation classification violations found. | Resolve P0 test-route gating first, then reclassify/fix write-on-read GETs and schema gaps before claiming API contract readiness. |
| 2026-05-05 | Spec consistency audit | `docs/spec/`, `docs/audit-status.md` | 6 findings opened; no duplicate source IDs, missing source references, severity-label mismatches, or markdown table formatting failures found. | Resolve AUDIT-002, AUDIT-003, and AUDIT-005 before relying on the specs as a complete source of truth for backend/frontend/test audits. |
| 2026-05-05 | Backend lifecycle/state-machine audit | `backend/app/models/*`, lifecycle services, `backend/app/core/runtime.py`, relevant backend tests, lifecycle/broker/runtime/recovery specs | 6 lifecycle findings opened; relevant backend lifecycle tests passed locally (`58 passed`). Entry intent authority is mostly in place, but broker ambiguity, close-open-risk ownership, stopped-runtime recovery, manual/AUTO preservation, simulated provenance, and transition guards remain open. | Prioritize P0 lifecycle fixes before expanding route/frontend visibility tests; then add broker ambiguity, close retry, stopped-open-risk recovery, simulated provenance, and transition-table tests. |
